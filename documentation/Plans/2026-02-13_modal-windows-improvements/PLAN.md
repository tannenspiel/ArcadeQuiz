# План: Modal Windows Improvements

**Дата:** 2026-02-13
**Статус:** 📋 PLANNED
**Приоритет:** MEDIUM

---

## ⚠️ КРИТИЧЕСКИЕ ОГРАНЕЧЕНИЯ

**Из UI-Layout критичности (НЕ НАРУШАТЬ!):**

> ❌ **НЕ менять** файлы расчёта размеров шрифтов:
> - `FontSizeCalculator.ts`
> - `ModalSizeCalculator.ts`
> - `KeyQuestionModal.ts` (особенно метод `createUI()`)
>
> ❌ **НЕ добавлять** новые импорты в эти файлы
> ❌ **НЕ менять** `camera.zoom()` или позиции

**Почему это важно:**
Расчёт размеров шрифтов зависит от:
1. `calculateUnifiedBaseFontSize(scene, currentLevel)` — только от уровня
2. `getBoundingClientRect()` — реальные размеры canvas
3. `cam.width`, `cam.height` — размеры камеры
4. `modalSize` из `ModalSizeCalculator`

**Любые изменения в этих файлах могут сломать систему размеров шрифтов!**

---

## Краткая сводка задач

| # | Задача | Сложность | Зависимости |
|---|---------|------------|-------------|
| 1 | Удалить неиспользуемые React компоненты | LOW | Нет |
| 2 | Создать ModalPositioningHelper | MEDIUM | 1 |
| 3 | Рефакторинг PortalModal (использовать ModalPositioningHelper) | MEDIUM | 2 |
| 4 | Рефакторинг GameOverModal (использовать ModalPositioningHelper) | MEDIUM | 2 |
| 5 | Исправить жёсткие размеры в CoinBubbleQuiz | MEDIUM | 2 |
| 6 | Добавить флаг отладки для логов кнопок | LOW | Нет |
| 7 | Обновить документацию | LOW | 1-6 |

---

## Детальный план с зависимостями

```
ШАГ 1: Удаление (безопасно)
    ├─> Удалить QuizModal.tsx
    ├─> Удалить GameOverModal.tsx
    ├─> Удалить тестовые файлы
    └─> Обновить импорты (если есть)

ШАГ 2: Создание ModalPositioningHelper (база для рефакторинга)
    ├─> Создать src/game/ui/ModalPositioningHelper.ts
    ├─> Вынести snapToGrid, snapToGridDouble
    ├─> Вынести calculateModalSize (обёртка, не меняя логику!)
    └─> Тесты для ModalPositioningHelper

ШАГ 3: Рефакторинг PortalModal (использовать ModalPositioningHelper)
    ├─> Заменить локальные snapToGrid на импортированные
    ├─> НЕ менять FontSizeCalculator.ts
    ├─> НЕ менять calculateModalSize логику
    └─> Тесты

ШАГ 4: Рефакторинг GameOverModal (использовать ModalPositioningHelper)
    ├─> Заменить локальные snapToGrid на импортированные
    ├─> НЕ менять FontSizeCalculator.ts
    ├─> НЕ менять calculateModalSize логику
    └─> Тесты

ШАГ 5: Исправление CoinBubbleQuiz (динамические размеры)
    ├─> Использовать ModalPositioningHelper
    ├─> Рассчитывать размеры динамически
    ├─> Добавить MIN_BUBBLE_SIZE
    └─> Тесты

ШАГ 6: Флаг отладки для логов кнопок
    ├─> Добавить DEBUG_BUTTON_EVENTS в gameConfig.ts
    ├─> Обновить Button.ts (условный logger.log)
    └─> Тесты

ШАГ 7: Обновление документации
    ├─> Обновить UI_TEXT_SCALING.md
    ├─> Обновить MODAL_GUIDE.md
    └─> Обновить PROJECT_MAP.md
```

---

## Шаг 1: Удалить неиспользуемые React компоненты

**Зависимости:** Нет
**Сложность:** LOW
**Риск:** MINIMAL (файлы не используются)

### Проверка использования (выполнена):

| Компонент | Статус | Используется в |
|-----------|----------|---------------|
| `LoadingSpinner.tsx` | ✅ АКТИВЕН | `App.tsx` |
| `UIOverlay.tsx` | ❌ НЕ ИСПОЛЬЗУЕТСЯ | Только тесты |
| `QuizModal.tsx` | ❌ НЕ ИСПОЛЬЗУЕТСЯ | Только тесты |
| `GameOverModal.tsx` | ❌ НЕ ИСПОЛЬЗУЕТСЯ | Только тесты |
| `LoadingScreen.tsx.deprecated` | ❌ УСТАРЕВШИЙ | Нигде |

### Действия:

1. **Удалить компоненты (3 файла):**
   - `src/react/components/QuizModal.tsx`
   - `src/react/components/GameOverModal.tsx`
   - `src/react/components/UIOverlay.tsx`

2. **Удалить тестовые файлы (3 файла):**
   - `src/tests/unit/react/QuizModal.test.tsx`
   - `src/tests/unit/react/GameOverModal.test.tsx`
   - `src/tests/unit/react/UIOverlay.test.tsx`

3. **Удалить устаревший файл:**
   - `src/react/components/LoadingScreen.tsx.deprecated`

4. **✅ НЕ УДАЛЯТЬ:**
   - `src/react/components/LoadingSpinner.tsx` — используется в `App.tsx`

5. **Запустить тесты:**
   ```bash
   npm run test
   ```

### Ожидаемый результат:
- ✅ 7 файлов удалены (3 компонента + 3 теста + 1 deprecated)
- ✅ Тесты проходят (~1800/1800 после удаления тестов)
- ✅ `LoadingSpinner.tsx` сохранён

---

## Шаг 2: Создать ModalPositioningHelper

**Зависимости:** Шаг 1
**Сложность:** MEDIUM
**Риск:** LOW (новый файл, не влияет на существующую логику)

### Действия:

1. **Создать файл:** `src/game/ui/ModalPositioningHelper.ts`

2. **Вынести общие функции** (без изменения логики!):

```typescript
// Grid Snapping функции (выносятся из всех модалок)
export const snapToGrid = (val: number, baseScale: number = BASE_SCALE): number => {
  return Math.round(val / baseScale) * baseScale;
};

export const snapToGridDouble = (val: number, baseScale: number = BASE_SCALE): number => {
  return Math.round(val / (baseScale * 2)) * (baseScale * 2);
};

// Обёртка для calculateModalSize (НЕ меняя логику!)
export const getModalSize = (
  camWidth: number,
  camHeight: number,
  canvasWidth: number,
  canvasHeight: number,
  padding: number,
  modalName: string
) => {
  // Прямой вызов существующей функции
  return calculateModalSize(camWidth, camHeight, canvasWidth, canvasHeight, padding, modalName);
};
```

3. **Создать тесты:** `src/tests/unit/ui/ModalPositioningHelper.test.ts`

### Ожидаемый результат:
- ✅ Файл создан
- ✅ Тесты проходят
- ✅ НЕ изменены FontSizeCalculator.ts, ModalSizeCalculator.ts, KeyQuestionModal.ts

---

## Шаг 3: Рефакторинг PortalModal

**Зависимости:** Шаг 2
**Сложность:** MEDIUM
**Риск:** MEDIUM (рефакторинг активного модального окна)

### Действия:

1. **Импортировать из ModalPositioningHelper:**
   - `snapToGrid`
   - `snapToGridDouble`
   - `getModalSize`

2. **Удалить локальные определения** (строки 148-149):
   ```typescript
   // ❌ Удалить:
   const snapToGrid = (val: number) => Math.round(val / BASE_SCALE) * BASE_SCALE;
   const snapToGridDouble = (val: number) => Math.round(val / (BASE_SCALE * 2)) * (BASE_SCALE * 2);
   ```

3. **Заменить вызовы calculateModalSize** на `getModalSize` (строка 137):
   ```typescript
   // ✅ Заменить:
   let modalSize = getModalSize(
     cam.width, cam.height, canvasWidth, canvasHeight,
     40, 'PortalModal'
   );
   ```

4. **НЕ МЕНЯТЬ:**
   - FontSizeCalculator.ts ❌
   - Логику calculateUnifiedBaseFontSize ❌
   - camera.zoom ❌

### Ожидаемый результат:
- ✅ PortalModal использует ModalPositioningHelper
- ✅ Тесты проходят
- ✅ Размеры шрифтов не изменились

---

## Шаг 4: Рефакторинг GameOverModal

**Зависимости:** Шаг 2
**Сложность:** MEDIUM
**Риск:** MEDIUM (рефакторинг активного модального окна)

### Действия:

1. **Импортировать из ModalPositioningHelper:**
   - `snapToGrid`
   - `snapToGridDouble`
   - `getModalSize`

2. **Удалить локальные определения** (строки 121-122):
   ```typescript
   // ❌ Удалить:
   const snapToGrid = (val: number) => Math.round(val / BASE_SCALE) * BASE_SCALE;
   const snapToGridDouble = (val: number) => Math.round(val / (BASE_SCALE * 2)) * (BASE_SCALE * 2);
   ```

3. **Заменить вызовы calculateModalSize** на `getModalSize` (строка 110):
   ```typescript
   // ✅ Заменить:
   let modalSize = getModalSize(
     cam.width, cam.height, canvasWidth, canvasHeight,
     40, 'GameOverModal'
   );
   ```

4. **НЕ МЕНЯТЬ:**
   - FontSizeCalculator.ts ❌
   - Логику calculateUnifiedBaseFontSize ❌
   - camera.zoom ❌

### Ожидаемый результат:
- ✅ GameOverModal использует ModalPositioningHelper
- ✅ Тесты проходят
- ✅ Размеры шрифтов не изменились

---

## Шаг 5: Исправление CoinBubbleQuiz

**Зависимости:** Шаг 2
**Сложность:** MEDIUM
**Риск:** MEDIUM (изменение логики расчёта размеров)

### Действия:

1. **Импортировать из ModalPositioningHelper:**
   - `snapToGrid`
   - `snapToGridDouble`

2. **Заменить жёсткие размеры** (строки 169-172) на динамические:
   ```typescript
   // ❌ Было:
   const bubbleWidth = modalWidth * 0.95;
   const bubbleHeight = modalHeight / 5;

   // ✅ Стало:
   const MIN_BUBBLE_SIZE = 60; // Минимальный размер в px
   const bubbleWidth = snapToGridDouble(Math.max(MIN_BUBBLE_SIZE, modalWidth * 0.95));
   const bubbleHeight = snapToGridDouble(Math.max(MIN_BUBBLE_SIZE, modalHeight * 0.2));
   ```

3. **НЕ МЕНЯТЬ:**
   - FontSizeCalculator.ts ❌
   - ModalSizeCalculator.ts ❌
   - Логику calculateUnifiedBaseFontSize ❌

### Ожидаемый результат:
- ✅ Размеры бабблов динамические
- ✅ Работает на экстремально узких экранах
- ✅ Тесты проходят

---

## Шаг 6: Флаг отладки для логов кнопок

**Зависимости:** Нет
**Сложность:** LOW
**Риск:** MINIMAL (добавление флага)

### Действия:

1. **Добавить в `src/config/gameConfig.ts`:**
   ```typescript
   export const DEBUG_BUTTON_EVENTS = false;
   ```

2. **Обновить `src/game/ui/Button.ts`:**
   ```typescript
   // ❌ Было:
   logger.log('BUTTON_EVENTS', 'Button clicked');

   // ✅ Стало:
   if (DEBUG_BUTTON_EVENTS) {
     logger.log('BUTTON_EVENTS', 'Button clicked');
   }
   ```

3. **НЕ МЕНЯТЬ:**
   - FontSizeCalculator.ts ❌
   - ModalSizeCalculator.ts ❌
   - KeyQuestionModal.ts ❌

### Ожидаемый результат:
- ✅ Логи кнопок отключены в production
- ✅ Можно включить флагом для отладки

---

## Шаг 7: Обновление документации

**Зависимости:** Шаги 1-6
**Сложность:** LOW
**Риск:** MINIMAL (только документация)

### Действия:

1. **Обновить `documentation/main/ui/MODAL_GUIDE.md`:**
   - Добавить описание ModalPositioningHelper
   - Обновить примеры кода

2. **Обновить `documentation/main/ui/UI_TEXT_SCALING.md`:**
   - Добавить раздел о неоднородности множителей
   - Объяснить, почему это особенность дизайна

3. **Обновить `documentation/export_llm/PROJECT_MAP.md`:**
   - Удалить упоминания QuizModal.tsx и GameOverModal.tsx

### Ожидаемый результат:
- ✅ Документация актуальна
- ✅ Неоднородность множителей задокументирована как feature

---

## Проверка перед завершением

### Тесты:
```bash
npm run test          # Все тесты проходят
npm run test:coverage # Покрытие не уменьшилось
```

### Визуальная проверка:
1. Открыть игру на разных устройствах
2. Проверить модальные окна:
   - KeyQuestionModal (вопросы ключей)
   - PortalModal (порталы)
   - GameOverModal (окончание игры)
   - CoinBubbleQuiz (монетки)

### Проверка размеров шрифтов:
1. Открыть консоль браузера
2. Проверить логи `MODAL_SIZE:`
3. Убедиться, что размеры **не изменились** от текущей реализации

---

## Примечания

### Почему НЕ унифицировать множители?

**PortalModal (фиксированный 1.3):**
- Короткие тексты: "ВОЙТИ В ПОРТАЛ", "ОТМЕНА"
- Фиксированный размер даёт предсказуемый результат
- Адаптивный множитель (1.26–1.54) сделает текст слишком крупным на узких экранах

**GameOverModal (фиксированный 2.0/1.3):**
- Заголовки "YOU WIN", "GAME OVER" — эмоциональный элемент дизайна
- Большой размер (2.0x) создаёт эффект победы/поражения
- Меньшая адаптивность компенсируется большим размером

### Зависимости от порядка вызовов

**КРИТИЧЕСКИ ВАЖНО:**
1. `calculateUnifiedBaseFontSize()` зависит ТОЛЬКО от `currentLevel`
2. `calculateModalSize()` зависит от размеров камеры
3. **НЕ** зависит от `GamePhase`, `hudManager.update()`, `gameState.setOracleActivated()`

**Поэтому при рефакторинге:**
- ✅ Можно выносить общие функции (snapToGrid)
- ✅ Можно менять порядок вызовов внутри одного модального окна
- ❌ НЕЛЬЗЯ менять FontSizeCalculator.ts
- ❌ НЕЛЬЗЯ менять ModalSizeCalculator.ts
- ❌ НЕЛЬЗЯ менять KeyQuestionModal.ts

---

**Дата создания:** 2026-02-13
**Версия плана:** 1.0
