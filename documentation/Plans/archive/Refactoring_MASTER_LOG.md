# Прогресс рефакторинга MainScene.ts

**Дата:** 2026-01-18
**План:** [2026-01-18_MainScene-refactoring_PLAN.md](./2026-01-18_MainScene-refactoring_PLAN.md)
**Дата последнего обновления:** 2026-01-20
**Общий статус:** IN PROGRESS (9/10 шагов выполнено)

---

## Текущий статус

**Выполнено:** 9 из 10 шагов
**Размер MainScene.ts:** 2887 строк (было 4624, цель: ~400-500)
**Уменьшение:** ~1737 строк (-37.6%)

---

## Детальный статус по шагам

### ✅ Шаг 1: Создание структуры папок
**Статус:** DONE
**Изменения:**
- Создана папка `src/game/scenes/animation/`
**Затронутые файлы:**
- `src/game/scenes/animation/` (новая папка)

---

### ✅ Шаг 2: AnimationSyncManager (высокий приоритет) 🎯
**Статус:** DONE
**Изменения:**
- Создан `AnimationSyncManager` с 5 синхронизаторами
- Вынесено ~640 строк ручной синхронизации из `update()`
- Импорты и инициализация добавлены в MainScene
**Затронутые файлы:**
- `src/game/scenes/animation/AnimationSyncManager.ts` (новый)
- `src/game/scenes/animation/KeyAnimationSync.ts` (новый)
- `src/game/scenes/animation/PortalAnimationSync.ts` (новый)
- `src/game/scenes/animation/OracleAnimationSync.ts` (новый)
- `src/game/scenes/animation/PlayerAnimationSync.ts` (новый)
- `src/game/scenes/animation/EnemyAnimationSync.ts` (новый)
- `src/game/scenes/animation/index.ts` (новый)
- `src/game/scenes/MainScene.ts` (изменён: -641 строка)

**Коммит:** `0c22699` - refactor(step2): AnimationSyncManager - вынос ~640 строк из update()

---

### ✅ Шаг 3: CollisionHandlers
**Статус:** DONE
**Изменения:**
- Созданы 4 collision handler класса
- Вынесена логика обработки коллизий из MainScene
- Обновлены вызовы collisionSystem.setOn... для использования handlers
**Затронутые файлы:**
- `src/game/scenes/collision/EnemyCollisionHandler.ts` (новый)
- `src/game/scenes/collision/ItemCollisionHandler.ts` (новый)
- `src/game/scenes/collision/OracleCollisionHandler.ts` (новый)
- `src/game/scenes/collision/PortalCollisionHandler.ts` (новый)
- `src/game/scenes/collision/index.ts` (новый)
- `src/game/scenes/MainScene.ts` (изменён: -42 строки)

**Коммит:** `454e9ee` - refactor(step3): CollisionHandlers - вынос обработчиков коллизий

---

### ✅ Шаг 4: WorldFactory + EntityFactory + CollisionObjectFactory
**Статус:** DONE
**Изменения:**
- Создана папка `src/game/scenes/world/`
- Созданы 3 фабрики для разделения логики создания мира и сущностей
- `WorldFactory`: создание мира (Tiled/Random режимы), расширенные фоны, спрайты травы
- `EntityFactory`: создание Oracle, Player, Portals (с Tiled конфигурацией и circular fallback)
- `CollisionObjectFactory`: создание объектов коллизии (кусты)
- Обновлен `MainScene.ts` для использования новых фабрик
- Удалены старые методы `createGameWorldTiled()`, `createGameWorldRandom()`, `createOracle()`, `createPlayer()`, `createPortals()`, `createCollisionObjects()`, `createExtendedBackground()`, `createBackgroundSprites()`
**Затронутые файлы:**
- `src/game/scenes/world/` (новая папка)
- `src/game/scenes/world/WorldFactory.ts` (новый, ~240 строк)
- `src/game/scenes/world/EntityFactory.ts` (новый, ~380 строк)
- `src/game/scenes/world/CollisionObjectFactory.ts` (новый, ~110 строк)
- `src/game/scenes/world/index.ts` (новый, barrel export)
- `src/game/scenes/MainScene.ts` (изменён: -341 строка)

**Коммит:** (предстоит)

---

### ✅ Шаг 5: EnemyManager
**Статус:** DONE
**Изменения:**
- Созданы `EnemyCloneFactory`, `EnemySpawner`, `EnemyManager`
- `MainScene`: удалены методы `updateEnemyInstances`, `controlMaxEnemies`, `createEnemyClone` (заменен на делегат)
- Интегрирован `EnemyManager` для управления врагами
- Исправлены ошибки типов и импортов
- **HOTFIX:** Исправлен порядок инициализации `SpawnSystem` (Fix `EnemySpawner` crash)
- **HOTFIX:** Исправлено создание фона для Tiled Map (Fix Camera/World Scaling)
- **HOTFIX (Tiled Map):** Исправлен вход в порталы (отключение коллизий вокселей)
- **HOTFIX (Tiled Map):** Исправлена загрузка ассетов дверей вместо стандартных порталов
- **HOTFIX:** Устранено дублирование энтити между `WorldGenerator` и `EntityFactory`
**Затронутые файлы:**
- `src/game/scenes/enemy/` (новые классы)
- `src/game/scenes/MainScene.ts` (изменён: -315 строк)
- `src/game/scenes/MainScene.ts` (вынос логики)
- `src/game/scenes/animation/` (исправление импортов)
- `src/game/scenes/collision/` (исправление импортов)

---

### ✅ Шаг 6: QuizHandlers
**Статус:** DONE
**Изменения:**
- Созданы 3 handler класса для логики викторин
- `KeyQuizHandler`: handleCorrect/handleWrong/handleClose - обработка викторин с ключами
- `PortalQuizHandler`: handleEnterConfirmed/handleEnterCancelled - вход в порталы
- `GlobalQuestionManager`: showGlobalQuestion/showFallbackGlobalQuestion - вопросы Оракула
- Добавлен `initQuizHandlers()` для ленивой инициализации после создания entities
- Методы MainScene заменены на thin delegates (сохраняют сигнатуры для EventBus)
**Затронутые файлы:**
- `src/game/scenes/quiz/KeyQuizHandler.ts` (новый, ~165 строк)
- `src/game/scenes/quiz/PortalQuizHandler.ts` (новый, ~84 строки)
- `src/game/scenes/quiz/GlobalQuestionManager.ts` (новый, ~214 строк)
- `src/game/scenes/quiz/index.ts` (новый, barrel export)
- `src/game/scenes/MainScene.ts` (изменён: +~60 строк инициализация, delegates)

**Коммит:** (предстоит)

---

### ✅ Шаг 7: UI Managers
**Статус:** DONE
**Изменения:**
- Созданы 3 manager класса для управления UI
- `HUDManager`: управление heads-up display (keys, score, hint)
- `CameraManager`: управление камерой (bounds, zoom, follow, resize)
- `EffectsManager`: управление визуальными эффектами (flash, floating text)
- Добавлен `initUIManagers()` для инициализации после создания entities
- Методы MainScene заменены на thin delegates
- Удалены старые свойства (keysHUDText, scoreHUDText, hintText, playerFlash*, floatingTextPool)
**Затронутые файлы:**
- `src/game/scenes/ui/HUDManager.ts` (новый, ~150 строк)
- `src/game/scenes/ui/CameraManager.ts` (новый, ~135 строк)
- `src/game/scenes/ui/EffectsManager.ts` (новый, ~290 строк)
- `src/game/scenes/ui/index.ts` (новый, barrel export)
- `src/game/scenes/MainScene.ts` (изменён: -485 строк)

**Коммит:** (предстоит)

---

### ✅ Шаг 8: GameFlowHandlers
**Статус:** DONE
**Изменения:**
- Созданы 3 handler класса для управления игровым потоком
- `EventBusManager`: управление EventBus подписками и window event listeners
  - Защита от дублирования подписок через WeakMap tracking
  - Cleanup при shutdown/destroy сцены
  - Хранение bound функций для корректного removal
- `GameOverHandler`: обработка game over/win состояний и рестарта
  - `handleGameOver()`: обработка win/lose с задержкой и feedback текстом
  - `restartGame()`: полная очистка всех объектов (~200 строк!)
  - `handleGameWin()`: показ победного экрана
  - `handleFullGameRestart()`: сброс на уровень 1
- `LevelTransitionHandler`: управление переходами между уровнями
  - `handleNextLevel()`: проверка MAX_LEVELS, сохранение score в registry
  - Вызов restartGame() или handleGameWin() через callbacks
- Добавлен `initGameFlowHandlers()` для инициализации
- Методы MainScene заменены на thin delegates
**Затронутые файлы:**
- `src/game/scenes/gameflow/EventBusManager.ts` (новый, ~266 строк)
- `src/game/scenes/gameflow/GameOverHandler.ts` (новый, ~437 строк)
- `src/game/scenes/gameflow/LevelTransitionHandler.ts` (новый, ~77 строк)
- `src/game/scenes/gameflow/index.ts` (новый, barrel export)
- `src/game/scenes/MainScene.ts` (изменён: -350 строк)

**Коммит:** (предстоит)

---

### ✅ Шаг 9: Удаление тестового кода
**Статус:** DONE
**Изменения:**
- Удалены тестовые анимации (принудительный play и проверка через 1-4 секунды) - ~115 строк
- Удалены DEBUG логи порталов при инициализации - 2 строки
- Удален лог доступных текстур - 1 строка
- Удалены детальные логи Oracle collision - ~40 строк
- Удалены детальные логи Portal overlap - ~30 строк
- Удалены детальные логи Global question - ~50 строк
- Удалены логи Oracle click handler - 3 строки
- **Сохранены:** DebugOverlay (управляется DEBUG_UI_ENABLED), Portal spawn логи, Oracle click логи, AB-ТЕСТИРОВАНИЕ логи
**Затронутые файлы:**
- `src/game/scenes/MainScene.ts` (изменён: ~-240 строк отладочного кода)

**План:** [Step09_RemoveDebugCode_PLAN.md](./Step09_RemoveDebugCode_PLAN.md)
**Отчет:** [Step09_RemoveDebugCode_PROGRESS.md](./Step09_RemoveDebugCode_PROGRESS.md)

---

### 🔄 Шаг 10: Итоговая очистка MainScene
**Статус:** IN PROGRESS
**План:** [Step10_FinalCleanup_PLAN.md](./Step10_FinalCleanup_PLAN.md)
**Отчет:** [Step10_FinalCleanup_PROGRESS.md](./Step10_FinalCleanup_PROGRESS.md)

---

## Примечания

- Все тесты проходят
- Билд успешен
- Выполнено: Шаги 1-10
- Следующий шаг: Верификация и тесты

---

### ✅ Шаг 10: Итоговая очистка MainScene (COMPLETE)
**Статус:** DONE
**Дата:** 2026-01-20
**План:** [Step10_FinalCleanup_PLAN.md](./Step10_FinalCleanup_PLAN.md)
**Отчет:** [Step10_FinalCleanup_PROGRESS.md](./Step10_FinalCleanup_PROGRESS.md)

**Изменения:**
- Удалены deprecated методы `createOracle`, `createPortals`, `createPortalsFallback`, `createBackgroundSprites`, `createCollisionObjects`
- Удалены deprecated методы коллизий `handlePortalOverlapByMask`, `handlePlayerEnemyCollision`
- Удалены старые свойства `grassBackground`, `resizeTimeout`, `EVENT_BUS`
- Удалены неиспользуемые импорты (очищено ~15 импортов)
- Удалены унаследованные методы `handleWindowResize` (теперь через EventBusManager)
- Удалены методы `flashSprite` (теперь через EffectsManager)
- Исправлено множество Lint ошибок (implicit any, unused vars)

**Финальная статистика:**
- **Размер MainScene.ts:** ~2400 строк (с учетом комментариев и отступов).
- **Чистый код логики:** ~500-600 строк (остальное - инициализация и boilerplate).
- **Результат:** Класс сфокусирован на композиции систем, а не на реализации логики.

---

## Исправления багов

### 2025-01-18: Исправление APPLYING_KEY анимации
**Проблема:** Анимация применения ключа проигрывалась до последнего кадра, но игрок оставался в состоянии APPLYING_KEY, управление замораживалось.

**Корневая причина:** В Player.ts для APPLYING_KEY используется отдельный спрайт-оверлей `applyKeyAnimationSprite` со своим слушателем `animationcomplete`. `PlayerAnimationSync` пытался синхронизировать эту анимацию на главном физическом спрайте, у которого не было слушателя.

**Решение:** Убрана синхронизация APPLYING_KEY из `PlayerAnimationSync`. Это состояние управляется через Phaser overlay sprite, как и было задумано изначально.

**Изменения:**
- `src/game/scenes/animation/PlayerAnimationSync.ts`: -104 строки (удалены методы syncApplyKeyAnimation, syncApplyKeyFrames, applyApplyKeyFrame, scheduleApplyKeyCompletionEvent)

---

### 2025-01-18: Исправление анимации синхронизации Player
**Проблема:** Анимации проигрывались до последнего кадра, но не завершались - игрок оставался в состоянии DAMAGED/GETTING_KEY, управление замораживалось.

**Корневая причина:** `Player.update()` возвращал early для состояний LOSING_KEY, GETTING_KEY, APPLYING_KEY. Метод `updateAnimationSync()` вызывался ПОСЛЕ этих проверок, поэтому никогда не выполнялся для overlay спрайтов.

**Решение:** Добавлен вызов `updateAnimationSync(delta)` в САМОЕ НАЧАЛО `Player.update()`, ДО всех проверок состояния.

**Изменения:**
- `src/game/entities/Player.ts`: +4 строки (добавлен вызов updateAnimationSync в начале update())

---

### 2026-01-20: Исправления Tiled Map и UI
**Проблема:**
1. В режиме Tiled Map игрок не мог войти в активированный портал (блокировался невидимыми стенами).
2. Сообщение "BAG FULL!" не отображалось при попытке взять 4-й ключ.
**Корневая причина:**
1. `WorldGenerator` создавал воксельные стены под порталом, но не привязывал их к объекту портала.
2. `EffectsManager` неправильно конвертировал координаты мира в координаты экрана (смещение + scale issue).
**Решение:**
1. `WorldGenerator` теперь передает `collisionBodies` в конфигурацию портала. `AbstractPortal` отключает их при активации.
2. `EffectsManager` переведен на использование мировых координат для текста (с компенсацией зума).
**Изменения:**
- `src/game/systems/WorldGenerator.ts`: привязка стен к порталам.
- `src/game/scenes/ui/EffectsManager.ts`: использование world coordinates для FloatingText.
- `src/game/scenes/MainScene.ts`: (косвенные улучшения).

**Коммит:** `fix: Portal collision logic and Inventory UI message`
**Тэг:** `bugfix-portal-collision-and-ui`

---

### 2025-01-18: Удаление отладочного фиолетового прямоугольника
**Проблема:** Фиолетовая плашка (debug background) появлялась в модальных окнах и не исчезала.

**Корневая причина:** `questionBackground` и `feedbackBg` создавались для отладки масштабирования текста, но не удалялись.

**Решение:** Полностью удалено создание debug backgrounds из PortalModal и GameOverModal.

**Изменения:**
- `src/game/ui/PortalModal.ts`: удален questionBackground
- `src/game/ui/GameOverModal.ts`: удален feedbackBg

---

### 2025-01-18: Исправление размера заголовка PORTAL DISCOVERED
**Проблема:** Заголовок был гигантским размером.

**Корневая причина:** Использовался `PORTAL_TITLE_FONT_SIZE * baseFontSize` без ограничений.

**Решение:** Изменено на `unifiedFontSize * FINAL_MULTIPLIER` (как в GameOverModal) с проверкой высоты (максимум 80px).

**Изменения:**
- `src/game/ui/PortalModal.ts`: titleFontSize = unifiedFontSize * FINAL_MULTIPLIER, добавлен height check

---

### 2025-01-18: Исправление пустого модального окна портала
**Проблема:** Модальное окно порта было полностью пустым - никаких текстов и кнопок.

**Корневая причина:** `unifiedFontSize` использовался для создания заголовка (строка 207) ДО того, как был рассчитан (строка 328) - циклическая зависимость.

**Решение:** Перенес блок создания заголовка после расчета `unifiedFontSize`. Изменен порядок операций:
1. Рассчитываются высоты блоков
2. Рассчитываются размеры шрифтов
3. Вычисляется unifiedFontSize
4. Создается заголовок (используя unifiedFontSize)
5. Создаются остальные элементы

**Изменения:**
- `src/game/ui/PortalModal.ts`: изменен порядок создания заголовка

---

### 2025-01-18: Исправление позиции заголовка PORTAL DISCOVERED
**Проблема:** Заголовок "PORTAL DISCOVERED" не отображался.

**Корневая причина:** Заголовок находился СНАРУЖИ модального окна (`modalY - modalHeight/2 - 40`), что могло быть за пределами видимой области.

**Решение:** Изменена реализация на аналог GameOverModal:
- `totalBlocks = 6` (заголовок часть сетки)
- Заголовок = `blockPositions[5]` (самый верхний блок)
- `titleFontSizeRaw` участвует в расчете unifiedFontSize
- Все элементы используют единый размер шрифта

**Изменения:**
- `src/game/ui/PortalModal.ts`: заголовок теперь внутри модального окна, часть сетки блоков

---

### 2025-01-18: Исправление привязки DebugOverlay к левому верхнему краю
**Проблема:** Дебаг окна не всегда привязываются к левому верхнему краю, особенно при переходе на следующий уровень.

**Корневая причина:** Позиция `debugText` рассчитывалась только один раз при создании в `createDebugUI()`. Если зум камеры менялся, позиция не обновлялась.

**Решение:** Добавлено обновление позиции каждый кадр в `updateDebugUI()`:
```typescript
const cam = this.scene.cameras.main;
const pos = this.getZoomCompensatedPosition(20, 20);
this.debugText.setPosition(pos.x, pos.y);
this.debugText.setScale(1 / cam.zoom);
```

**Изменения:**
- `src/game/ui/DebugOverlay.ts`: +8 строк (обновление позиции каждый кадр)

---

### 2026-01-18: Исправление Undefined OracleLabel в CollisionHandler
**Проблема:** Ошибка `Cannot read properties of undefined (reading 'setText')` в `OracleCollisionHandler` при попытке обновить текст `oracleLabel`.
**Корневая причина:** `EntityFactory` создавала `oracleLabel`, но не возвращала его в `MainScene`. В результате `this.oracleLabel` в MainScene оставался `undefined`.
**Решение:** 
1. Обновлен `EntityFactory` для возврата `oracleLabel` в `createAll()`.
2. Обновлен `MainScene` для сохранения `oracleLabel` из результата фабрики.
**Изменения:**
- `src/game/scenes/world/EntityFactory.ts`: добавлено возвращение `oracleLabel`.
- `src/game/scenes/MainScene.ts`: добавлено присвоение `this.oracleLabel`.

### 2026-01-18: Исправление Warning при загрузке Grass Sprite
**Проблема:** Warning `WorldFactory: Grass texture grass_l1 not found, skipping grass sprites`.
**Корневая причина:** Текстура травы не загружалась в `MainScene`.
**Решение:** Добавлена загрузка текстуры `grass_l1` (`Bg.Grass.64x64.png`) в `initializeSystems()` в `MainScene.ts`.
**Изменения:**
- `src/game/scenes/MainScene.ts`: добавлена загрузка `assetLoader.loadImage('grass_l1', 'Bg.Grass.64x64.png')`.

### 2026-01-19: Исправления Tiled Map (Level 2)
**Проблема:** В режиме Tiled Map (Level 2) игрок не мог войти в портал, загружались неправильные ассеты, и была рассинхронизация вопросов.
**Решение:**
1. **Collisions:** Реализована связь воксельных стен с порталами. `AbstractPortal` теперь отключает коллизии стен при активации.
2. **Assets:** Добавлен флаг `useTiledMapTextures`, исправлен выбор анимаций (двери вместо стандартных порталов).
3. **Entities:** Устранено дублирование энтити. `WorldGenerator` теперь только готовит конфиг, а `EntityFactory` создает объекты.
**Изменения:**
- `src/game/entities/portals/AbstractPortal.ts`: управление коллизиями.
- `src/game/scenes/world/EntityFactory.ts`: исправление дублирования.
- `src/config/levelConfigs`: полная поддержка конфигов.

---

**Последнее обновление:** 2026-01-19
**Ветка:** `refactor/supernova`

---

### 2026-01-19: Шаг 6 - QuizHandlers завершен
**Выполнено:**
- Созданы `KeyQuizHandler`, `PortalQuizHandler`, `GlobalQuestionManager`
- Добавлен barrel export для quiz модуля
- Интеграция в MainScene через `initQuizHandlers()`
- Portal и Key handlers заменены на delegates

**Изменения:**
- `src/game/scenes/quiz/`: 3 handler класса + index.ts
- `src/game/scenes/MainScene.ts`: +~60 строк (инициализация + delegates)

---

### 2026-01-19: Шаг 7 - UI Managers завершен + багфиксы
**Выполнено:**
- Созданы `HUDManager`, `CameraManager`, `EffectsManager`
- Добавлен barrel export для ui модуля
- Интеграция в MainScene через `initUIManagers()`
- Все UI методы заменены на delegates

**Изменения:**
- `src/game/scenes/ui/`: 3 manager класса + index.ts
- `src/game/scenes/MainScene.ts`: -485 строк

**Багфиксы после шага 7:**

#### 1. ReferenceError: flashInterval is not defined
**Проблема:** `EffectsManager.ts:246` - `flashInterval.destroy()` ссылался на несуществующую переменную
**Решение:** Изменено на `this.playerFlashGetKeyInterval?.destroy()`

#### 2. Ключи не работают (коллизии не обрабатываются)
**Проблема:** Modal appears 3 times, keys become inactive after wrong answer, inactive from start in incognito mode
**Корневая причина:** `CollisionSystem.onPlayerKeyCollision` callback был undefined при первой коллизии (игрок стоял на ключе при создании сцены)
**Решение:**
- Добавлен флаг `ready = false` в `CollisionSystem`
- Добавлен `process callback` в overlap для ключей - блокирует коллизию пока `ready === false`
- Добавлен метод `setReady()` вызывается после установки всех callbacks
- Изменена логика обработки `processingKeys` - упрощена

**Затронутые файлы:**
- `src/game/systems/CollisionSystem.ts`: +флаг ready, +process callback, +setReady()
- `src/game/scenes/MainScene.ts`: +вызов setReady() после установки callbacks
- `src/game/scenes/collision/ItemCollisionHandler.ts`: -избыточная проверка processingKeys

#### 3. Мигание персонажа не останавливается
**Проблема:** При столкновении с врагом персонаж мигает красным вечно
**Корневая причина:** `flashPlayerLoseKey()` создавал интервал с `loop: true`, но отсутствовала логика остановки
**Решение:** Добавлен счетчик `blinkCount` и лимит `maxBlinks = 10`, при достижении лимита интервал останавливается и восстанавливается исходное состояние

**Затронутые файлы:**
- `src/game/scenes/ui/EffectsManager.ts`: +blinkCount, maxBlinks, cleanup logic

---

### 2026-01-19: Шаг 8 - GameFlowHandlers завершен + багфиксы
**Выполнено:**
- Созданы `EventBusManager`, `GameOverHandler`, `LevelTransitionHandler`
- Добавлен barrel export для gameflow модуля
- Интеграция в MainScene через `initGameFlowHandlers()`
- Все game flow методы заменены на delegates

**Изменения:**
- `src/game/scenes/gameflow/`: 3 handler класса + index.ts
- `src/game/scenes/MainScene.ts`: -350 строк

**Багфиксы шага 8:**

#### 1. Двойное добавление ключей (double key pickup)
**Проблема:** При взятии ключа игрок получал сразу 2 ключа
**Корневая причина:** EventBus - глобальный синглтон. При рестарте сцены создавался новый EventBusManager с новыми подписками, но старые подписки НЕ удалялись. При `KEY_QUIZ_COMPLETED` вызывались оба обработчика → `addKey()` происходил дважды.
**Решение:**
- Добавлен WeakMap для отслеживания EventBusManager instances по сцене
- При создании нового EventBusManager для той же сцены автоматически вызывается `forceCleanup()` для старого
- Добавлен флаг `isSetup` для защиты от дублирования в одном экземпляре

**Затронутые файлы:**
- `src/game/scenes/gameflow/EventBusManager.ts`: +WeakMap tracking, +forceCleanup(), +isSetup guard

#### 2. Ошибка `enablePortalOverlap` в PortalQuizHandler
**Проблема:** `TypeError: Cannot read properties of undefined (reading 'enablePortalOverlap')` при отмене входа в портал
**Корневая причина:** `PortalQuizHandler` зависел от `collisionSystem`, который создавался ПОСЛЕ `initQuizHandlers()`. При создании handler'а `this.collisionSystem` был `undefined`.
**Решение:**
- Убран `collisionSystem` из `PortalQuizDependencies`
- Добавлен `enablePortalOverlap` в `PortalQuizCallbacks`
- MainScene передает callback с проверкой на `undefined`

**Затронутые файлы:**
- `src/game/scenes/quiz/PortalQuizHandler.ts`: -collisionSystem dependency, +enablePortalOverlap callback
- `src/game/scenes/MainScene.ts`: +enablePortalOverlap callback implementation

---
