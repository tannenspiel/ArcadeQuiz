# TEST AUDIT REPORT - ArcadeQuiz

**Дата:** 2026-02-10
**Аудитор:** Claude (Test Auditor)
**Задача:** Проверка тестов на соответствие документированному поведению игры

---

## ИСПОЛНИТЕЛЬНАЯ СВОДКА

| Категория | Статус | Найдено проблем |
|-----------|--------|----------------|
| **КРИТИЧЕСКИЕ БАГИ** | 🔴 RED | 1 |
| **Отсутствующие тесты** | 🟡 YELLOW | 15+ |
| **Неполное покрытие** | 🟡 YELLOW | 8 |
| **НЕСООТВЕТСТВИЕ СПЕЦИФИКАЦИИ** | 🟡 YELLOW | 12+ |

---

## 🔴 КРИТИЧЕСКИЕ БАГИ

### 1. ScoreSystem.ts - Неимпортированный logger

**Файл:** `src/game/systems/ScoreSystem.ts:151`

**Проблема:**
```typescript
public removeScore(amount: number): void {
  this.score = applyPenalty(this.score, -amount);
  logger.log('SCORE', `Score penalty: -${amount} points (new total: ${this.score})`);
  // ^^^^ ReferenceError: logger is not defined
}
```

**Почему тесты это не поймали:**
- Тесты `ScoreSystem.test.ts` никогда не вызывают метод `removeScore()`
- Метод существует в коде, но не протестирован

**Как должно быть:**
```typescript
import { logger } from '../../utils/Logger';
```

---

## 🟡 ОТСУТСТВУЮЩИЕ ТЕСТЫ

### ScoreSystem.test.ts - 7 отсутствующих тестов

Согласно документации (`GameDescription.md`), система очков должна:

| Требование | Статус теста | Ожидаемое поведение |
|------------|--------------|---------------------|
| Монетка уникальная | ❌ НЕ ТЕСТИРОВАНО | +3 очка (REWARD.COIN_UNIQUE) |
| Монетка повторная | ❌ НЕ ТЕСТИРОВАНО | +2 очка (REWARD.COIN_REPEAT) |
| Ключ уникальный | ⚠️ НЕПОЛНО | +5 очков, но тест не проверяет значение |
| Штраф за урон | ❌ НЕ ТЕСТИРОВАНО | -2 очка (PENALTY.ENEMY_DAMAGE_BASE) |
| Штраф за ошибку квиза | ❌ НЕ ТЕСТИРОВАНО | -3 (монетка) / -5 (ключ) |
| Минимум очков = 0 | ❌ НЕ ТЕСТИРОВАНО | Очки не могут быть отрицательными |
| `removeScore()` | ❌ НЕ ТЕСТИРОВАНО | Новый метод v4 |

**Текущий тест:** `scoreSystem.addKeyScore()` проверяет только `expect(score).toBeGreaterThan(0)`

**Проблема:** Если завтра константа изменится с 5 на 1, тест всё равно пройдёт!

**Правильный тест:**
```typescript
it('должен добавлять +5 очков за уникальный ключ', () => {
  scoreSystem.addKeyScore(true);
  expect(scoreSystem.getScore()).toBe(5); // Конкретное значение!
});

it('должен добавлять +3 очка за повторный ключ', () => {
  scoreSystem.addKeyScore(false);
  expect(scoreSystem.getScore()).toBe(3);
});
```

### Player.test.ts - 8+ отсутствующих тестов

Согласно `GameDescription.md` и `GOLDEN_HEARTS_SYSTEM.md`:

| Требование | Статус |
|------------|--------|
| `updateGoldHearts()` | ❌ НЕ ТЕСТИРОВАНО |
| Защита ключами от урона | ❌ НЕ ТЕСТИРОВАНО |
| Монеты НЕ защищают от урона | ❌ НЕ ТЕСТИРОВАНО |
| `onKeyCollected()` → золотые сердечки | ❌ НЕ ТЕСТИРОВАНО |
| Скрытие сердечек в COIN фазе | ❌ НЕ ТЕСТИРОВАНО |
| Показ сердечек в KEY фазе | ❌ НЕ ТЕСТИРОВАНО |

**Критичное упущение:** Тесты проверяют переходы состояний (IDLE → DAMAGED → DEAD), но НЕ проверяют основную игровую механику:

> "Собранные **ключи** создают магический щит. Когда враг прикасается к герою, один ключ тратится... Если ключей нет, герой теряет жизнь."

> "Важное отличие монет: Собранные **монеты** НЕ защищают героя. При столкновении с врагом (если нет ключей) герой теряет И монету, И жизнь."

---

## 🟡 НЕПОЛНОЕ ПОКРЫТИЕ СУЩЕСТВУЮЩИХ ТЕСТОВ

### BubblePositionCalculator.test.ts

**Что тестируется:**
- ✅ Баббл выше спрайта (`result < spriteY`)
- ✅ Разные типы бабблов дают разные позиции
- ✅ Краевые условия (Y=0, отрицательные Y)

**Что НЕ тестируется (из `BUBBLE_SYSTEM.md`):**

| Требование из документации | Статус теста |
|----------------------------|--------------|
| Oracle bubble: 152×56px, рабочая область 134×36px | ❌ НЕ ТЕСТИРОВАНО |
| Portal bubble: 136×48px, рабочая область 118×28px | ❌ НЕ ТЕСТИРОВАНО |
| Формула: bubble Y aligned to grid (BASE_SCALE=4) | ❌ НЕ ТЕСТИРОВАНО |
| Баббл следует за спрайтом при движении | ❌ НЕ ТЕСТИРОВАНО |

### FontSizeCalculator / PixelFontCalculator / TextAnalyzer.test.ts

Эти тесты (Phase 8) охватывают внутреннюю логику функций, но НЕ проверяют соответствие документации:

| Из документации (`FONT_SIZING_SYSTEM.md`) | Статус |
|-------------------------------------------|--------|
| 7 диапазонов aspect ratio (ULTRA_NARROW → MONITOR_LARGE) | ⚠️ Только проверяется, что возвращает число |
| TARGET_PIXEL_FONT_SIZE = 18px | ⚠️ Тест проверяет 18, но не связывает с константой |
| Множители шрифтов: 1.26–1.54 | ❌ НЕ ТЕСТИРОВАНО |

---

## 📊 СТАТИСТИКА ПОКРЫТИЯ VS СООТВЕТСТВИЕ СПЕЦИФИКАЦИИ

| Модуль | Покрытие кода | Соответствие SPEC | Вывод |
|--------|---------------|-------------------|-------|
| `BubblePositionCalculator` | ~100% | ~40% | Покрытие хорошее, но тесты общие |
| `PixelFontCalculator` | ~95% | ~50% | Не привязаны к константам |
| `TextAnalyzer` | ~95% | ~70% | Хорошие краевые условия |
| `FontSizeCalculator` | ~92% | ~60% | Множители не проверяются |
| `ScoreSystem` | <50% | ~20% | 🔴 КРИТИЧНО: новые методы не протестированы |
| `Player` | ~53% | ~30% | 🔴 КРИТИЧНО: игровая механика не протестирована |

---

## 🎯 КЛЮЧЕВЫЕ НАХОДКИ

### 1. Тесты "для покрытия" vs Тесты "для поведения"

**Пример из ScoreSystem.test.ts:**
```typescript
it('должен добавлять очки за ключ', () => {
  scoreSystem.addKeyScore();
  expect(scoreSystem.getScore()).toBeGreaterThan(0); // ❌ Слишком общее!
});
```

**Проблема:** Этот тест пройдёт даже если константа изменится с 5 на 1, или на 100.

**Правильно:**
```typescript
it('должен добавлять +5 очков за уникальный ключ (согласно GameDescription.md)', () => {
  scoreSystem.addKeyScore(true);
  expect(scoreSystem.getScore()).toBe(5);
});
```

### 2. Имплицитная зависимость от констант

Тесты `FontSizeCalculator.test.ts` используют `MAX_OPTIMAL_FONT_SIZE`, но не проверяют:
- Что это за константа
- Какое значение она имеет
- Связь с документацией

### 3. Отсутствие интеграционных сценариев

Из `GameDescription.md`:

> "Фаза 1: Сбор Монет. Чтобы пробудить Оракула, нужно собрать 3 магические монеты."

**НЕТ тестов, которые проверяют:**
- Что после 3 монет Оракул "просыпается"
- Что в COIN фазе появляются только монеты
- Что в KEY фазе появляются только ключи

---

## 🔧 РЕКОМЕНДАЦИИ

### Приоритет 🔴 HIGH (Критично)

1. **Исправить баг в ScoreSystem.ts** - добавить импорт `logger`
2. **Добавить тесты для removeScore()** - проверка защиты от отрицательных очков
3. **Добавить тесты для защиты ключами** - основная игровая механика!

### Приоритет 🟡 MEDIUM (Важно)

4. **Привязать тесты к конкретным значениям констант**
5. **Добавить тесты для updateGoldHearts()**
6. **Протестировать фазы игры (COIN vs KEY)**
7. **Добавить тесты для addCoinScore() с конкретными значениями**

### Приоритет 🟢 LOW (Желательно)

8. **Добавить интеграционные тесты для полного игрового цикла**
9. **Протестировать размеры бабблов (BUBBLE_SIZES константы)**
10. **Добавить тесты для множителей шрифтов (ASPECT_RATIO_RANGES)**

---

## 📋 CHECKLIST ДЛЯ НОВЫХ ТЕСТОВ

### Перед написанием теста спросить себя:

- [ ] Какое поведение из документации я тестирую?
- [ ] Какое конкретное значение должно быть (ссылка на константу)?
- [ ] Что изменится в игре, если этот тест упадёт?
- [ ] Поймаю ли я баг, если константа изменится неправильно?

### Пример хорошего теста:

```typescript
describe('Очки согласно GameDescription.md', () => {
  it('Правильный ответ на ключ (впервые) = +5 очков', () => {
    scoreSystem.addKeyScore(true);
    expect(scoreSystem.getScore()).toBe(REWARD.KEY_UNIQUE); // 5
  });

  it('Штраф за урон (когда нет ключей) = -2 очка', () => {
    scoreSystem.setScore(10);
    scoreSystem.removeScore(PENALTY.ENEMY_DAMAGE_BASE);
    expect(scoreSystem.getScore()).toBe(8); // 10 + (-2) = 8
  });

  it('Очки не могут быть отрицательными (минимум = 0)', () => {
    scoreSystem.setScore(1);
    scoreSystem.removeScore(10); // Штраф больше текущих очков
    expect(scoreSystem.getScore()).toBe(0); // Clamp to 0
  });
});
```

---

## 📝 ЗАКЛЮЧЕНИЕ

**Вердикт:** Тесты обеспечивают хорошее покрытие кода (>90% в game/utils), но **плохое покрытие игровой механики**.

**Главная проблема:** Тесты проверяют "что код работает", но не "что код работает правильно согласно спецификации".

**Риск:**
- 🔴 Высокий: Изменение констант не будет обнаружено
- 🔴 Высокий: Нарушение игровой механики (защита ключами) не будет обнаружено
- 🟡 Средний: Несоответствие UI размерам не будет обнаружено

**Рекомендация:** Пересмотреть все тесты с привязкой к конкретным значениям из документации.

---

**Отчёт создан:** 2026-02-10
**Следующий аудит:** После исправления критических багов
