# План рефакторинга хардкода

**Дата:** 2026-02-01
**Статус:** 📋 PLANNED
**Приоритет:** 🔴 HIGH
**Основанный на:** AUDIT_REPORT.md

---

## ⚠️ Риски и меры безопасности

### 🔴 Критический риск: AnimationSyncManager и ручная синхронизация

**Проблема:** В проекте используется ручная синхронизация кадров через `AnimationSyncManager` и его модули (`KeyAnimationSync`, `CoinAnimationSync`, `PortalAnimationSync`). Эти модули проверяют строковые ключи анимаций.

**Риск:** Если изменить строковые ключи анимаций в одном месте, но забыть обновить в `*AnimationSync.ts` — анимации "застынут".

**Меры безопасности:**
- ⚠️ **Фаза 3 отложена/опциональна** — только если подтверждена безопасная реализация
- Перед рефакторингом проверить ВСЕ файлы `*AnimationSync.ts` на использование строковых ключей
- После изменений — тестировать анимации в игре, не только юнит-тесты
- Рассмотреть вариант: НЕ трогать animation keys, оставить как строки

---

### 🟡 Важный риск: Z-Depth Screen Space vs World Space

**Проблема:** UI элементы используют `setScrollFactor(0)` (Screen Space), а игровые объекты — World Space. Глубина влияет на порядок рендеринга внутри каждой группы.

**Риск:** При "слепом" обновлении `setDepth` можно пропустить динамически создаваемые объекты (частицы, эффекты).

**Меры безопасности:**
- Разделить `DEPTHS` на две группы: `WORLD_*` и `SCREEN_*`
- После обновления проверить визуально все модальные окна, bubbles, эффекты
- Проверить, что UI элементы не перекрываются игроком/врагами

---

### 🟡 Важный риск: Phaser Audio Cache

**Проблема:** Phaser кэширует аудио по ключам. `loadSound(path)` → ключ, `play(key)` → воспроизведение.

**Риск:** Если изменится путь в `AUDIO_PATHS`, но не обновится `loadSound()` — звуки пропадут.

**Меры безопасности:**
- После Фазы 2 проверить консоль на ошибки missing audio
- Убедиться, что SOUND_KEYS не используются напрямую в play()

---

### 🟡 Важный риск: Scope констант GAME_TIMING

**Проблема:** Тайминги спавна и level-specific значения хранятся в `level1.config.json`, `level2.config.json`.

**Риск:** Случайно перекрыть level-specific настройки общими константами, изменив баланс уровней.

**Меры безопасности:**
- `GAME_TIMING` — ТОЛЬКО для глобальных таймингов (knockback, cooldowns, effects)
- Level-specific значения (spawn rates, spawn delays) — ОСТАВИТЬ в JSON конфигах
- Не добавлять в `GAME_TIMING`: `initialSpawnDelay`, `periodicSpawnDelay`, `spawnCounts`

---

### 🟢 Низкий риск: UI тексты и пробелы

**Проблема:** Конкатенация строк в UI. Отсутствие пробела в конце константы → "слипание" текста.

**Меры безопасности:**
- Проверять все UI компоненты визуально после изменений
- Добавить в UI_TEXT комментарии про пробелы в конце строк

---

## Контекст

Аудит выявил множество захардкоженных значений, которые должны быть вынесены в константы для улучшения поддерживаемости, консистентности и конфигурируемости кода.

**Затронутые файлы/модули:**
- `src/constants/gameConstants.ts` (основной файл для новых констант)
- `src/constants/textStyles.ts` (UI цвета и тексты)
- `src/game/ui/*` (все UI компоненты)
- `src/game/systems/AudioManager.ts` (аудио пути)
- `src/game/entities/*` (игровые сущности)

---

## Шаги реализации

### Фаза 1: Z-Depth стандартизация 🔴

**⚠️ Риск:** Screen Space vs World Space конфликты. См. раздел "Риски" выше.

#### Шаг 1.1: Создать константу DEPTHS с разделением World/Screen
**Файл:** `src/constants/gameConstants.ts`

**Задачи:**
- Добавить объект `DEPTHS` с иерархией слоёв
- Явно разделить на WORLD и SCREEN пространства
- Использовать `as const` для типобезопасности

**Структура:**
```typescript
export const DEPTHS = {
  // === WORLD SPACE (setScrollFactor по умолчанию) ===
  WORLD: {
    BACKGROUND: -100,
    TILED_MAP: 0,           // Tiled Map слои

    // Entities (от низшего к высшему)
    ENEMY: 7,
    ORACLE: 8,
    ORACLE_COIN_INDICATOR: 8.1,
    HEART_GLOW: 10,
    HEART_BASE: 11,

    ITEMS: 100,             // Coin, Key, Heart (над Tiled Map)
    PORTAL: 150,
    PLAYER: 200
  },

  // === SCREEN SPACE (setScrollFactor(0)) ===
  SCREEN: {
    // Bubbles (используют setScrollFactor(0))
    QUESTION_BUBBLE: 100,

    // Buttons
    UI_BUTTON_BG: 1002,
    UI_BUTTON_TEXT: 1003,

    // Modals
    MODAL_BG: 2000,
    MODAL_TEXT: 2001,
    MODAL_BUTTON: 2002,
    MODAL_CLOSE: 9999,

    // Overlays
    DEBUG_OVERLAY: 999999,
    SPAWN_MATRIX_GRID: -50
  }
} as const;

// Для обратной совместимости (старый код):
export const DEPTH_WORLD = DEPTHS.WORLD;
export const DEPTH_SCREEN = DEPTHS.SCREEN;
```

#### Шаг 1.2: Обновить UI компоненты для использования DEPTHS

**Файлы для обновления:**

| Файл | Тип | Изменение |
|------|-----|-----------|
| `DebugOverlay.ts` | SCREEN | `999999` → `DEPTHS.SCREEN.DEBUG_OVERLAY` |
| `DebugOverlay.ts` | SCREEN | `-50` → `DEPTHS.SCREEN.SPAWN_MATRIX_GRID` |
| `CoinBubbleQuiz.ts` | SCREEN | `2000` → `DEPTHS.SCREEN.MODAL_BG` |
| `Button.ts` | SCREEN | `1002` → `DEPTHS.SCREEN.UI_BUTTON_BG` |
| `Button.ts` | SCREEN | `1003` → `DEPTHS.SCREEN.UI_BUTTON_TEXT` |
| `QuestionBubble.ts` | SCREEN | `100` → `DEPTHS.SCREEN.QUESTION_BUBBLE` |
| `GameOverModal.ts` | SCREEN | `2000` → `DEPTHS.SCREEN.MODAL_BG` |
| `GameOverModal.ts` | SCREEN | `2001` → `DEPTHS.SCREEN.MODAL_TEXT` |
| `GameOverModal.ts` | SCREEN | `2002` → `DEPTHS.SCREEN.MODAL_BUTTON` |
| `PortalModal.ts` | SCREEN | `9999` → `DEPTHS.SCREEN.MODAL_CLOSE` |
| `KeyQuestionModal.ts` | SCREEN | `2000` → `DEPTHS.SCREEN.MODAL_BG` |
| `KeyQuestionModal.ts` | SCREEN | `2001` → `DEPTHS.SCREEN.MODAL_TEXT` |
| `KeyQuestionModal.ts` | SCREEN | `2002` → `DEPTHS.SCREEN.MODAL_BUTTON` |

**World Space объекты:**

| Файл | Изменение |
|------|-----------|
| `Coin.ts` | `setDepth(100)` → `setDepth(DEPTHS.WORLD.ITEMS)` |
| `Key.ts` | `setDepth(100)` → `setDepth(DEPTHS.WORLD.ITEMS)` |
| `Heart.ts` | `setDepth(100)` → `setDepth(DEPTHS.WORLD.ITEMS)` |

**⚠️ Проверка после Фазы 1:**
- [ ] Запустить игру, проверить все модальные окна
- [ ] Проверить, что UI не перекрывается игроком/врагами
- [ ] Проверить, что Coin/Key/Heart визуально над Tiled Map

#### Шаг 1.2: Обновить UI компоненты для использования DEPTHS

**Файлы для обновления:**
1. `src/game/ui/DebugOverlay.ts`
   - `setDepth(999999)` → `setDepth(DEPTHS.DEBUG_OVERLAY)`
   - `setDepth(-50)` → `setDepth(DEPTHS.SPAWN_MATRIX_GRID)`

2. `src/game/ui/CoinBubbleQuiz.ts`
   - `setDepth(2000)` → `setDepth(DEPTHS.MODAL_BG)`

3. `src/game/ui/Button.ts`
   - `setDepth(1002)` → `setDepth(DEPTHS.UI_BUTTON_BG)`
   - `setDepth(1003)` → `setDepth(DEPTHS.UI_BUTTON_TEXT)`

4. `src/game/ui/QuestionBubble.ts`
   - `setDepth(100)` → `setDepth(DEPTHS.QUESTION_BUBBLE)`

5. `src/game/ui/GameOverModal.ts`
   - Все `setDepth(2000)` → `setDepth(DEPTHS.MODAL_BG)`
   - Все `setDepth(2001)` → `setDepth(DEPTHS.MODAL_TEXT)`
   - Все `setDepth(2002)` → `setDepth(DEPTHS.MODAL_BUTTON)`

6. `src/game/ui/PortalModal.ts`
   - `setDepth(9999)` → `setDepth(DEPTHS.UI_MAX)`

7. `src/game/ui/KeyQuestionModal.ts`
   - Все `setDepth(2000)` → `setDepth(DEPTHS.MODAL_BG)`
   - Все `setDepth(2001)` → `setDepth(DEPTHS.MODAL_TEXT)`
   - Все `setDepth(2002)` → `setDepth(DEPTHS.MODAL_BUTTON)`

8. `src/game/entities/items/Coin.ts`
   - `setDepth(100)` → `setDepth(DEPTHS.ITEMS)`

9. `src/game/entities/items/Key.ts`
   - `setDepth(100)` → `setDepth(DEPTHS.ITEMS)`

10. `src/game/entities/items/Heart.ts`
    - `setDepth(100)` → `setDepth(DEPTHS.ITEMS)`

---

### Фаза 2: Audio paths централизация 🔴

**⚠️ Риск:** Phaser Audio Cache — звуки могут пропасть. См. раздел "Риски" выше.

#### Шаг 2.1: Создать константу AUDIO_PATHS
**Файл:** `src/constants/gameConstants.ts`

**Задачи:**
- Создать константу с путями к аудио файлам
- Сохранить соответствие с текущими `SOUND_KEYS`

**Структура:**
```typescript
export const AUDIO_PATHS = {
  MUSIC: {
    BASE: 'mp3/Music.Base.mp3',
    WIN: 'mp3/Music.Win.mp3',
    GAME_OVER: 'mp3/Music.GameOver.mp3'
  },
  SFX: {
    BUTTONS: {
      CLICK: 'mp3/SFX_Btn.CLICK.mp3',
      QUESTION_CLOSE: 'mp3/SFX_Btn.Question.CLOSE.mp3'
    },
    CHARACTER: {
      DAMAGE: 'mp3/SFX_Character.Damage.mp3',
      PICKUP_KEY: 'mp3/SFX_Character.PickUp.Key.mp3',
      PICKUP_COIN: 'mp3/SFX_Character.PickUp.Coin.mp3',
      COIN_SUCCESS: 'mp3/SFX_Character.Coin.Success.mp3',
      DAMAGE_COIN: 'mp3/SFX_Character.Damage.Coin.mp3',
      LOSE_KEY: 'mp3/SFX_Character.LoseKey.mp3',
      LOSE_LIFE: 'mp3/SFX_Character.LoseLife.mp3'
    },
    GAME: {
      WRONG_ANSWER: 'mp3/SFX_Game.WrongAnswer.mp3',
      CORRECT_ANSWER: 'mp3/SFX_Game.CorrectAnswer.mp3',
      LEVEL_COMPLETE: 'mp3/SFX_Game.LevelComplete.mp3',
      ALL_KEYS_COLLECTED: 'mp3/SFX_Game.AllKeysCollected.mp3',
      PORTAL_OPEN: 'mp3/SFX_Game.PortalOpen.mp3'
    }
  }
} as const;
```

#### Шаг 2.2: Обновить AudioManager
**Файл:** `src/game/systems/AudioManager.ts`

**Задачи:**
- Заменить все hardcoded audio paths на `AUDIO_PATHS`
- Пример: `'mp3/Music.Base.mp3'` → `AUDIO_PATHS.MUSIC.BASE`
- **КРИТИЧНО:** Убедиться, что `loadSound()` использует правильные ключи

**⚠️ Проверка после Фазы 2:**
- [ ] Запустить игру
- [ ] Проверить консоль браузера на ошибки (F12 → Console)
- [ ] Искомые ошибки: `"No audio found in cache for key: ..."`
- [ ] Протестировать все звуки: клики, damage, pickup, portal
- [ ] Убедиться, что музыка играет на фоне

---

### Фаза 3: Animation keys стандартизация ❌ ИСКЛЮЧЕНА

**Статус:** ❌ **EXCLUDED** — Анализ выполнен 2026-02-08, фаза исключена из плана

**Анализ выполнен:**
- Проанализированы `AnimationSyncManager`, `KeyAnimationSync`, `CoinAnimationSync`, `PortalAnimationSync`
- Проанализированы `Coin.ts`, `Key.ts`, `spritesheetConfigs.ts`

**Вывод:**
> **Строковые ключи анимаций — это правильный подход в Phaser 3.**
> Animation keys по документации Phaser — это **строки**, которые определяются в spritesheet configs и играются через `sprite.play('key_name')`.
>
> Синхронизаторы НЕ проверяют имя анимации — они работают напрямую с `currentAnim.frames` и вызывают `setFrame(frameIndex)`.
>
> Замена `'coin_idle'` на `ANIMATION_KEYS.ITEMS.COIN_IDLE` не даст никакого преимущества — это "оправданный хардкод" по дизайну Phaser.

**Решение:**
- ❌ Фаза исключена из плана
- ✅ Animation keys остаются как строковые литералы (корректный подход для Phaser 3)
- ✅ Проблема отсутствует

**См. детали анализа в HISTORY.md (2026-02-08)**

3. Создать список затронутых файлов

#### Шаг 3.1: (Опционально) Создать константы ANIMATION_KEYS
**Только если Шаг 3.0 показал, что безопасно**

```typescript
// ВНИМАНИЕ: Только если ВСЕ затронутые файлы обновлены!
export const ANIMATION_KEYS = {
  PLAYER: {
    DOWN: 'boy_down',
    UP: 'boy_up',
    LEFT: 'boy_left',
    RIGHT: 'boy_right',
    IDLE: 'boy_idle'
  },
  ENEMIES: {
    BEAST: 'beast',
    DRAGON: 'dragon',
    FLAM: 'flam'
  },
  ITEMS: {
    COIN_IDLE: 'coin_idle',
    KEY_IDLE: 'key_idle'
  },
  EFFECTS: {
    DEATH: 'enemy_death'
  },
  DIRECTIONS: ['down', 'up', 'left', 'right'] as const
} as const;
```

#### Шаг 3.2: (Опционально) Обновить использование animation keys

**Файлы для обновления (ТОЛЬКО после анализа):**
1. `src/game/scenes/animation/*AnimationSync.ts` — **КРИТИЧНО!**
2. `src/game/systems/SpriteAnimationHandler.ts`
3. `src/game/entities/items/Coin.ts`
4. `src/game/entities/items/Key.ts`
5. `src/game/entities/enemies/AbstractEnemy.ts`

**⚠️ Проверка после Фазы 3 (если выполнялась):**
- [ ] Запустить игру
- [ ] Проверить анимации игрока (movement)
- [ ] Проверить анимации врагов (movement)
- [ ] Проверить анимации Coin (idle animation)
- [ ] Проверить анимации Key (idle animation)
- [ ] Проверить анимации Portal (state changes)
- [ ] Проверить анимацию death (enemy_death)
- [ ] Если ЛЮБАЯ анимация застыла — откатить изменения

---

### Фаза 4: Game mechanics constants 🟡

**⚠️ Риск:** Scope констант — не перекрыть level-specific настройки.

**Важно:**
- `GAME_TIMING` — ТОЛЬКО для **глобальных** таймингов (knockback, cooldowns, effects)
- **НЕ** добавлять level-specific значения:
  - ❌ `initialSpawnDelay`, `periodicSpawnDelay`, `spawnCounts` — остаются в `level*.config.json`
  - ❌ Enemy spawn rates — остаются в level configs

#### Шаг 4.1: Создать константу GAME_TIMING (только глобальные значения)
**Файл:** `src/constants/gameConstants.ts`

**Структура:**
```typescript
export const GAME_TIMING = {
  // === Player mechanics (глобальные) ===
  KNOCKBACK_FORCE: 200,
  KNOCKBACK_DURATION: 300,
  DAMAGE_ANIMATION_DURATION: 100,

  // === Enemy mechanics (глобальные, не level-specific!) ===
  ENEMY_COLLISION_COOLDOWN: 500,
  ENEMY_DETECTION_COOLDOWN: 2000,
  DAMAGE_BLINK_DURATION: 1000,
  DAMAGE_BLINK_INTERVAL: 50,
  DEATH_ANIMATION_FALLBACK_DURATION: 100,

  // === Oracle (глобальные константы) ===
  ORACLE_MAX_KEYS: 3,
  ORACLE_MAX_COINS: 3,

  // === Effects (глобальные) ===
  SHAKE_DURATION: 1000,
  FLASH_DURATION: 100,
  EFFECTS_START_DELAY: 200,
  FADE_DURATION: 500,

  // === UI (глобальные) ===
  BUTTON_ANIMATION_DURATION: 600,
  PORTAL_INTERACTION_DURATION: 100,
  QUESTION_BUBBLE_ANIMATION_DURATION: 300,

  // === Technical (глобальные) ===
  FPS_60_DELAY: 16,

  // === ВАЖНО: НЕ добавлять в GAME_TIMING ===
  // ❌ initialSpawnDelay — в level config
  // ❌ periodicSpawnDelay — в level config
  // ❌ spawnCounts (enemies, items) — в level config
  // ❌ Enemy speeds — в level config
} as const;
```

#### Шаг 4.2: Обновить использование timing констант

**Файлы для обновления:**

| Файл | Изменения |
|------|-----------|
| `Player.ts` | `200` → `GAME_TIMING.KNOCKBACK_FORCE`, `300` → `GAME_TIMING.KNOCKBACK_DURATION`, `100` → `GAME_TIMING.DAMAGE_ANIMATION_DURATION` |
| `AbstractEnemy.ts` | `500` → `GAME_TIMING.ENEMY_COLLISION_COOLDOWN`, `2000` → `GAME_TIMING.ENEMY_DETECTION_COOLDOWN`, blink values |
| `Oracle.ts` | `3` → `GAME_TIMING.ORACLE_MAX_KEYS`, `3` → `GAME_TIMING.ORACLE_MAX_COINS` |
| `EffectsManager.ts` | All timing values → `GAME_TIMING.*` |
| `Button.ts` | `600` → `GAME_TIMING.BUTTON_ANIMATION_DURATION` |
| `PortalCollisionHandler.ts` | `100` → `GAME_TIMING.PORTAL_INTERACTION_DURATION` |
| `QuestionBubble.ts` | `300` → `GAME_TIMING.QUESTION_BUBBLE_ANIMATION_DURATION` |

**⚠️ Проверка после Фазы 4:**
- [ ] Запустить Level 1 и Level 2
- [ ] Проверить, что спавн работает как раньше (баланс не изменился)
- [ ] Проверить knockback при столкновении с врагом
- [ ] Проверить все визуальные эффекты (shake, flash, fade)
- [ ] Проверить Oracle (3 keys, 3 coins)

---

### Фаза 5: UI colors и тексты 🟢

**⚠️ Риск:** Пробелы в конце строк — "слипание" текста.

---

### ⚠️ ⚠️ ⚠️ КРИТИЧЕСКОЕ ПРЕДУПРЕЖДЕНИЕ: Пробелы в конце строк-префиксов

#### Проблема

При конкатенации строковых префиксов с числами отсутствие пробела в конце приводит к "слипанию":

```typescript
// ❌ ОШИБКА — нет пробела:
const prefix = 'Score:';
const result = `${prefix}${123}`;  // "Score:123" ❌❌❌

// ✅ ПРАВИЛЬНО — есть пробел:
const prefix = 'Score: ';
const result = `${prefix}${123}`;  // "Score: 123" ✅
```

#### Текущий код (уже правильный)

В `HUDManager.ts` пробелы уже есть правильно:

```typescript
// ✅ ПРАВИЛЬНО — пробел уже есть:
this.scoreHUDText.setText(`Счёт: ${this.deps.scoreSystem.getScore()}`);
// Результат: "Счёт: 1500" ✅

this.hintText.setText(`Ключей: ${currentKeys}/3`);
// Результат: "Ключей: 2/3" ✅
```

#### Как реализовать в UI_TEXT (правильный способ)

```typescript
// ✅ ВАРИАНТ 1: Пробел в конце константы (РЕКОМЕНДУЕТСЯ)
export const UI_TEXT = {
  // ============================================================
  // ⚠️ ВНИМАНИЕ: Пробелы в конце строк ОБЯЗАТЕЛЬНЫ! ⚠️
  // ============================================================
  // Причина: конкатенация с числами → "Score: 123", не "Score:123"
  //
  // Если забыт пробел → текст "слипается":
  //   'Score:' + 123 = "Score:123" ❌
  //   'Score: ' + 123 = "Score: 123" ✅
  // ============================================================

  // English
  SCORE_PREFIX: 'Score: ',      // ← Пробел в конце!
  CLOSE_BUTTON: 'Close',         // ← Без пробела (это не префикс)

  // Русские тексты
  SCORE_RU: 'Счёт: ',           // ← Пробел в конце!
  KEYS_RU: 'Ключей: ',          // ← Пробел в конце!
  COINS_RU: 'Монеток: '         // ← Пробел в конце! (для будущего)
} as const;
```

#### Использование в коде

```typescript
// Было (template literal):
this.scoreHUDText.setText(`Счёт: ${score}`);

// Станет (с константой):
import { UI_TEXT } from '../../constants/textStyles';

this.scoreHUDText.setText(`${UI_TEXT.SCORE_RU}${score}`);
// Результат: "Счёт: 1500" ✅
```

---

#### ❌ Чего НЕ делать (ошибочные варианты)

```typescript
// ❌ ОШИБКА 1: Без пробела в конце
export const UI_TEXT = {
  SCORE_PREFIX: 'Score:',       // ❌ Нет пробела!
  SCORE_RU: 'Счёт:',           // ❌ Нет пробела!
  KEYS_RU: 'Ключей:'           // ❌ Нет пробела!
} as const;

// Результат: "Score:123", "Счёт:1500" ❌❌❌


// ❌ ОШИБКА 2: Добавление пробела при использовании (путаница)
export const UI_TEXT = {
  SCORE_PREFIX: 'Score:',       // Без пробела
  // ...
} as const;

// Потом:
this.scoreHUDText.setText(`${UI_TEXT.SCORE_PREFIX} ${score}`);
//                                       ↑ добавили пробел здесь
// Проблема: неочевидно, легко забыть, дублирование логики


// ❌ ОШИБКА 3: Использование padEnd (избыточно)
export const UI_TEXT = {
  SCORE_PREFIX: 'Score'.padEnd(6, ' '),  // 'Score '
  // Проблема: неочевидно, что значит 6, легко сломать
} as const;
```

---

#### 🔒 Альтернативный подход (опционально, если хотим защиты)

Если хотим дополнительной защиты от человеческих ошибок:

```typescript
// Базовые строки (БЕЗ пробелов)
export const UI_TEXT_BASE = {
  SCORE: 'Score',
  CLOSE_BUTTON: 'Close',
  SCORE_RU: 'Счёт',
  KEYS_RU: 'Ключей',
  COINS_RU: 'Монеток'
} as const;

// Helper функция для безопасного форматирования:
export const formatPrefix = (prefix: string, value: string | number): string => {
  return `${prefix}: ${value}`;  // Пробел добавляется автоматически!
};

// Использование:
import { UI_TEXT_BASE, formatPrefix } from '../../constants/textStyles';

this.scoreHUDText.setText(formatPrefix(UI_TEXT_BASE.SCORE_RU, score));
// Результат: "Счёт: 1500" ✅
```

**Но рекомендуется Вариант 1** — проще и понятнее.

---

#### Чек-лист для Шага 5.2

После создания `UI_TEXT`:

- [ ] Все префиксы заканчиваются на пробел (`: `)
- [ ] Только префиксы (non-prefix строки без пробела)
- [ ] Добавлен явный комментарий про пробелы
- [ ] Проверено визуально в игре после рефакторинга

---

#### Шаг 5.1: Добавить недостающие цвета в textStyles.ts

**Файл:** `src/constants/textStyles.ts`

```typescript
export const COLORS = {
  // ... существующие ...

  // === Button hover/pressed states ===
  // ВНИМАНИЕ: CoinBubbleQuiz использует эти tints
  BUTTON_HOVER_GOLD: '#FFEC8B',    // Lighter gold on hover
  BUTTON_PRESSED_GOLD: '#D4A017'   // Darker gold (pressed)
} as const;
```

#### Шаг 5.2: Создать константу UI_TEXT
**Файл:** `src/constants/textStyles.ts`

```typescript
export const UI_TEXT = {
  // === ВНИМАНИЕ: Пробелы в конце строк! ===
  // Подробности см. в документации выше в этой фазе
  SCORE_PREFIX: 'Score: ',      // Обратить внимание на пробел в конце!
  CLOSE_BUTTON: 'Close',

  // === Русские тексты ===
  SCORE_RU: 'Счёт: ',           // Обратить внимание на пробел в конце!
  KEYS_RU: 'Ключей: '           // Обратить внимание на пробел в конце!
} as const;
```

#### Шаг 5.3: Обновить UI компоненты

**Файлы для обновления:**

| Файл | Изменения |
|------|-----------|
| `Button.ts` | `0xffffff` → `COLORS.WHITE`, добавить hover/pressed tint из `COLORS` |
| `CoinBubbleQuiz.ts` | `0xFFEC8B` → `COLORS.BUTTON_HOVER_GOLD`, `0xD4A017` → `COLORS.BUTTON_PRESSED_GOLD`, `0x000000` → `COLORS.BLACK` |
| `GameOverModal.ts` | `"Score: "` → `UI_TEXT.SCORE_PREFIX` |
| `KeyQuestionModal.ts` | `"Close"` → `UI_TEXT.CLOSE_BUTTON` |
| `HUDManager.ts` | `"Счёт: "` → `UI_TEXT.SCORE_RU`, `"Ключей: "` → `UI_TEXT.KEYS_RU` |

**⚠️ Проверка после Фазы 5:**
- [ ] Проверить все модальные окна визуально
- [ ] Проверить форматирование текста (нет "слипания")
- [ ] Проверить hover/pressed состояния кнопок
- [ ] Проверить HUD на русском языке
- [ ] Проверить цвета кнопок в CoinBubbleQuiz

---

## Тестирование

### После каждой фазы:

1. **Юнит-тесты:**
   ```bash
   npm test
   ```
   Ожидание: Все 1207+ тестов проходят

2. **Проверка в браузере:**
   - Открыть http://localhost:3000
   - Открыть DevTools (F12 → Console)
   - Проверить отсутствие ошибок

3. **Функциональное тестирование:**

| Фаза | Что проверить |
|------|---------------|
| Фаза 1 | Z-Depth: UI над миром, модальные окна видны, Coin/Key/Heart над Tiled Map |
| Фаза 2 | Audio: все звуки играют, нет ошибок "No audio found in cache" |
| Фаза 3 | Animation: все анимации работают, ничего не застыло |
| Фаза 4 | Mechanics: knockback, cooldowns, effects работают, баланс уровней не изменился |
| Фаза 5 | UI: текст форматирован правильно, нет "слипания", цвета корректны |

### Финальная проверка (после всех фаз):

- [ ] Level 1 проходит полностью
- [ ] Level 2 проходит полностью
- [ ] Все модальные окна открываются/закрываются
- [ ] Coin/Key/Heart собираются корректно
- [ ] Порталы работают
- [ ] Враги behave как раньше
- [ ] HUD отображается корректно
- [ ] Debug overlay показывает корректные данные

---

## Зависимости

**Независимые фазы:**
- ✅ Фаза 1 (Z-Depth) — независимая
- ✅ Фаза 2 (Audio) — независимая
- ✅ Фаза 4 (Game mechanics) — независимая
- ✅ Фаза 5 (UI) — независимая

**Зависимая фаза:**
- ⚠️ **Фаза 3 (Animation Keys)** — требует предварительного анализа, может быть пропущена

**Рекомендация:** Выполнять фазы по порядку, но Фазу 3 можно пропустить.

---

## Примечания

### Оставить как есть (оправданный хардкод):

| Категория | Примеры | Причина |
|-----------|---------|---------|
| Frame dimensions | `frameWidth: 16, frameHeight: 16` | Получены из actual assets |
| FPS values | `frameRate: 8, 12, 24` | Artistic choice |
| Простые значения | `0, 1, -1, true, false` | Универсальные константы |
| Технические | `16` (60 FPS delay) | Техническая реализация |
| **Animation Keys** | `'coin_idle'`, `'boy_down'` | ⚠️ **AnimationSyncManager зависимость** |
| Level config values | `spawnDelay`, `spawnCounts` | Level-specific баланс |

### Scope констант:

```typescript
// ✅ В gameConstants.ts (глобальное):
GAME_TIMING        // Global mechanics (knockback, cooldowns)
DEPTHS             // Z-depth иерархия
AUDIO_PATHS        // Audio file paths

// ✅ В textStyles.ts (UI):
COLORS             // UI colors
UI_TEXT            // UI текстовые строки

// ❌ НЕ трогать:
level*.config.json // Level-specific баланс
spritesheetConfigs // Asset-derived значения
```

---

## Критерии завершения

План считается выполненным, когда:

1. ✅ Фазы 1, 2, 4, 5 завершены с полными проверками
2. ✅ Фаза 3 либо выполнена (после анализа), либо пропущена с обоснованием
3. ✅ Все 1207+ тестов проходят
4. ✅ Игра работает без визуальных/функциональных регрессий
5. ✅ Консоль браузера чистая (нет ошибок)
6. ✅ PROGRESS.md обновлён со статусом "DONE"
