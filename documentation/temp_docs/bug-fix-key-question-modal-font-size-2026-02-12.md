# Bug Report: Huge Font Size in Key Question Modal Buttons

**Дата:** 2026-02-12
**Файл:** `src/game/ui/KeyQuestionModal.ts`
**Версия:** после исправления

---

## 📋 Описание проблемы

**Симптомы:**
- Текст в кнопках и фидбэках модального окна вопросов имеет **огромный размер**
- Размер шрифта визуально значительно больше, чем в других модальных окнах
- Текст в кнопках выглядит сжатым и растянутым по ширине

**Влияние на UX:**
- Плохая читаемость текста кнопок
- Несоответствие размеру с другими UI элементами
- Менее профессиональный вид интерфейса

---

## 🔍 Анализ корневой причины

### 1. Неправильный расчёт доступной ширины для кнопок

**Исходный код (строки 427-429):**
```typescript
const buttonCount = this.parsedQuestion.allAnswers.length;
const buttonWidth = contentAreaWidth;              // ❌ Вся ширина модального окна!
const buttonHeight = blockHeight;
```

**Проблема:**
- `buttonWidth` использует всю ширину модального окна (`contentAreaWidth`)
- Кнопки должны использовать только часть ширины (своя ширина / количество кнопок)
- При текущем подходе доступная ширина для текста слишком мала → размер шрифта получается слишком большим

**Правильный код:**
```typescript
const buttonCount = this.parsedQuestion.allAnswers.length;
const totalButtonSpacing = (buttonCount - 1) * buttonSpacing;
const totalBlocks = 5;
const totalSpacings = totalBlocks - 1;

// ✅ ИСПРАВЛЕНИЕ: Ширина для одной кнопки = contentAreaWidth минус padding×2, делённая на количество кнопок
const singleButtonWidth = (contentAreaWidth - (internalPadding * 2)) / buttonCount;

// Доступная ширина для текста кнопки = ширина кнопки минус её padding
const buttonAvailableWidth = singleButtonWidth - (internalPadding * 2);
const buttonAvailableHeight = blockHeight;
```

---

### 2. Сравнение с другими модальными окнами

**PortalModal.ts — использует тот же подход:**
```typescript
// ✅ Используем baseFontSize напрямую для кнопок, без отдельного расчёта
const buttonWidth = contentAreaWidth;
const buttonHeight = blockHeight;
```

**GameOverModal.ts — также использует упрощённый подход:**
```typescript
// ✅ КНОПКИ: используем тот же размер, что и для текстовых элементов
const buttonWidth = contentAreaWidth;
const buttonHeight = blockHeight; // или любая фиксированная высота блока
```

**Почему в KeyQuestionModal используется более сложный расчёт?**
- Возможно для совместимости с `calculateButtonFontSize()` который требует `buttonWidth` и `buttonHeight`
- Но это приводит к неправильному результату

---

## 🛠️ Объяснение различий в подходах

### Подход KeyQuestionModal (сложный, с багом):
1. Рассчитывает `singleButtonWidth` — ширину одной кнопки с учётом spacing
2. Передаёт `buttonAvailableWidth = singleButtonWidth - padding * 2`
3. Рассчитывает ширину как `(вся modalWidth - отступы * 2) / количество кнопок`

### Подход PortalModal/GameOverModal (простой, без бага):
1. Использует `baseFontSize` напрямую для кнопок
2. Передаёт полную `contentAreaWidth` и `blockHeight` кнопкам
3. Не использует `calculateButtonFontSize()` или `getButtonPadding()`

**Почему разница?**
- KeyQuestionModal наследует более старый код для совместимости с `calculateButtonFontSize()`
- Но при этом должен был исправлен базовый расчёт `buttonWidth`

---

## ✅ Исправление

**Изменённый код:**
```typescript
const buttonCount = this.parsedQuestion.allAnswers.length;
const totalButtonSpacing = (buttonCount - 1) * buttonSpacing;
const totalBlocks = 5;
const totalSpacings = totalBlocks - 1;
const singleButtonWidth = (contentAreaWidth - (internalPadding * 2)) / buttonCount;

// ✅ ИСПРАВЛЕНИЕ: Ширина для одной кнопки = contentAreaWidth минус padding×2, делённая на количество кнопок
const buttonAvailableWidth = singleButtonWidth - (internalPadding * 2);
const buttonAvailableHeight = blockHeight;
```

**Результат:**
- Теперь каждая кнопка использует только свою часть ширины
- Доступная ширина для текста рассчитывается корректно
- Размер шрифта больше не будет раздутым

---

## 📝 Ожидаемое поведение

После исправления размер шрифта в кнопках должен стать:
- ✓ Соизмеримым с размером вопроса/фидбэка
- ✓ Пропорциональным другим элементам UI
- ✓ Удобочитаемым и профессиональным

---

**Для проверки:**
1. Откройте модальное окно вопроса на уровне 2
2. Посмотрите на размер текста в кнопках
3. Сравните с размером текста в других модальных окнах

---

**Коммит:** TODO (после проверки)
