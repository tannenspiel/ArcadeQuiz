# Карта проекта ArcadeQuiz

**Версия:** 5.1
**Дата обновления:** 2026-02-10

**Изменения в v5.1:**
- ✅ **DOCUMENTATION AUDIT:** Обновление ProjectMap.md с новыми директориями сцены.
  - Добавлены секции `enemy/`, `gameflow/`, `ui/`, `world/` в "Быстрый поиск"
  - enemy/ — EnemyManager, EnemySpawner, EnemyCloneFactory
  - gameflow/ — EventBusManager, GameOverHandler, LevelTransitionHandler
  - ui/ — CameraManager, EffectsManager, HUDManager
  - world/ — WorldFactory, EntityFactory, CollisionObjectFactory
  - Добавлена секция "Тестовая инфраструктура" в "Быстрый поиск"

**Изменения в v5.0:**
- ✅ **PERFORMANCE OPTIMIZATION:** Комплексная оптимизация загрузки.
  - **FCP (First Contentful Paint):** 1.4s (показатель улучшен с 3.4s).
  - **Hybrid Loading Screen:** Мгновенный DOM-оверлей (React/CSS) + Phaser-загрузка на фоне.
  - **Deferred Execution:** Запуск тяжелых скриптов Phaser отложен через `setTimeout` после рендера UI.
  - **Vite Config:** `modulePreload: false` для ускорения начальной отрисовки, `gzip` компрессия ассетов.
  - **Production Cleanup:** Все консольные логи (`console.log`, `debug`) вырезаются при сборке через `esbuild`.

**Изменения в v4.9:**
- ✅ **DYNAMIC LEVEL SYSTEM:** Рефакторинг системы уровней для поддержки N уровней.
  - LoadingScene использует `for (1..MAX_LEVELS)` для динамической загрузки ассетов
  - LevelManager загружает конфиги из Phaser.Cache вместо статических импортов
  - Dynamic Asset Keys через `getLevelAssetKey()` и `LevelAssetKeys`
  - MAX_LEVELS централизован в `gameConstants.ts` (текущее значение: 3)
  - WorldFactory использует динамические ключи для фонов
  - LevelTransitionHandler импортирует MAX_LEVELS из gameConstants
  - **N-Level Support VERIFIED** - Level 3 создан, 1206 тестов проходят!
  - Подробности: `documentation/Plans/refactoring-levels/`

**Изменения в v4.8:**
- ✅ **HARDCODE AUDIT (PHASE 1 & 2):** Централизация констант.
  - Добавлены `DEPTHS` (Z-index management) и `AUDIO_PATHS` в `gameConstants.ts`.
  - Устранены магические числа в UI и звуковой системе.
- ✅ **BUGFIX: GRASS GENERATION:** Исправлена логика генерации травы.
  - Корректная загрузка спрайтшита `BG_GRASS_SHEET` (16x16).
  - Использование `GrassBackgroundSprite` вместо хардкода.
  - Исправлена плотность генерации (0 спрайтов -> корректное заполнение).
- ✅ **ASSETS:** Уточнены пути к вопросам: `src/assets/Game_01/questions/`.

**Изменения в v4.7:**
- ✅ **ДИНАМИЧЕСКИЙ HUD:** Приветственные сообщения ("Собери монетки...", "Открой ключами...") до первого сбора.
- ✅ **ИНДИКАТОРЫ ОРАКУЛА:** 3 визуальных индикатора монет (горизонтальный ряд, Z: 8.1, оригинальный масштаб).
- ✅ **АВТОМАТИЗАЦИЯ UI:** Индикаторы скрываются при активации Оракула.

**Изменения в v4.6:**
- ✅ **DISTANCE-BASED INTERACTIONS:** Переход на проверку расстояния для айтемов (50px радиус).
- ✅ **COIN MECHANIC:** Полная реализация (звуки, квизы в бабблах, логика потери).
- ✅ **CLEANUP:** Удалены круговые хитбоксы айтемов и устаревшие анимации смерти монет.
- ✅ **DASHBOARD:** Статистика тестов обновлена до 1207 passing.

**Изменения в v4.5:**
**Проект:** ArcadeQuiz - Игра-викторина на Phaser 3 + React + TypeScript

**Изменения в v4.4:**
- ✅ **РЕОРГАНИЗАЦИЯ ДОКУМЕНТАЦИИ:** Структурирование всей документации в 6 категорий
  - `game-systems/` (4 файла) - игровые системы
  - `ui/` (6 файлов) - UI документация
  - `development/` (8 файлов) - для разработчиков
  - `project/` (4 файла) - о проекте
  - `planning/` (5 файлов) - планы и прогресс
  - `reference/` (1 файл) - справочная информация
  - Добавлены новые гайды: UI_GUIDE.md, MODAL_GUIDE.md, DEBUGGING_GUIDE.md
  - Добавлена документация GOLDEN_HEARTS_SYSTEM.md для новых золотых сердечек
  - Обновлен export_llm/ PROJECT_MAP.md (v2.1) с модульной структурой scenes/

**Изменения в v4.3:**
- ✅ **РЕФАКТОРИНГ СИСТЕМЫ ЛОГИРОВАНИЯ ЗАВЕРШЕН:** 345+ console.log → logger.log()
  - 39+ файлов рефакторено с категоризацией логов
  - 50+ категорий для тонкой настройки
  - Добавлен `LOGGING_SYSTEM.md` с полным описанием системы
- ✅ Добавлены категории: QUIZ_GLOBAL, QUIZ_KEY, QUIZ_PORTAL, GAME_OVER, LEVEL_TRANSITION, COLLISION_*
- ✅ Все информационные логи управляются через `.env` флаги

**Изменения в v4.2:**
- ✅ **РЕФАКТОРИНГ Debаг-системы:** Разделены флаги управления отладочным UI
  - `DEBUG_OVERLAY_ENABLED` — текстовый оверлей (FPS, позиция, счёт, враги)
  - `DEBUG_VISUAL_GRID_ENABLED` — визуальная сетка на карте
  - `DEBUG_SPAWN_GRID_ENABLED` — детальные логи в консоли
- ✅ Добавлен FPS в текстовый дебаг-оверлей
- ✅ Обновлён `DEBUG_OVERLAY.md` с новой структурой флагов

**Изменения в v4.1:**
- ✅ Добавлена секция "Структура MainScene.ts" с полной навигацией по файлу (~4599 строк)
- ✅ Добавлена таблица быстрого поиска функционала в MainScene

**Изменения в v4.0:**
- ✅ Исправлены все ошибки TypeScript (было 74 → стало 0)
- ✅ Удалены устаревшие свойства `antialias` и `roundPixels` из `TextStyle` (не поддерживаются в Phaser 3)
- ✅ Обновлены имена: `runes` → `keys` в конфигурациях и типах
- ✅ Обновлены методы: `setOnPlayerRuneCollision` → `setOnPlayerKeyCollision`
- ✅ Исправлен тип `PortalState` (удалены `ACTIVATING_1`, `ACTIVATING_2`, осталось только `ACTIVATING`)
- ✅ Конструктор `SpawnSystem` теперь требует `quizManager` как третий параметр: `new SpawnSystem(scene, levelManager, quizManager)`
- ✅ Тип `LevelQuestionsData` обновлён: `winmessage` → `gameWinMessage` и `levelWinMessage`

---

## 📋 Содержание

1.  [Быстрый поиск](#быстрый-поиск)
2.  [Обзор проекта](#обзор-проекта)
3.  [Структура файлов](#структура-файлов)
4.  [Структура документации](#структура-документации)
5.  [Детальное описание компонентов](#детальное-описание-компонентов)
    *   [Игровые сцены](#-игровые-сцены-srcgamescenes)
    *   [Игровые сущности](#-игровые-сущности-srcgameentities)
    *   [Фоновые сущности](#-фоновые-сущности-srcgameentitiesbackground)
    *   [Объекты коллизии](#-объекты-коллизии-srcgameentitiescollision)
    *   [Утилиты](#-утилиты-srcgameutils)
    *   [Игровые системы](#-игровые-системы-srcgamesystems)
    *   [Ядро игры](#-ядро-игры-srcgamecore)
    *   [UI компоненты](#-ui-компоненты-srcgameui)
6.  [Конфигурационные файлы](#конфигурационные-файлы)
7.  [Типы и интерфейсы](#типы-и-интерфейсы)
8.  [Потоки данных](#потоки-данных)
9.  [Ключевые методы и функции](#ключевые-методы-и-функции)

---

## Быстрый поиск

### 🎮 Игровые сцены
- **BaseScene** → `src/game/scenes/BaseScene.ts` - Базовый класс сцены
- **LoadingScene** → `src/game/scenes/LoadingScene.ts` - Загрузка ресурсов
- **MainScene** → `src/game/scenes/MainScene.ts` - Основная игровая сцена
- **animation/** (`src/game/scenes/animation/`) - синхронизация анимаций
    - `AnimationSyncManager.ts` - центральный менеджер синхронизации
    - `KeyAnimationSync.ts` - синхронизация ключей
    - `CoinAnimationSync.ts` - синхронизация монет
    - `EnemyAnimationSync.ts` - синхронизация врагов
- **collision/** (`src/game/scenes/collision/`) - обработчики столкновений
    - `CollisionManager.ts` - управление всеми коллизиями
    - `ItemCollisionHandler.ts` - сбор предметов (сердечки, монеты, ключи)
    - `OracleCollisionHandler.ts` - взаимодействие с оракулом (депозит монет/ключей)
    - `EnemyCollisionHandler.ts` - столкновения с врагами
    - `PortalCollisionHandler.ts` - взаимодействие с порталами
- **quiz/** (`src/game/scenes/quiz/`) - логика викторин
    - `KeyQuizHandler.ts` - логика квиза для ключей
    - `CoinQuizHandler.ts` - логика квиза для монет
    - `PortalQuizHandler.ts` - логика квиза для порталов
- **enemy/** (`src/game/scenes/enemy/`) - система врагов
    - `EnemyManager.ts` - менеджер врагов
    - `EnemySpawner.ts` - спавнер врагов
    - `EnemyCloneFactory.ts` - фабрика клонов врагов
- **gameflow/** (`src/game/scenes/gameflow/`) - управление игровым потоком
    - `EventBusManager.ts` - менеджер событий
    - `GameOverHandler.ts` - обработчик окончания игры
    - `LevelTransitionHandler.ts` - обработчик перехода уровней
- **ui/** (`src/game/scenes/ui/`) - UI сцены
    - `CameraManager.ts` - менеджер камеры
    - `EffectsManager.ts` - менеджер эффектов
    - `HUDManager.ts` - менеджер HUD
- **world/** (`src/game/scenes/world/`) - управление миром
    - `WorldFactory.ts` - фабрика мира
    - `EntityFactory.ts` - фабрика сущностей
    - `CollisionObjectFactory.ts` - фабрика объектов коллизии
- **GlobalQuestionManager** → `src/game/scenes/quiz/GlobalQuestionManager.ts` - Менеджер глобальных вопросов

### 👤 Сущности
- **Player** → `src/game/entities/Player.ts` - Игрок
- **AbstractItem** → `src/game/entities/items/AbstractItem.ts` - Базовый класс предметов
- **Coin** → `src/game/entities/items/Coin.ts` - Монета (квиз)
    - **Entities:** Player.ts, Oracle.ts, AbstractEnemy.ts, AbstractPortal.ts, AbstractItem.ts, Coin.ts, Key.ts, Heart.ts
- **Heart** → `src/game/entities/items/Heart.ts` - Сердце (здоровье)
- **Key** → `src/game/entities/items/Key.ts` - Ключ (квиз)
- **AbstractEnemy** → `src/game/entities/enemies/AbstractEnemy.ts` - Базовый класс врагов
- **EnemyRandomWalker** → `src/game/entities/enemies/EnemyRandomWalker.ts` - Случайное блуждание
- **EnemyChaser** → `src/game/entities/enemies/EnemyChaser.ts` - Преследователь
- **EnemyFlam** → `src/game/entities/enemies/EnemyFlam.ts` - Универсальный враг с настраиваемым поведением (клонирование, преследование)
- **AbstractPortal** → `src/game/entities/portals/AbstractPortal.ts` - Базовый класс порталов
- **StandardPortal** → `src/game/entities/portals/StandardPortal.ts` - Стандартный портал
- **Oracle.ts** (`src/game/entities/Oracle.ts`) — Древний оракул. Принимает монетки (фаза COIN) и активируется, открывая порталы.

### 🎨 Фоновые сущности
- **AbstractBackgroundSprite** → `src/game/entities/background/AbstractBackgroundSprite.ts` - Базовый класс фоновых спрайтов
- **GrassBackgroundSprite** → `src/game/entities/background/GrassBackgroundSprite.ts` - Фоновые спрайты травы

### 🧱 Объекты коллизии
- **AbstractCollisionObject** → `src/game/entities/collision/AbstractCollisionObject.ts` - Базовый класс объектов коллизии
- **BushCollisionObject** → `src/game/entities/collision/BushCollisionObject.ts` - Кусты как объекты коллизии (только Standard Mode)

### ⚙️ Системы
- **HealthSystem** → `src/game/systems/HealthSystem.ts` - Система здоровья
- **ScoreSystem** → `src/game/systems/ScoreSystem.ts` - Система очков
- **QuizManager** → `src/game/systems/QuizManager.ts` - Управление вопросами
- **SpawnSystem** → `src/game/systems/SpawnSystem.ts` - Система спавна
- **CollisionSystem** → `src/game/systems/CollisionSystem.ts` - Система коллизий
- **AudioManager** → `src/game/systems/AudioManager.ts` - Управление звуком
- **AnimationManager** → `src/game/systems/AnimationManager.ts` - Универсальная система анимаций
- **SpriteAnimationHandler** → `src/game/systems/SpriteAnimationHandler.ts` - Обработчик анимаций для physics спрайтов
- **WorldGenerator** → `src/game/systems/WorldGenerator.ts` - Генератор игрового мира (Tiled Map / Random)

### 🔧 Ядро
- **AssetLoader** → `src/game/core/AssetLoader.ts` - Загрузка ресурсов
- **GameState.ts** (`src/game/core/GameState.ts`) — Хранит состояние игры: фаза (`COIN`/`KEY`), счёт, жизни, монеты, ключи.
- **LevelManager** → `src/game/core/LevelManager.ts` - Управление уровнями

### 🛠️ Утилиты
- **Logger** → `src/utils/Logger.ts` - Система категоризированного логирования
- **debugConfig** → `src/config/debugConfig.ts` - Флаги управления логированием
- **BrowserLogger** → `src/utils/BrowserLogger.ts` - Перехват консоли для скачивания логов
- **TextAnalyzer** → `src/game/utils/TextAnalyzer.ts` - Анализ текстов в вопросах
- **BubblePositionCalculator** → `src/game/utils/BubblePositionCalculator.ts` - Расчет позиции баббла
- **FontSizeCalculator** → `src/game/utils/FontSizeCalculator.ts` - Калькулятор размера шрифта
- **PixelFontCalculator** → `src/game/utils/PixelFontCalculator.ts` - Калькулятор для пиксельных шрифтов

### 🧪 Тестовая инфраструктура (`src/tests/`)
- **unit/** (`src/tests/unit/`) - модульные тесты
  - `core/` - тесты ядра (GameState, LevelManager, AssetLoader)
  - `entities/` - тесты сущностей (Player, Enemy, Portal, Oracle, Items)
  - `systems/` - тесты систем (HealthSystem, ScoreSystem, QuizManager, SpawnSystem)
  - `scenes/` - тесты сцен (BaseScene, LoadingScene, MainScene submodule)
  - `ui/` - тесты UI компонентов (Button, Modal, QuestionBubble, DebugOverlay)
  - `utils/` - тесты утилит (DeviceUtils, FontSizeCalculator)
  - `constants/` - тесты констант (scalingConstants)
- **integration/** (`src/tests/integration/`) - интеграционные тесты
  - `phaser-react/` - тесты интеграции Phaser и React (EventBus)
  - `bug-fixes/` - тесты исправлений багов (async-error-handling)
  - `scenes/` - интеграционные тесты сцен (MainScene)
  - `modal-scaling/` - тесты масштабирования модальных окон
- **e2e/** (`src/tests/e2e/`) - end-to-end тесты (Cypress)
  - `cypress/e2e/` - E2E тесты игрового процесса
- **mocks/** (`src/tests/mocks/`) - моки для тестов
  - `phaser-mock.ts` - моки Phaser объектов
- **helpers/** (`src/tests/helpers/`) - хелперы для тестов
  - `mocks.ts` - вспомогательные моки и утилиты
- **setup.ts** (`src/tests/setup.ts`) - настройка тестового окружения

### 📋 Документация - Основная (main/)

#### UI документация (ui/)
- **UI_GUIDE** → `documentation/main/ui/UI_GUIDE.md` - Комплексное руководство по UI (Grid Snapping, масштабирование, кнопки)
- **MODAL_GUIDE** → `documentation/main/ui/MODAL_GUIDE.md` - Руководство по модальным окнам (KeyQuestionModal, PortalModal, GameOverModal)
- **UI_COMPONENTS** → `documentation/main/ui/UI_COMPONENTS.md` - UI компоненты
- **UI_TEXT_SCALING** → `documentation/main/ui/UI_TEXT_SCALING.md` - Grid Snapping и масштабирование текста
- **FONT_SIZING_SYSTEM** → `documentation/main/ui/FONT_SIZING_SYSTEM.md` - Система расчёта размеров шрифтов
- **SCALING_SYSTEM** → `documentation/main/ui/SCALING_SYSTEM.md` - Система масштабирования

#### Development документация (development/)
- **DEBUGGING_GUIDE** → `documentation/main/development/DEBUGGING_GUIDE.md` - Руководство по отладке (DebugOverlay, флаги, диагностика)

#### Основная документация
- **GOLDEN_HEARTS_SYSTEM** → `documentation/main/GOLDEN_HEARTS_SYSTEM.md` - Система золотых сердечек (отображение ключей)
- **LOGGING_SYSTEM** → `documentation/main/LOGGING_SYSTEM.md` - Полная документация системы логирования
- **SCALING_SYSTEM** → `documentation/main/SCALING_SYSTEM.md` - Детальная система масштабирования и letterboxing
- **UI_COMPONENTS** → `documentation/main/UI_COMPONENTS.md` - Документация UI компонентов (Кнопки, Модальные окна, NineSlice)
- **UI_TEXT_SCALING** → `documentation/main/UI_TEXT_SCALING.md` - Grid Snapping и масштабирование текста
- **DEBUGGING** → `documentation/main/DEBUGGING.md` - Руководство по отладке
- **DEBUG_OVERLAY** → `documentation/main/DEBUG_OVERLAY.md` - Документация системы отладочного UI

#### Референсы (reference/)
- **DEBUG_UI_SYSTEM** → `documentation/main/reference/DEBUG_UI_SYSTEM.md` - Детальный анализ UI отладки

### 📋 Документация - LLM (export_llm/)
- Упрощённые версии для быстрого ознакомления ИИ (генерируются из main/)

### 🎨 UI компоненты
- **Button** → `src/game/ui/Button.ts` - Кнопка с состояниями (см. UI_COMPONENTS.md)
- **UIManager** → `src/game/ui/UIManager.ts`
**Назначение:** Централизованный менеджер всех модальных окон

**Управляемые модальные окна:**
- **KeyQuestionModal.ts** — Модальное окно вопроса для ключа (React-интеграция).
- **CoinBubbleQuiz.ts** — Легкий UI в экранном пространстве для вопросов монет (без модального окна).
- **PortalModal.ts** — Модальное окно для входа в портал.
- `GameOverModal` - модальное окно окончания игры

**Методы:**
- `handlePortalEnterRequest(data)` - Показать модальное окно портала
- `handleShowKeyQuiz(data)` - Показать модальное окно вопроса ключа
- `handleGameOver(data)` - Показать модальное окно Game Over
- `showGameWinModal(score, feedbackText, onRestart)` - Показать модальное окно победы
- `destroy()` - Уничтожить все модальные окна и отписаться от событий

**Особенности:**
- Управляется через события EventBus (`PORTAL_ENTER`, `SHOW_KEY_QUIZ`, `GAME_OVER`)
- Автоматическая очистка существующих модальных окон перед созданием новых
- Централизованная обработка всех модальных окон в одном месте
- **NineSliceBackground** → `src/game/ui/NineSliceBackground.ts` - 9-slice фон для модальных окон
- **CoinBubbleQuiz** → `src/game/ui/CoinBubbleQuiz.ts` - UI квиза с монетой (два баббла)
- **QuestionBubble** → `src/game/ui/QuestionBubble.ts` - Баббл сообщения с вопросом/ответом (для Оракула и порталов)
- **KeyQuestionModal** → `src/game/ui/KeyQuestionModal.ts` - Модальное окно вопроса ключа
- **PortalModal** → `src/game/ui/PortalModal.ts` - Модальное окно портала
- **GameOverModal** → `src/game/ui/GameOverModal.ts` - Модальное окно Game Over
- **DebugOverlay** → `src/game/ui/DebugOverlay.ts` - Отладочный UI и визуализация сетки матрицы спавна

### 📝 Типы
- **enemyTypes** → `src/types/enemyTypes.ts` - Типы врагов
- **portalTypes** → `src/types/portalTypes.ts` - Типы порталов
- **questionTypes** → `src/types/questionTypes.ts` - Типы вопросов
- **levelTypes** → `src/types/levelTypes.ts` - Типы уровней
- **gameTypes** → `src/types/gameTypes.ts` - Основные типы
- **scoreTypes** → `src/types/scoreTypes.ts` - Типы очков

### ⚙️ Конфигурация
- **gameConfig** → `src/config/gameConfig.ts` - Конфигурация игры (темы, A/B тесты)
- **levelConfigs** → `src/config/levelConfigs/level1.config.json` - Конфиги уровней
- **gameConstants** → `src/constants/gameConstants.ts` - Игровые константы (включая `ITEM_INTERACTION_RADIUS: 50`)
- **spritesheetConfigs** → `src/config/spritesheetConfigs.ts` - Конфигурация всех спрайтшитов и анимаций

---

## Обзор проекта

ArcadeQuiz - это игра-викторина с элементами аркады, где игрок собирает ключи, отвечает на вопросы и активирует порталы для победы.

**Технологии:**
- Phaser 3 - игровой движок
- React - UI компоненты
- TypeScript - типизация
- Vite - сборщик

---

## Структура файлов

```
ArcadeQuiz/
├── .cursor/
│   └── rules/
│       └── 00-main-rules.mdc          # Правила для Cursor AI
├── documentation/                      # Вся документация проекта
│   ├── main/                          # Основная документация
│   │   ├── game-systems/             # Игровые системы (4 файла)
│   │   ├── ui/                       # UI документация (6 файлов)
│   │   ├── development/              # Для разработчиков (8 файлов)
│   │   ├── project/                  # О проекте (4 файла)
│   │   ├── planning/                 # Планы и прогресс (5 файлов)
│   │   │   ├── LEVER_REFACTORING/    # План рефакторинга уровней
│   │   │   │   ├── AUDIT_LEVEL_SYSTEM.md
│   │   │   │   └── PLAN_LEVEL_REFACTORING.md
│   │   └── reference/                # Техническая документация (1 файл)
│   │   │   └── DEBUG_UI_SYSTEM.md    # Детальный анализ UI отладки
│   │   ├── ProjectMap.md              # Этот файл - карта проекта
│   │   ├── ARCHITECTURE.md            # Подробное описание архитектуры
│   │   ├── BUILD.md                   # Инструкции по сборке и запуску
│   │   ├── DEVELOPMENT.md             # Руководство по разработке
│   │   ├── DocumentationMaintenance.md # Поддержание документации
│   │   ├── TESTING.md                 # Руководство по тестированию
│   │   ├── GameDescription.md         # Описание игры
│   │   ├── PrototypeAnalysis.md       # Анализ прототипа
│   │   ├── ModularArchitecturePlan.md # План модульной архитектуры
│   │   ├── ModularArchitectureProgress.md # Прогресс реализации
│   │   ├── DEBUGGING.md               # Руководство по отладке и диагностике
│   │   ├── DEBUG_OVERLAY.md           # Документация системы отладочного UI
│   │   ├── LOGGING_SYSTEM.md          # Система логирования
│   │   ├── TILED_MAP_IMPLEMENTATION.md # Реализация уровней с Tiled Map
│   │   ├── BUBBLE_SYSTEM.md           # Система бабблов (вопросы/ответы)
│   │   ├── SPAWN_MATRIX_SYSTEM.md     # Матричная система позиционирования объектов
│   │   ├── SCALING_SYSTEM.md          # Система масштабирования и letterboxing
│   │   ├── GOLDEN_HEARTS_SYSTEM.md    # Система золотых сердечек (ключи)
│   │   ├── UI_COMPONENTS.md           # Документация UI компонентов
│   │   └── UI_TEXT_SCALING.md         # Grid Snapping и масштабирование текста
│   ├── export_llm/                    # Документация для LLM (упрощённые версии)
│   ├── templates/                     # Шаблоны документов
│   └── temp_docs/                     # Временные файлы-отчеты
│       ├── DocumentationCreationPlan.md # План создания документов
│       ├── DocumentationCreationReport.md # Отчет о создании документации
│       ├── DocumentationPlan.md      # План документации проекта
│       ├── DocumentationReorganizationReport.md # Отчет о реорганизации
│       ├── DocumentationStructureReport.md # Отчет о структуре
│       └── README.md                  # Описание папки
├── src/                                # Исходный код проекта
│   ├── main.tsx                        # Точка входа React
│   ├── react/                          # React компоненты
│   │   ├── App.tsx                     # Главный компонент
│   │   ├── PhaserGame.tsx              # Обертка Phaser
│   │   └── components/                 # UI компоненты
│   ├── game/                           # Phaser игровая логика
│   │   ├── EventBus.ts                 # Система событий
│   │   ├── core/                       # Ядро игры
│   │   ├── entities/                   # Игровые сущности
│   │   │   ├── items/                  # Предметы
│   │   │   │   ├── AbstractItem.ts     # Базовый класс для всех собираемых предметов.
│   │   │   │   ├── Coin.ts             # Золотая монета (фаза COIN).
│   │   │   │   ├── Key.ts              # Магический ключ (фаза KEY).
│   │   │   │   └── Heart.ts            # Сердечко (восстанавливает здоровье).
│   │   ├── scenes/                     # Игровые сцены
│   │   ├── systems/                    # Игровые системы
│   │   └── ui/                         # UI элементы Phaser
│   ├── types/                          # TypeScript типы
│   ├── constants/                      # Константы
│   ├── config/                         # Конфигурация
│   ├── assets/                         # Ресурсы игры
│   └── tests/                          # Тесты проекта
│       ├── unit/                       # Unit тесты
│       ├── integration/                 # Integration тесты
│       └── e2e/                        # E2E тесты (Cypress)
└── ...
```

---

## Детальное описание компонентов

### 🎮 Игровые сцены (`src/game/scenes/`)

#### `BaseScene.ts`
**Путь:** `src/game/scenes/BaseScene.ts`  
**Назначение:** Базовый класс для всех игровых сцен

**Методы:**
- `initBaseSystems()` - Инициализация AssetLoader, LevelManager, GameState
- `setupPhysics()` - Настройка физики Arcade
- `setupCamera()` - Настройка камеры
- `getCameraWorldSize()` - Получение размера камеры в координатах мира

**Свойства:**
- `assetLoader: AssetLoader` - Загрузчик ресурсов
- `levelManager: LevelManager` - Менеджер уровней
- `gameState: GameState` - Глобальное состояние

#### `LoadingScene.ts`
**Путь:** `src/game/scenes/LoadingScene.ts`  
**Назначение:** Сцена загрузки ресурсов

**Методы:**
- `preload()` - Загрузка всех текстур и спрайтлистов
- `create()` - Создание анимаций, переход в MainScene

**Загружаемые ресурсы:**
- Все спрайтшиты загружаются через `AnimationManager` из `SPRITESHEET_CONFIGS`
- Спрайтшиты: `beast_sheet`, `dragon_sheet`, `flam_sheet`, `boy_walk_sheet`, `key_sheet`, `boy_jump`
- Анимации создаются в `MainScene` после загрузки (не в `LoadingScene`)
- Изображения: `Heart5x5.png` (для здоровья над головой игрока), `Portal_64x48.png` (для порталов), `QuestionImage*.png`
- Ключи используют `key_sheet` вместо `Dice 20.png`

#### `MainScene.ts`
**Путь:** `src/game/scenes/MainScene.ts`  
**Назначение:** Основная игровая сцена

**Ключевые методы:**
- `create()` - Инициализация сцены
- `update()` - Игровой цикл
- `spawnInitialObjects()` - Начальный спавн объектов
- `setupPeriodicSpawns()` - Настройка периодических спавнов
- `handlePlayerKeyCollision(key)` - Обработка сбора ключа (запуск мини-квиза)
  - ✅ Защита от двойного вызова: проверка на существующее модальное окно и флаг обработки ключа
  - ✅ Каждый ключ помечается флагом `isProcessing` для предотвращения повторной обработки
  - ✅ Флаг сбрасывается при завершении взаимодействия (правильный/неправильный ответ, закрытие, ошибка)
- `handlePortalOverlapEntry()` - Обработка входа в портал
- `showPortalModal()` - Показ модального окна портала
- `handlePortalEntry()` - Обработка входа в портал (победа/поражение)
- `handleQuizCompleted(data)` - Единый обработчик завершения квиза (успех/неудача)
- `handlePlayerOracleCollision()` - Обработка взаимодействия с Оракулом
- `handlePortalEnterConfirmed(portal)` - Вход в портал после подтверждения
- `handlePortalEnterCancelled()` - Отмена входа в портал
- `createGameWorldTiled()` - Создание мира из Tiled Map
- `createGameWorldRandom()` - Создание случайного мира (старый режим)
- `resumeGame()` - Возобновление игры после паузы
- `showGlobalQuestion()` - Показ глобального вопроса при активации алтаря
- `createPortals()` - Создание порталов из глобального вопроса
- `handleGameOver()` - Обработка окончания игры

**Свойства:**
- `player: Player` - Игрок
- `enemyInstances: AbstractEnemy[]` - Массив врагов
- `portalInstances: AbstractPortal[]` - Массив порталов
- `enemies, chasers, hearts, keys, portals: Phaser.Physics.Arcade.Group` - Группы объектов
- `healthSystem, scoreSystem, quizManager, audioManager, spawnSystem, collisionSystem` - Системы
- `currentKeyQuestionModal, currentPortalModal, currentGameOverModal` - Модальные окна
- `pendingPortal: AbstractPortal | null` - Портал, ожидающий входа
- `debugOverlay?: DebugOverlay` - Отладочный UI (создается при `DEBUG_UI_ENABLED === true`)

**Отладочный UI (DebugOverlay):**
- Создается в `create()` после настройки камеры (строки 226-237)
- Обновляется в `update()` каждый кадр (строки 560-561, 1583-1584)
- Уничтожается в `shutdown()` (строки 3933-3938)
- Управляется через переменную окружения `VITE_DEBUG_UI_ENABLED`
- См. [DEBUG_OVERLAY.md](DEBUG_OVERLAY.md) для подробной документации

### 👤 Игровые сущности (`src/game/entities/`)

#### `Player.ts`
**Путь:** `src/game/entities/Player.ts`  
**Назначение:** Класс игрока

**Методы:**
- `getPosition()` - Получить позицию
- `getX()`, `getY()` - Получить координаты
- `stop()` - Остановить движение и анимацию
- `getSprite()` - Получить спрайт

**Управление:**
- Клавиатура: WASD / стрелки
- Drag and Move для мобильных устройств

**Параметры:**
- Скорость: `PLAYER_SPEED = 250` (из `gameConstants.ts`)
- Размер: `ACTOR_SIZES.PLAYER = 4.0`
- Спрайтшит: `boy_walk_sheet` (GRID, 4x4 — 4 направления x 4 кадра)
- Анимации: `boy_down`, `boy_up`, `boy_left`, `boy_right`
- Использует `SpriteAnimationHandler` для автоматической синхронизации кадров
- **Depth:** `6` (выше порталов, предметов и кустов, но ниже врагов и оракула)

#### `AbstractEnemy.ts`
**Путь:** `src/game/entities/enemies/AbstractEnemy.ts`  
**Назначение:** Базовый класс врагов

**Методы:**
- `updateAI(player)` - Абстрактный метод AI (переопределяется в подклассах)
- `update(player)` - Обновление врага
  - ✅ Защита от клонирования во время паузы: проверка `scene.scene.isPaused()` в начале метода
  - ✅ Если сцена на паузе (модальное окно открыто), враг не обновляется и не клонируется
- `onPlayerCollision(player)` - Обработка коллизии с игроком
- `getDamage()` - Получить урон
- `isImmortalEnemy()` - Проверить бессмертность
- `destroy()` - Уничтожить врага
- `isActive()` - Проверить активность

**Свойства:**
- `sprite: Phaser.Types.Physics.Arcade.SpriteWithDynamicBody` - Спрайт
- `type: EnemyType` - Тип врага
- `baseSpeed: number` - Базовая скорость
- `isImmortal: boolean` - Флаг бессмертности
- `damage: number` - Урон при коллизии (по умолчанию 1)
- `isDying: boolean` - Флаг уничтожения
- `animationHandler: SpriteAnimationHandler` - Обработчик анимаций

**Размер:** `setDepth(7)` - Враги выше персонажа, но ниже оракула

**Спрайтшиты:**
- `beast_sheet` — для `EnemyRandomWalker` (GRID, 4x4)
- `dragon_sheet` — для `EnemyChaser` (GRID, 4x4)
- `flam_sheet` — для `EnemyFlam` (GRID, 4x4)
- Анимации: `{prefix}_down`, `{prefix}_up`, `{prefix}_left`, `{prefix}_right`
- Использует `SpriteAnimationHandler` для автоматической синхронизации кадров

#### `EnemyRandomWalker.ts`
**Путь:** `src/game/entities/enemies/EnemyRandomWalker.ts`  
**Назначение:** Враг со случайным блужданием

**AI:** Случайное движение по карте, отскок от границ

#### `EnemyChaser.ts`
**Путь:** `src/game/entities/enemies/EnemyChaser.ts`  
**Назначение:** Враг-преследователь

**AI:** 
- Преследует игрока в радиусе `chaseRadius` (из конфига, например 256px)
- Использует `chaseSpeed` при преследовании (если указана и > 0), иначе `baseSpeed`
- Иначе случайное блуждание с базовой скоростью

#### `EnemyFlam.ts`
**Путь:** `src/game/entities/enemies/EnemyFlam.ts`  
**Назначение:** Универсальный враг с настраиваемым поведением через конфиг

**AI:** 
- Поведение настраивается через конфиг уровня (`chaseRadius`, `cloneDetectionRadius`, `cloneCount`, `chaseSpeed`)
- Может преследовать игрока (если `chaseRadius > 0`)
- Может клонироваться при приближении игрока (если `cloneDetectionRadius > 0` и `cloneCount > 0`)
- Иначе случайное блуждание с периодической сменой направления (каждые 2 секунды)
- Использует `chaseSpeed` при преследовании (если указана и > 0), иначе `baseSpeed`

#### `AbstractPortal.ts`
**Путь:** `src/game/entities/portals/AbstractPortal.ts`  
**Назначение:** Базовый класс порталов

**Методы:**
- `depositKey()` - Депозит ключа (требуется 3 для открытия)
- `activate()` - Активация портала
- `isOpen()` - Проверить открыт ли портал
- `isActivating()` - Проверить активируется ли
- `mustExit()` - Проверить флаг mustExit
- `setMustExit()` - Установить флаг mustExit
- `resetMustExit()` - Сбросить флаг mustExit
- `getDamage()` - Получить урон (по умолчанию 1)
- `getConfig()` - Получить конфиг портала
- `getSprite()` - Получить спрайт

**Свойства:**
- `sprite: Phaser.Physics.Arcade.Sprite` - Спрайт портала
- `config: PortalConfig` - Конфигурация портала
- `state: PortalState` - Состояние портала
- `damage: number` - Урон при входе в неправильный портал (по умолчанию 1)

**Размер:** `ACTOR_SIZES.PORTAL = 2.0`  
**Размер спрайта:** 64×48 пикселей (до масштабирования)  
**Текстуры и анимации:**
- Базовое состояние: `Portal_X.Base_24F_1536x48.png` (X = A, B, C) - 24 кадра, 8 fps, циклично
- Анимация активации: `Portal_X.Activation_24F_1536x48.png` (X = A, B, C) - 24 кадра, 8 fps, циклично во время активации
- Активированное состояние: `Portal_X.Activated_24F_1536x48.png` (X = A, B, C) - 24 кадра, 8 fps, циклично
- Все состояния порталов используют анимации (нет статических текстур)
**Depth:** `4` (выше кустов и текстов, но ниже предметов, персонажа, врагов и оракула)

#### `StandardPortal.ts`
**Путь:** `src/game/entities/portals/StandardPortal.ts`  
**Назначение:** Стандартный портал

**Методы:**
- `onKeyDeposit()` - Обработка депозита ключа
- `onEnter()` - Обработка входа (возвращает `isCorrect`)

#### `Oracle.ts`
**Путь:** `src/game/entities/Oracle.ts`  
**Назначение:** Класс оракула с системой состояний и анимаций

**Методы:**
- `depositKey()` - Добавить ключ в оракул (переходит в состояние ACTIVATING)
- `getState()` - Получить текущее состояние (OracleState)
- `getStoredKeys()` - Получить количество хранимых ключей
- `isActivated()` - Проверить, активирован ли оракул
- `getSprite()` - Получить спрайт
- `reset()` - Сбросить состояние оракула
- `destroy()` - Уничтожить оракул

**Свойства:**
- `sprite: Phaser.Physics.Arcade.Sprite` - Спрайт оракула
- `currentState: OracleState` - Текущее состояние (BASE, ACTIVATING, ACTIVATED)
- `storedKeys: number` - Количество хранимых ключей (0-3)
- `maxKeys: number` - Максимальное количество ключей (3)

**Состояния:**
- `BASE` - Базовое состояние (статическая текстура `Oracle.Base_32x64.png`)
- `ACTIVATING` - Состояние активации (анимация `oracle_activation`, проигрывается один раз при каждом ключе)
- `ACTIVATED` - Активированное состояние (анимация `oracle_activated`, проигрывается циклично)

**Размер:** `ACTOR_SIZES.ORACLE = 1.0`  
**Текстуры:**
- Базовое состояние: `Oracle.Base_32x64.png` (статическая текстура, 32×64 пикселей)
- Анимация активации: `Oracle.Activation_16F_512x64.png` (16 кадров, 32×64 каждый, 8 fps, repeat: 0)
- Активированное состояние: `Oracle.Activated_16F_512x64.png` (16 кадров, 32×64 каждый, 8 fps, repeat: -1)
**Depth:** `8` (самый верхний слой игровых объектов, выше всех остальных)

**Особенности:**
- Использует систему состояний с автоматическими переходами
- Для physics спрайтов требуется ручная синхронизация кадров в `MainScene.update()`
- При доставке каждого ключа проигрывается анимация активации (16 кадров, 2 секунды)
- После получения всех 3 ключей переходит в активированное состояние с циклической анимацией (16 кадров, 2 секунды на цикл)
- Все анимации используют единую скорость 8 fps

### 🎨 Фоновые сущности (`src/game/entities/background/`)

#### `AbstractBackgroundSprite.ts`
**Путь:** `src/game/entities/background/AbstractBackgroundSprite.ts`
**Назначение:** Абстрактный класс для фоновых спрайтов

**Интерфейс:** `BackgroundSpriteConfig` - конфигурация фонового спрайта

**Методы:**
- `createSprite(x, y, frameIndex)` - Создать спрайт с указанным фреймом
- `getRandomFrameIndex()` - Получить случайный индекс фрейма
- `spawnOnMap(mapWidth, mapHeight, count?)` - Разместить спрайты на карте (абстрактный)
- `destroy()` - Уничтожить все спрайты
- `getSprites()` - Получить все спрайты

**Особенности:**
- Используется для декоративных элементов без коллизий
- **Depth:** `1` (выше текстуры карты -100, но ниже всех остальных объектов)
- Поддержка кадрирования текстуры для выбора фрейма

#### `GrassBackgroundSprite.ts`
**Путь:** `src/game/entities/background/GrassBackgroundSprite.ts`
**Назначение:** Фоновые спрайты травы

**Текстура:** `Background.Grass_128x128.png` (128×128, 8×8 сетка, 64 кадра)

**Методы:**
- `spawnOnMap(mapWidth, mapHeight, count?)` - Разместить траву случайно по карте

### 🧱 Объекты коллизии (`src/game/entities/collision/`)

#### `AbstractCollisionObject.ts`
**Путь:** `src/game/entities/collision/AbstractCollisionObject.ts`
**Назначение:** Абстрактный класс для статичных объектов коллизии

**Интерфейс:** `CollisionObjectConfig` - конфигурация объекта коллизии

**Методы:**
- `createSprite(x, y, frameIndex)` - Создать физический спрайт с коллизией
- `getRandomFrameIndex()` - Получить случайный индекс фрейма
- `spawnOnMap(mapWidth, mapHeight, count?, spawnSystem?)` - Разместить объекты на карте (абстрактный)
- `destroy()` - Уничтожить все спрайты
- `getSprites()` - Получить все спрайты
- `getGroup()` - Получить группу спрайтов для коллизий

**Особенности:**
- Статичные объекты с физикой и коллизией (`setImmovable(true)`, `setPushable(false)`)
- **Depth:** `6` (по умолчанию выше порталов, но ниже игрока)
- Отладочная отрисовка тела коллизии (опционально)
- Размер коллизии с отступом для кустов (`COLLISION_CONFIG.BUSH_COLLISION_OFFSET`)

#### `BushCollisionObject.ts`
**Путь:** `src/game/entities/collision/BushCollisionObject.ts`
**Назначение:** Кусты как объекты коллизии

**Текстура:** `Bush.32x32.png` (32×32, 4×4 сетка, 16 кадров)

**Методы:**
- `spawnOnMap(mapWidth, mapHeight, count?, spawnSystem?)` - Разместить кусты через SpawnMatrix

### 🛠️ Утилиты (`src/game/utils/`)

#### Система логирования

**📚 Документация:** [LOGGING_SYSTEM.md](LOGGING_SYSTEM.md) - Полное описание системы логирования

**✅ Статус рефакторинга:** 345+ `console.log` → `logger.log()` (ЗАВЕРШЕНО)

#### `Logger.ts`
**Путь:** `src/utils/Logger.ts`
**Назначение:** Основной класс системы логирования с категориями

**Методы:**
- `log(category, message, data?)` - Добавить лог с категорией
- `warn(category, message, data?)` - Добавить предупреждение
- `clear()` - Очистить логи
- `getLogs()` - Получить все логи как массив
- `getLogsAsString()` - Получить логи как строку
- `downloadLogs(filename?)` - Скачать логи как файл
- `setConsoleLogging(enabled)` - Включить/выключить лог в консоль
- `setFileLogging(enabled)` - Включить/выключить лог в файл

**Особенности:**
- **50+ категорий** для тонкой настройки (COLLISION, QUIZ, UI, SCENE_INIT, и т.д.)
- **Фильтрация через .env** - управление без изменения кода
- **Timestamp** в ISO формате для каждого лога
- **Circular reference** защита при сериализации объектов
- **Максимум 10000 логов** в памяти с автоматическим удалением старых

**Категории (основные):**
- `COLLISION`, `COLLISION_PORTAL`, `COLLISION_ITEM` → `DEBUG_COLLISION`
- `QUIZ`, `QUIZ_GLOBAL`, `QUIZ_KEY`, `QUIZ_PORTAL` → `DEBUG_QUIZ`
- `GAME_OVER`, `LEVEL_TRANSITION` → `DEBUG_GAMEFLOW`
- `MODAL_UI`, `MODAL_SIZE`, `BUTTON_EVENTS` → `DEBUG_UI`
- `SCENE_INIT`, `SCENE_CREATE`, `SCENE_PHYSICS`, `SCENE_CAMERA` → `DEBUG_SCENE_INIT`
- `PLAYER_STATE`, `PLAYER_ANIMATION`, `ENEMY_*` → `DEBUG_ENTITIES`/`DEBUG_ANIMATION`
- `SPAWN_SYSTEM`, `WORLD_GENERATOR`, `ENTITY_FACTORY` → `DEBUG_SYSTEMS`

**Пример использования:**
```typescript
import { logger } from '@/utils/Logger';

logger.log('PLAYER_STATE', 'Health changed', { current: 2, max: 3 });
// Вывод: [2026-01-24T18:19:55.285Z] [PLAYER_STATE] Health changed {"current":2,"max":3}

logger.warn('COLLISION', 'No body found', { entity: key });
// Вывод: [2026-01-24T18:19:55.285Z] [WARN] [COLLISION] No body found {...}
```

#### `debugConfig.ts`
**Путь:** `src/config/debugConfig.ts`
**Назначение:** Флаги управления логированием и вспомогательные функции

**Экспортируемые флаги:**
- `DEBUG_ALL` - Глобальный флаг (включает ВСЁ)
- `DEBUG_ENABLED` - Базовый debug режим
- `DEBUG_SCENES` - Логи сцен
- `DEBUG_UI` - Логи UI
- `DEBUG_ENTITIES` - Логи сущностей
- `DEBUG_SYSTEMS` - Логи систем
- `DEBUG_COLLISION` - Логи коллизий
- `DEBUG_ANIMATION` - Логи анимаций
- `DEBUG_QUIZ` - Логи викторин
- `DEBUG_GAMEFLOW` - Логи игрового потока
- `DEBUG_BOOTSTRAP` - Логи загрузки игры
- `DEBUG_ASSETS` - Логи загрузки ассетов
- `DEBUG_SCENE_INIT` - Детальная инициализация сцен
- `DEBUG_PERF` - Логи производительности
- `DEBUG_SPAWN_VERBOSE` - Детальные логи сетки

**Визуальные флаги (Debug Overlay):**
- `DEBUG_OVERLAY_ENABLED` - Текстовый оверлей (FPS, позиция, счёт)
- `DEBUG_VISUAL_GRID_ENABLED` - Визуальная сетка спавна
- `DEBUG_SPAWN_GRID_ENABLED` - Детальные логи сетки в консоли

**Вспомогательные функции:**
```typescript
import {
  logScene,
  logUI,
  logEntity,
  logSystem,
  logCollision,
  logAnimation,
  logQuiz,
  logGameflow,
  logPerf,
  logBootstrap,
  logAsset,
  logSceneInit
} from '@/config/debugConfig';

logScene('MainScene created');
logUI('Modal shown', { modalType: 'KEY_QUESTION' });
```

#### `BrowserLogger.ts`
**Путь:** `src/utils/BrowserLogger.ts`
**Назначение:** Перехват console.log/warn/error для скачивания логов из браузера

**Функции в консоли браузера:**
```javascript
downloadLogs()           // Скачать 'animation_debug.log'
getLogCount()            // Получить количество логов
clearLogs()              // Очистить все логи
logger.downloadLogs('custom-name.log')  // Кастомное имя
```

**Особенности:**
- Автоматически перехватывает все console вызовы
- Хранит логи в памяти для последующего скачивания
- Добавляет подсказки при загрузке игры в консоль

#### `TextAnalyzer.ts`
**Путь:** `src/game/utils/TextAnalyzer.ts`
**Назначение:** Утилита для анализа текстов в вопросах

**Интерфейс:** `LongestTexts` - результаты анализа (question, answer, feedback, maxLength)

**Функции:**
- `findLongestTexts(questionsData)` - Найти самый длинный текст в данных уровня

**Особенности:**
- Анализирует miniQuizzes, globalQuizzes, globalQuestion, globalQuestionWithImage
- Находит самые длинные тексты для вопросов, ответов и фидбэков
- Поддерживает как один уровень, так и массив уровней

#### `BubblePositionCalculator.ts`
**Путь:** `src/game/utils/BubblePositionCalculator.ts`
**Назначение:** Утилита для расчета позиции баббла сообщения

**Функции:**
- `calculateBubbleY(spriteY, bubbleType, spriteType)` - Вычислить позицию Y баббла

**Правила позиционирования:**
- По X: баббл всегда по центру Оракула/портала
- По Y: нижняя граница баббла совпадает с верхней границей спрайта

#### `FontSizeCalculator.ts`
**Путь:** `src/game/utils/FontSizeCalculator.ts`
**Назначение:** Калькулятор размера шрифта с проверкой дефолтного размера

**Функции:**
- `calculateOptimalBaseFontSize()` - Рассчитать оптимальный базовый размер (бинарный поиск)
- `calculateBaseFontSize()` - Рассчитать размер шрифта для вопроса/фидбэка
- `calculateButtonFontSize()` - Рассчитать размер шрифта для кнопок
- `calculateUnifiedBaseFontSize()` - Рассчитать единый базовый размер для всех модальных окон

**Особенности:**
- Всегда стремится использовать дефолтный размер, уменьшает только если текст не влезает
- Использует бинарный поиск для нахождения максимального влезающего размера
- Поддержка пиксельных шрифтов через `PixelFontCalculator`

#### `PixelFontCalculator.ts`
**Путь:** `src/game/utils/PixelFontCalculator.ts`
**Назначение:** Специальный калькулятор для пиксельных шрифтов

**Функции:**
- `calculatePixelBaseFontSize()` - Рассчитать размер для пиксельного шрифта (текст)
- `calculatePixelButtonFontSize()` - Рассчитать размер для пиксельного шрифта (кнопки)

### ⚙️ Игровые системы (`src/game/systems/`)

#### `HealthSystem.ts`
**Путь:** `src/game/systems/HealthSystem.ts`  
**Назначение:** Система здоровья

**Методы:**
- `takeDamage(amount)` - Нанести урон, возвращает `isAlive`
- `heal(amount)` - Восстановить здоровье
- `updatePlayerHealthPosition()` - Обновить позицию здоровья над игроком
- `getHealth()` - Получить текущее здоровье
- `getMaxHealth()` - Получить максимальное здоровье

**Особенности:**
- Использует текстуру `Heart5x5.png` с масштабом 4x для отображения сердечек над головой игрока
- Контейнер со здоровьем имеет **depth `6`** (на уровне персонажа)

#### `ScoreSystem.ts`
**Путь:** `src/game/systems/ScoreSystem.ts`  
**Назначение:** Система подсчета очков

**Методы:**
- `addKeyScore(isUnique)` - Добавить очки за ключ (+3 за уникальный, +1 за повторный, -1 за потерю)
- `addPortalScore()` - Добавить очки за правильный портал (+10)
- `getScore()` - Получить текущие очки
- `getMaxPossibleScore()` - Получить макс. возможный счет на уровне
- `getTotalMaxPossibleScore()` - Получить макс. возможный счет за всю игру
- `reset()` - Сбросить очки

#### `QuizManager.ts`
**Путь:** `src/game/systems/QuizManager.ts`  
**Назначение:** Управление вопросами

**Методы:**
- `loadLevelQuestions(levelNumber)` - Загрузить вопросы уровня
- `getRandomMiniQuiz()` - Получить случайный мини-квиз для ключа
- `getGlobalQuestion()` - Получить глобальный вопрос для порталов
- `parseQuestion(questionData)` - Парсить вопрос (текст или с изображением)
- `getWinMessages()` - Получить победные сообщения

#### `SpawnSystem.ts`
**Путь:** `src/game/systems/SpawnSystem.ts`  
**Назначение:** Система безопасного спавна объектов на карте с использованием матричной системы

**📚 Документация:** [SPAWN_MATRIX_SYSTEM.md](SPAWN_MATRIX_SYSTEM.md) - Подробное описание матричной системы позиционирования

**✅ Матричная система спавна:**
- Использует `SpawnMatrix` для отслеживания занятости ячеек карты
- Карта 2048×2048 разделена на ячейки 64×64 (итого 32×32 ячейки)
- Типы занятости: `'free'`, `'permanent'`, `'item'`, `'enemy'`
- Гарантирует отсутствие пересечений объектов при спавне
- Враги могут перезаписывать предметы, но не постоянные объекты (порталы, оракул, кусты)

**Методы матричной системы:**
- `spawnOracleMatrix()` - Спавн оракула в центре (2×4 ячейки)
- `spawnPlayerMatrix(oracleX, oracleY)` - Спавн персонажа под оракулом (1×1 ячейка)
- `spawnPortalMatrix(centerX, centerY, radius)` - Спавн портала на окружности (4×3 ячейки)
- `spawnBushMatrix()` - Спавн куста (2×2 ячейки)
- `spawnHeartMatrix()` - Спавн сердечка (1×1 ячейка)
- `spawnKeyMatrix()` - Спавн ключа (1×1 ячейка)
- `spawnEnemyMatrix()` - Спавн врага (1×1 ячейка, может перезаписывать предметы)

**Ключевые методы:**
- `spawnInitialEnemies(enemyGroup, chaserGroup, playerX?, playerY?)` - Начальный спавн врагов (использует матрицу)
- `spawnEnemy(enemyGroup, chaserGroup, playerX?, playerY?)` - Периодический спавн врага (использует матрицу)
- `spawnItems(heartsGroup, keysGroup, playerX?, playerY?)` - Начальный спавн предметов (использует матрицу)
- `spawnPeriodicItems(heartsGroup, keysGroup, playerX?, playerY?)` - Периодический спавн предметов (использует матрицу)
- `getSafePosition(radius, playerX?, playerY?)` - Устаревший метод (используется как fallback)
- `clearOccupiedZones()` - Очистить матрицу (для перезапуска уровня)

**Особенности:**
- Все позиции возвращаются в **виртуальных координатах игрового мира** (0-2048)
- Матрица автоматически выравнивает позиции по ячейкам
- Гарантирует отсутствие пересечений объектов
- При невозможности найти свободную позицию возвращает `{ success: false }`

**Использует:** `LevelManager` для получения конфигов спавна, `SpawnMatrix` для управления занятостью ячеек

#### `CollisionSystem.ts`
**Путь:** `src/game/systems/CollisionSystem.ts`  
**Назначение:** Система обработки коллизий

**Методы:**
- `setupCollisions()` - Настройка всех коллизий
- Обработчики коллизий для всех типов объектов
- **Поддержка Tiled Map:** Обработка воксельных коллизий и Sensor Body интеракции

#### `AudioManager.ts`
**Путь:** `src/game/systems/AudioManager.ts`  
**Назначение:** Управление звуком

**Методы:**
- `loadAllSounds()` - Загрузка всех аудио файлов из конфигурации
- `playBackgroundMusic()`, `playWinMusic()`, `playGameOverMusic()` - Музыка
- `playButtonClick()`, `playQuestionSuccess()`, `playQuestionFailure()` - Звуки UI
- `playPickupLife()`, `playPickupRune()`, `playApplyRune()` - Звуки игровых событий
- `playDamage()`, `playDamageRune()` - Звуки урона
- `playOracleActivated()`, `playPortalActivated()`, `playPortalActivatedComplete()` - Звуки активации
- `setMasterVolume()`, `setMusicVolume()`, `setSoundVolume()` - Управление громкостью
- `updateAllVolumes()` - Обновление громкости всех звуков
- `pauseAll()`, `resumeAll()`, `stopMusic()` - Управление воспроизведением

**Особенности:**
- Все звуки определены в `SOUND_KEYS` и `SOUND_VOLUMES` в `gameConstants.ts`
- Трехуровневая система громкости: master, music, sound
- Индивидуальные базовые громкости для каждого звука
- Порталы имеют два звука активации: `PORTAL_ACTIVATED` (начало) и `PORTAL_ACTIVATED_COMPLETE` (когда появляется надпись ответа)
- Звук `damage` воспроизводится только при столкновении с врагами на карте, не используется в модальных окнах

#### `AnimationManager.ts`
**Путь:** `src/game/systems/AnimationManager.ts`  
**Назначение:** Универсальная система загрузки спрайтшитов и создания анимаций

**Методы:**
- `loadSpritesheet(config)` - Загрузка спрайтшита
- `createAnimations(spritesheetKey, animations)` - Создание анимаций
- `generateHorizontalFrames(start, end)` - Генерация кадров для горизонтальной полосы
- `generateVerticalFrames(start, end, cols)` - Генерация кадров для вертикальной полосы
- `generateGridDirectionFrames(direction, frameCount, cols)` - Генерация кадров для сетки с направлениями
- `generateGridFrames(startRow, endRow, startCol, endCol, cols)` - Генерация кадров для произвольной сетки

**Особенности:**
- Все спрайтшиты конфигурируются в `src/config/spritesheetConfigs.ts`
- Поддержка разных форматов: HORIZONTAL, VERTICAL, GRID, CUSTOM
- Автоматическая валидация кадров
- Предотвращение повторной загрузки и создания анимаций

#### `SpriteAnimationHandler.ts`
**Путь:** `src/game/systems/SpriteAnimationHandler.ts`  
**Назначение:** Универсальный обработчик анимаций для физических спрайтов

**Методы:**
- `playDirectionAnimation(velocityX, velocityY)` - Воспроизведение анимации по направлению движения
- `syncFrame()` - Ручная синхронизация кадров (для physics спрайтов)
- `stop()` - Остановка анимации
- `isPlaying()` - Проверка воспроизведения
- `getCurrentAnimation()` - Получение текущей анимации

**Особенности:**
- Используется для игрока и врагов (physics спрайты)
- Ручная синхронизация кадров через таймер (Phaser не обновляет кадры автоматически для physics спрайтов)
- Поддержка "мертвой зоны" для предотвращения мерцания анимаций

#### `WorldGenerator.ts`
**Путь:** `src/game/systems/WorldGenerator.ts`
**Назначение:** Генерация игрового мира

**Методы:**
- `generate(tiledMapKey, width, height)` - Генерация мира (выбирает способ в зависимости от конфига)
- `createTiledWorld(tiledMapKey)` - Создание мира из Tiled Map
- `createRandomWorld(width, height)` - Создание случайного мира (старый алгоритм)

**Особенности:**
- Поддерживает два режима: проработанные уровни из Tiled Map и процедурную генерацию
- Управляется флагом `useTiledMap` в конфиге уровня
- Интегрирован со `SpawnMatrix` для безопасного размещения объектов

### 🔧 Ядро игры (`src/game/core/`)

#### `AssetLoader.ts`
**Путь:** `src/game/core/AssetLoader.ts`  
**Назначение:** Загрузка ресурсов по темам

**Методы:**
- `loadImage(key, path)` - Загрузить изображение
- `loadAudio(key, path)` - Загрузить аудио
- `loadJSON(key, path)` - Загрузить JSON
- `loadLevelQuestions(levelNumber)` - Загрузить вопросы уровня

**Использует:** `CURRENT_THEME` из `gameConfig.ts` для выбора папки ресурсов

#### `GameState.ts`
**Путь:** `src/game/core/GameState.ts`  
**Назначение:** Глобальное состояние игры

**Методы:**
- `getHealth()`, `setHealth(value)` - Управление здоровьем
- `getKeys()`, `addKey()`, `removeKey()`, `setKeys(value)` - Управление ключами
- `getScore()`, `setScore(value)` - Управление очками
- `getLevel()`, `setLevel(value)` - Управление уровнем
- `isAltarActivated()`, `setAltarActivated(value)` - Состояние алтаря
- `reset()` - Сбросить состояние

#### `LevelManager.ts`
**Путь:** `src/game/core/LevelManager.ts`
**Назначение:** Управление уровнями

**✅ РЕФАКТОРИНГ v4.9:** Динамическая загрузка конфигураций из Phaser Cache.

**Методы:**
- `loadLevelConfig(levelNumber)` - Загрузить конфиг уровня из Phaser Cache (через `AssetLoader.getJSON()`)
- `getCurrentLevelConfig()` - Получить конфиг текущего уровня
- `getEnemySpawnConfig()` - Получить конфиг спавна врагов
- `getEnemySpeedConfig()` - Получить конфиг скорости врагов
- `getItemSpawnConfig()` - Получить конфиг спавна предметов
- `determineEnemyType(spawnConfig)` - Определить тип врага на основе соотношений
- `nextLevel()` - Перейти на следующий уровень (с проверкой MAX_LEVELS)
- `getDifficultyMultiplier()` - Получить множитель сложности
- `reset()` - Сбросить на первый уровень

**Особенности:**
- ✅ **JSON Schema Validation:** Проверка структуры загруженного JSON с fallback
- ✅ **Кеширование:** Загруженные конфиги кешируются во внутреннем Map
- ✅ **N-Level Support:** Работает с любым количеством уровней через MAX_LEVELS

**Fallback:** Если конфиг не найден в Cache, использует `getDefaultLevelConfig()`

**Источник данных:**
- Конфиги уровней: `src/config/levelConfigs/level{N}.config.json`
- Загружаются в LoadingScene через `dynamic import()` (Vite поддерживает импорт JSON из src/)

### 🎨 UI компоненты (`src/game/ui/`)

#### `Button.ts`
**Путь:** `src/game/ui/Button.ts`  
**Назначение:** Интерактивная кнопка с состояниями

**Состояния:** `NORMAL`, `HOVER`, `ACTIVE`, `DISABLED`, `BLINKING`, `WRONG`

**Методы:**
- `setState(state)` - Установить состояние
- `getState()` - Получить состояние
- `setText(text)` - Установить текст
- `setVisible(visible)` - Показать/скрыть
- `setDepth(depth)` - Установить depth
- `destroy()` - Уничтожить кнопку

**Особенности:**
- Защита от двойных кликов (проверка времени между pointerdown и pointerup)
- Адаптивный размер шрифта
- Поддержка wordWrap для длинного текста

#### `UIManager.ts`
**Путь:** `src/game/ui/UIManager.ts`
**Назначение:** Централизованный менеджер всех модальных окон

**Управляемые модальные окна:**
- **KeyQuestionModal.ts** — Модальное окно вопроса для ключа (React-интеграция).
- **CoinBubbleQuiz.ts** — Легкий UI в экранном пространстве для вопросов монет (без модального окна).
- **PortalModal.ts** — Модальное окно для входа в портал.
- `GameOverModal` - модальное окно окончания игры

**Методы:**
- `handlePortalEnterRequest(data)` - Показать модальное окно портала
- `handleShowKeyQuiz(data)` - Показать модальное окно вопроса ключа
- `handleGameOver(data)` - Показать модальное окно Game Over
- `showGameWinModal(score, feedbackText, onRestart)` - Показать модальное окно победы
- `destroy()` - Уничтожить все модальные окна и отписаться от событий

**Особенности:**
- Управляется через события EventBus (`PORTAL_ENTER`, `SHOW_KEY_QUIZ`, `GAME_OVER`)
- Автоматическая очистка существующих модальных окон перед созданием новых
- Централизованная обработка всех модальных окон в одном месте

#### `NineSliceBackground.ts`
**Путь:** `src/game/ui/NineSliceBackground.ts`
**Назначение:** 9-slice фон для модальных окон

**Текстура:** `UI.DialogBox_24x24.png` (спрайтшит 3×3, кадры 8×8)

**Методы:**
- `resize(width, height)` - Изменить размер фона
- `setTint(color)` - Установить цвет для всех сегментов
- `clearTint()` - Очистить цвет
- `setAlpha(alpha)` - Установить прозрачность
- `setupInteractive()` - Настроить интерактивность контейнера

**Особенности:**
- Использует TileSprite для границ и центра (повторение паттерна)
- Углы (A, C, G, I) рисуются поверх границ для предотвращения видимых швов
- Округление позиций для предотвращения размытия

#### `KeyQuestionModal.ts`
**Путь:** `src/game/ui/KeyQuestionModal.ts`  
**Назначение:** Модальное окно для вопросов при поднятии ключей

**Конфигурация:**
- Размер: 95% ширины экрана, 71.25% высоты
- Depth: 1000-1002

**Методы:**
- `handleAnswerClick(index, isCorrect)` - Обработка клика по ответу
- `handleClose()` - Обработка закрытия
- `destroy()` - Уничтожить модальное окно
- `setVisible(visible)` - Показать/скрыть

**Логика:**
- Правильный ответ: мигает, второе нажатие закрывает и дает ключ
- Неправильный ответ: показывает wrongFeedback, отключает кнопку, наносит урон
- Защита от случайных нажатий: кнопки активны через 300мс после создания

#### `QuestionBubble.ts`
**Путь:** `src/game/ui/QuestionBubble.ts`  
**Назначение:** Баббл сообщения для отображения вопросов Оракула и ответов порталов

**Типы бабблов:**
- `'oracle'` — для Оракула (BubbleMsg.Transparent152x56.png, 152×56px)
- `'portal'` — для порталов (BubbleMsg.Transparent136x48.png, 136×48px)

**Основные методы:**
- `setQuestion(questionData, assetLoader)` - Установить вопрос и опциональное изображение
- `toggleVisibility()` - Переключить видимость (показать/скрыть)
- `show()` / `hide()` - Показать/скрыть с анимацией
- `showHint(parentX, parentY, parentType)` - Показать подсказку под родительским объектом
- `updatePosition(x, y)` - Обновить позицию баббла
- `destroy()` - Уничтожить баббл

**Особенности:**
- Автоматический расчет размера шрифта на основе самого длинного текста
- Глобальный размер шрифта для всех бабблов (оптимизация)
- Поддержка вопросов с изображениями (текст 3/4 ширины, изображение 1/4)
- Анимация появления/исчезновения (fade in/out, 300ms)
- Подсказки с циклической ping-pong анимацией (75% → 25% → 75%)
- Управление видимостью через тап/клик по родительскому объекту
- Depth: 10 (максимальный слой, выше всех элементов)

**Интеграция:**
- Oracle: создается в `createQuestionBubble()`, управляется через `setQuestion()`, `toggleQuestionBubble()`
- Portal: создается в `createLabels()`, показывается при активации

**Документация:** [BUBBLE_SYSTEM.md](BUBBLE_SYSTEM.md)

#### `PortalModal.ts`
**Путь:** `src/game/ui/PortalModal.ts`  
**Назначение:** Модальное окно для подтверждения входа в портал

**Конфигурация:**
- Размер: рассчитывается через `calculateModalSize()` на основе реального размера canvas
- Depth: 1000-1002
- Layout: 5 равных блоков (вопрос, картинка, ответ, кнопка Войти, кнопка Отмена)

**Расчет размеров шрифтов:**
- Использует единый `baseFontSize` для всех элементов (вопрос, ответ, кнопки)
- `baseFontSize` рассчитывается через `calculateOptimalBaseFontSize()` на основе самого длинного текста
- Все элементы включаются в расчет `unifiedFontSize` (минимум из всех)
- Применяется единый множитель `1.5` ко всем элементам
- Гарантирует идентичность размеров шрифтов с кнопками в окнах конца игры

**Методы:**
- `handleEnter()` - Обработка входа в портал
- `handleCancel()` - Обработка отмены
- `destroy()` - Уничтожить модальное окно
- `setVisible(visible)` - Показать/скрыть

**Особенности:**
- Защита от случайных нажатий: кнопки активны через 150мс
- Цветовая индикация: зеленый для правильного портала, красный для неправильного
- Звук `damage` не воспроизводится при входе в неправильный портал (урон наносится, но звук только при столкновении с врагами)
- Отложенное создание UI: `scene.time.delayedCall(1, ...)` для корректного расчета размеров canvas

#### `GameOverModal.ts`
**Путь:** `src/game/ui/GameOverModal.ts`  
**Назначение:** Модальное окно окончания игры

**Типы:** `WIN_GAME`, `WIN_LEVEL`, `LOSE`

**Конфигурация:**

#### `DebugOverlay.ts`
**Путь:** `src/game/ui/DebugOverlay.ts`
**Назначение:** Класс для управления отладочным UI и визуализацией сетки матрицы спавна

**Основные функции:**
- **Текстовый оверлей:** FPS, позиция игрока, счёт, статистика врагов, ключи
- **Визуальная сетка:** ячейки матрицы спавна 64×64 с цветовой индикацией занятости
- Управление жизненным циклом отладочных элементов

**Методы:**
- `create()` — создание отладочного UI (текст)
- `createSpawnMatrixGrid()` — создание сетки матрицы спавна
- `update()` — обновление UI и сетки (независимо по флагам)
- `destroy()` — уничтожение элементов

**Зависимости:**
- Использует паттерн Dependency Injection через интерфейс `DebugOverlayDependencies`
- Получает данные через функции-геттеры (игрок, состояние игры, система спавна, враги, ключи, счёт)

**Конфигурация (3 независимых флага):**
| Флаг | Назначение | .env переменная |
|------|------------|-----------------|
| `DEBUG_OVERLAY_ENABLED` | Текстовый оверлей (FPS, позиция, счёт, враги) | `VITE_DEBUG_OVERLAY_ENABLED` |
| `DEBUG_VISUAL_GRID_ENABLED` | Визуальная сетка на карте (ячейки, занятость) | `VITE_DEBUG_VISUAL_GRID_ENABLED` |
| `DEBUG_SPAWN_GRID_ENABLED` | Детальные логи сетки в консоли | `VITE_DEBUG_SPAWN_GRID_ENABLED` |

**Документация:**
- [DEBUG_OVERLAY.md](../development/DEBUG_OVERLAY.md) — актуальная документация класса
- [DEBUG_UI_SYSTEM.md](../reference/DEBUG_UI_SYSTEM.md) — общая система отладочного UI и HUD

**Конфигурация:**
- Размер: рассчитывается через `calculateModalSize()` на основе реального размера canvas
- Depth: 2000-2002
- Layout: 5 равных блоков (заголовок, счетчик, персонаж, кнопка Рестарт, кнопка "Следующий уровень")

**Расчет размеров шрифтов:**
- Использует единый `baseFontSize` для всех элементов (заголовок, счетчик, кнопки)
- `baseFontSize` рассчитывается через `calculateOptimalBaseFontSize()` на основе самого длинного текста
- Все элементы включаются в расчет `unifiedFontSize` (минимум из всех)
- Применяется единый множитель `1.5` ко всем элементам
- Гарантирует идентичность размеров шрифтов с кнопками в окнах порталов

**Методы:**
- `destroy()` - Уничтожить модальное окно
- `setVisible(visible)` - Показать/скрыть
- `getPlayerSprite()` - Получить спрайт персонажа для обновления анимации

**Особенности:**
- Появляется с задержкой 1 секунда после события окончания игры, чтобы успели проиграться звуки события
- Отложенное создание UI: `scene.time.delayedCall(1, ...)` для корректного расчета размеров canvas
- Пересоздание UI при изменении ориентации экрана (не закрывается, так как игра завершена)

---

## Конфигурационные файлы

### `src/config/gameConfig.ts`
**Путь:** `src/config/gameConfig.ts`  
**Назначение:** Конфигурация игры

**Экспорты:**
- `CURRENT_THEME` - Текущая тема (`Game_01` или `Game_02`)
- `ASSETS_BASE_PATH` - Базовый путь к ресурсам
- `AB_TESTING` - Флаги A/B тестирования:
  - `ENABLE_FEEDBACKS` - Показывать feedbacks для правильных ответов
  - `ENABLE_WRONG_FEEDBACKS` - Показывать wrongFeedbacks для неправильных ответов

**Использует:** `import.meta.env` для получения переменных окружения (типы определены в `src/vite-env.d.ts`)

### `src/vite-env.d.ts`
**Путь:** `src/vite-env.d.ts`  
**Назначение:** TypeScript типы для Vite environment variables

**Содержит:**
- Интерфейс `ImportMetaEnv` с типами для переменных окружения:
  - `VITE_CURRENT_THEME?: string`
  - `VITE_ENABLE_FEEDBACKS?: string`
  - `VITE_ENABLE_WRONG_FEEDBACKS?: string`
- Расширение интерфейса `ImportMeta` для поддержки `import.meta.env`

### `src/config/levelConfigs/level1.config.json`
**Путь:** `src/config/levelConfigs/level1.config.json`  
**Назначение:** Конфигурация уровня 1

**Структура:**
```json
{
  "levelNumber": 1,
  "enemySpawn": {
    "initialCount": {             // Абсолютные количества для каждого типа
      "randomWalker": 4,          // Начальное количество randomWalker
      "chaser": 2,                // Начальное количество chaser
      "flam": 2                   // Начальное количество flam
    },
    "initialSpawnDelay": 200,     // Интервал между спавном начальных врагов (мс)
    "periodicSpawnDelay": 6000,   // Задержка периодического спавна (мс)
    "maxEnemies": 15,             // Максимальное количество врагов (null = не контролировать)
    "enabledTypes": ["randomWalker", "chaser", "flam"]
  },
  "enemySpeed": {
    "randomWalker": 80,          // Скорость случайных блуждающих
    "chaser": 30,                // Скорость преследующих
    "flam": 60                   // Скорость врага Flam
  },
  "enemyHealth": {               // ✅ Количество жизней для каждого типа врага
    "randomWalker": 2,            // Количество жизней случайных блуждающих
    "chaser": 1,                 // Количество жизней преследующих
    "flam": 2                    // Количество жизней врага Flam
  },
  "enemyBehavior": {             // ✅ Настройки поведения врагов (машина состояний)
    "randomWalker": {
      "chaseRadius": 0,          // Радиус преследования (0 = не преследует)
      "chaseSpeed": 80,           // Скорость преследования
      "cloneDetectionRadius": 0, // Радиус детекции для клонирования (0 = не клонируется)
      "cloneCount": 0,            // Количество клонов
      "cloneSpawnDelay": 300,     // Задержка спавна клонов (мс)
      "cloneLifetime": 10000,     // Время жизни клонов (мс)
      "clonesCanClone": false,    // Могут ли клоны создавать новые клоны
      "showDetectionRadius": true // Показывать ли радиус детекции (для отладки)
    },
    "chaser": {
      "chaseRadius": 256,         // Радиус преследования (преследует игрока)
      "chaseSpeed": 75,           // Скорость преследования
      "cloneDetectionRadius": 0,
      "cloneCount": 0,
      "cloneSpawnDelay": 0,
      "cloneLifetime": 0,
      "clonesCanClone": false,
      "showDetectionRadius": true
    },
    "flam": {
      "chaseRadius": 0,
      "chaseSpeed": 75,
      "cloneDetectionRadius": 205, // Радиус детекции для клонирования
      "cloneCount": 2,             // Количество клонов при детекции
      "cloneSpawnDelay": 300,
      "cloneLifetime": 10000,
      "clonesCanClone": false,
      "showDetectionRadius": true
    }
  },
  "itemSpawn": {
    "hearts": {
      "initial": 5,              // Начальное количество сердец
      "max": 10,                 // Максимальное количество сердец
      "spawnDelay": 24000        // Задержка периодического спавна (мс)
    },
    "keys": {
      "initial": 8,              // Начальное количество ключей
      "max": 10,                 // Максимальное количество ключей
      "spawnDelay": 24000        // Задержка периодического спавна (мс)
    }
  },
  "backgroundSprites": {         // ✅ Конфигурация фоновых спрайтов
    "grass": {
      "count": 81,               // Количество спрайтов травы
      "density": 0.08            // Плотность размещения
    }
  },
  "collisionObjects": {          // ✅ Конфигурация объектов коллизии
    "bush": {
      "count": 10,               // Количество кустов
      "showCollisionDebug": true // Показывать ли отладочную графику коллизий
    }
  }
}
```

**Примечания:**
- `enemyHealth` и `enemyBehavior` - опциональные поля (если не указаны, используются значения по умолчанию)
- `backgroundSprites` и `collisionObjects` - опциональные поля (если не указаны, объекты не спавнятся)
- Все враги используют матричную систему спавна для безопасного размещения

### `src/constants/gameConstants.ts`
**Путь:** `src/constants/gameConstants.ts`  
**Назначение:** Игровые константы

**Константы масштабирования:**
- `GAME_WIDTH = 720` - Виртуальная ширина экрана
- `GAME_HEIGHT = 1280` - Виртуальная высота экрана
- `BASE_SCALE = 4.0` - Базовый масштаб для всех игровых объектов

**Константы карты:**
- `MAP_WIDTH = 512` - Базовая ширина карты (масштабируется до 2048 через BASE_SCALE)
- `MAP_HEIGHT = 512` - Базовая высота карты (масштабируется до 2048 через BASE_SCALE)
- `MAP_CENTER_X = 1024` - Центр карты по X в виртуальных координатах
- `MAP_CENTER_Y = 1024` - Центр карты по Y в виртуальных координатах

**Константы движения:**
- `PLAYER_SPEED = 250` - Скорость игрока (в виртуальных координатах)
- `ENEMY_SPEED = 80` - Скорость обычных врагов (в виртуальных координатах)
- `CHASER_SPEED = 45` - Скорость преследующих (в виртуальных координатах)
- `MAX_HEALTH = 3` - Максимальное здоровье

**✅ Константы уровней (v4.9):**
- `MAX_LEVELS = 3` - Максимальное количество уровней в игре
- **Dynamic Asset Keys:**
  - `getLevelAssetKey(level, type)` - Генерирует ключ ассета (например, `map_bg_standard_l1`)
  - `LevelAssetKeys.getMapBgStandard(level)` - Получить ключ фона для уровня
  - `LevelAssetKeys.getMapBgTiledBase(level)` - Получить ключ tiled base для уровня
  - `LevelAssetKeys.getMapBgTiledStruct(level)` - Получить ключ tiled struct для уровня
  - `LevelAssetKeys.getMapBgTiledOverlay(level)` - Получить ключ tiled overlay для уровня
  - `LevelAssetKeys.getLevelConfig(level)` - Получить ключ конфига для уровня

**Как добавить новый уровень:**
1. Создайте `level{N}.config.json` в `src/config/levelConfigs/`
2. Создайте 4 фоновых изображения `src/assets/Game_01/images/Bg.*_Level{N}_512x512.png`
3. Создайте `src/assets/Game_01/questions/level{N}.questions.json` и `level{N}.coin-quiz.json`
4. Создайте `src/assets/Game_01/level_maps/Level{N}_map.json` (опционально)
5. Измените `MAX_LEVELS` в `src/constants/gameConstants.ts`

**ACTOR_SIZES:**
- `LARGE_ACTORS: 4.0` - Размер для игрока и врагов
- `MEDIUM_ACTORS: 2.0` - Размер для порталов и алтаря
- `SMALL_ACTORS: 4.0` - Размер для сердечек и ключей
- `PLAYER: 4.0` - Размер игрока (использует LARGE_ACTORS)
- `ENEMY: 4.0` - Размер врагов (использует LARGE_ACTORS)
- `HEART: 4.0` - Размер сердечек (использует SMALL_ACTORS)
- `KEY: 6.0` - Размер ключей (переопределение SMALL_ACTORS)
- `PORTAL: 2.0` - Размер порталов (использует MEDIUM_ACTORS)
- `ALTAR: 2.0` - Размер алтаря (использует MEDIUM_ACTORS)

**SOUND_KEYS:** Ключи всех звуков (BACKGROUND_MUSIC, BUTTON_CLICK, PICKUP_LIFE, PICKUP_RUNE, APPLY_RUNE, DAMAGE, DAMAGE_RUNE, QUESTION_SUCCESS, QUESTION_FAILURE, QUESTION_CLOSE, PORTAL_ENTER, PORTAL_CANCEL, WIN_MUSIC, GAME_OVER_MUSIC, ORACLE_ACTIVATED, PORTAL_ACTIVATED, PORTAL_ACTIVATED_COMPLETE)

**SOUND_VOLUMES:** Базовые громкости для каждого звука (от 0.0 до 1.0)

**KEYS:** Ключи текстур для внутренней генерации

**MINI_QUIZZES:** Массив мини-квизов (fallback, если не загружены из JSON)

**GLOBAL_QUESTION:** Глобальный вопрос (fallback)

**PORTALS_DATA:** Данные порталов (fallback, если не созданы из вопроса)

### `src/config/spritesheetConfigs.ts`
**Путь:** `src/config/spritesheetConfigs.ts`  
**Назначение:** Конфигурация всех спрайтшитов и анимаций

**Структура:**
- Массив `SPRITESHEET_CONFIGS` с конфигурациями для каждого спрайтшита
- Каждая конфигурация содержит:
  - `load` — параметры загрузки (key, path, frameWidth, frameHeight, layout, frameCount/rows/cols)
  - `animations` — массив анимаций (key, frames, frameRate, repeat, delay, yoyo)

**Поддерживаемые форматы:**
- `HORIZONTAL` — горизонтальная полоса (ключи, BoyJump)
- `VERTICAL` — вертикальная полоса
- `GRID` — сетка (игрок, враги: 4x4 — 4 направления x 4 кадра)
- `CUSTOM` — произвольные кадры

**Все спрайтшиты:**
- `beast_sheet`, `dragon_sheet`, `flam_sheet` — враги (GRID, 4 направления)
- `boy_walk_sheet` — игрок (GRID, 4 направления)
- `key_sheet` — ключ (HORIZONTAL, 4 кадра)
- `boy_jump` — анимация окончания игры (HORIZONTAL, 4 кадра)

### `src/assets/{THEME}/questions/level1.json`
**Путь:** `src/assets/{CURRENT_THEME}/questions/level1.json`  
**Назначение:** Вопросы уровня

**Структура:**
```json
{
  "category": "Название категории",
  "winmessage": {
    "win1": ["Сообщения при победе с 1 жизнью"],
    "win2": ["Сообщения при победе с 2 жизнями"],
    "win3": ["Сообщения при победе с 3 жизнями"]
  },
  "miniQuizzes": [
    {
      "question": "Текст вопроса",
      "correctAnswer": "Правильный ответ",
      "wrongAnswers": ["Неправильный 1", "Неправильный 2"],
      "feedbacks": ["Фидбэк 1", "Фидбэк 2"],
      "wrongFeedbacks": ["Фидбэк неправильного 1", "Фидбэк неправильного 2"]
    },
    {
      "question_Sign": {
        "text": "Текст вопроса с изображением",
        "image": "QuestionImage1st.png"
      },
      "correctAnswer": "Правильный ответ",
      "wrongAnswers": ["Неправильный 1", "Неправильный 2"],
      "feedbacks": ["Фидбэк 1"],
      "wrongFeedbacks": ["Фидбэк неправильного 1"]
    }
  ],
  "globalQuestion": {
    "question": "Глобальный вопрос для порталов",
    "correctAnswer": "Правильный ответ",
    "wrongAnswers": ["Неправильный 1", "Неправильный 2"],
    "feedbacks": ["Фидбэк 1"],
    "wrongFeedbacks": ["Фидбэк неправильного 1"]
  },
  "globalQuestionWithImage": {
    "question_Sign": {
      "text": "Глобальный вопрос с изображением",
      "image": "QuestionImage2nd.png"
    },
    "correctAnswer": "Правильный ответ",
    "wrongAnswers": ["Неправильный 1", "Неправильный 2"],
    "feedbacks": ["Фидбэк 1"],
    "wrongFeedbacks": ["Фидбэк неправильного 1"]
  }
}
```

---

## Типы и интерфейсы

### `src/types/portalTypes.ts`
- `PortalType` - enum: `STANDARD`, `TRAP`, `BONUS`
- `PortalConfig` - Конфигурация портала (id, type, isCorrect, answerText, damage?)
- `PortalState` - Состояние портала (isOpen, storedKeys, isActivating, mustExit)

### `src/types/levelTypes.ts`
- `EnemySpawnConfig` - Конфиг спавна врагов
- `EnemySpeedConfig` - Конфиг скорости врагов
- `ItemSpawnConfig` - Конфиг спавна предметов
- `LevelConfig` - Полный конфиг уровня

### `src/types/questionTypes.ts`
- `QuestionType` - enum: `TEXT_ONLY`, `TEXT_WITH_IMAGE`
- `StandardQuestion` - Стандартный вопрос (только текст)
- `QuestionWithImage` - Вопрос с изображением
- `QuestionData` - Объединенный тип вопроса
- `ParsedQuestion` - Результат парсинга вопроса
- `LevelQuestionsData` - Полная структура JSON файла с вопросами
- `WinMessages` - Победные сообщения

### `src/types/enemyTypes.ts`
- `EnemyType` - enum: `RANDOM_WALKER`, `CHASER`, `IMMORTAL`
- `EnemyConfig` - Конфигурация врага

### `src/types/gameTypes.ts`
- Основные типы игры

### `src/types/scoreTypes.ts`
- Типы для системы очков

### `src/types/animationTypes.ts`
- `SpritesheetLayout` — enum: `HORIZONTAL`, `VERTICAL`, `GRID`, `CUSTOM`
- `AnimationFrame` — интерфейс для кастомных кадров с длительностью
- `AnimationConfig` — конфигурация анимации (key, frames, frameRate, repeat, delay, yoyo)
- `SpritesheetLoadConfig` — конфигурация загрузки спрайтшита (key, path, frameWidth, frameHeight, layout, frameCount/rows/cols)
- `SpritesheetConfig` — полная конфигурация (load + animations)

---

## Потоки данных

### Инициализация игры
```
index.html
  └─> main.tsx
       └─> App.tsx
            ├─> PhaserGame.tsx (создает Phaser.Game)
            │    └─> LoadingScene (загрузка ресурсов)
            │         └─> MainScene (основная сцена)
            └─> UIOverlay (HUD)
```

### Коммуникация React ↔ Phaser
```
EventBus (Phaser.Events.EventEmitter)
  ├─> React компоненты подписываются на события
  │    ├─> 'update-hud' (здоровье, ключи, алтарь)
  │    ├─> 'show-quiz' (показать вопрос)
  │    └─> 'game-over' (конец игры)
  └─> Phaser сцены отправляют события
       ├─> MainScene.emitHUDUpdate()
       ├─> MainScene.handlePlayerKeyCollision()
       └─> MainScene.handleGameOver()
```

### Игровой цикл
```
MainScene.update()
  ├─> Player.update() (движение)
  ├─> HealthSystem.updatePlayerHealthPosition()
  ├─> Enemy.update() (AI врагов)
  ├─> Camera follow (автоматическое через startFollow)
  ├─> CollisionSystem (проверка коллизий)
  │   ├─> handlePlayerKeyCollision() → KeyQuestionModal
  │   │   ✅ Защита от двойного вызова: проверка на существующее модальное окно и флаг обработки ключа
  │   └─> handlePortalOverlapEntry() → PortalModal
  ├─> DebugOverlay.update() (если DEBUG_UI_ENABLED: каждые 30 кадров для UI, каждые 10 кадров для сетки)
  └─> Rune rings rendering
```

### Инициализация отладочного UI
```
MainScene.create()
  ├─> setupCameraFollow() (настройка камеры)
  ├─> ... (создание игровых объектов)
  └─> DebugOverlay (если DEBUG_UI_ENABLED)
       ├─> new DebugOverlay(scene, dependencies) (строки 226-234)
       ├─> debugOverlay.create() (создание debug text, строка 235)
       └─> debugOverlay.createSpawnMatrixGrid() (создание сетки, строка 237)
```

### Взаимодействие с порталами
```
handlePortalOverlapEntry(portal)
  └─> showPortalModal(portal)
       └─> PortalModal
            ├─> onEnter → handlePortalEntry()
            │              ├─> isCorrect → WIN (scoreSystem.addPortalScore())
            │              └─> !isCorrect → LOSE (healthSystem.takeDamage(portal.getDamage()))
            └─> onCancel → resumeGame() (возобновление игры)
```

---

## Ключевые методы и функции

### Управление спавном объектов

**✅ Матричная система:**
- Все объекты спавнятся через матричную систему `SpawnMatrix`
- Матрица отслеживает занятость ячеек 64×64 пикселей
- Гарантирует отсутствие пересечений объектов
- Враги могут перезаписывать предметы, но не постоянные объекты (порталы, оракул, кусты)

**Начальный спавн:**
- `MainScene.createOracle()` → `SpawnSystem.spawnOracleMatrix()` (2×4 ячейки)
- `MainScene.createPlayer()` → `SpawnSystem.spawnPlayerMatrix()` (1×1 ячейка)
- `MainScene.createPortals()` → `SpawnSystem.spawnPortalMatrix()` (4×3 ячейки каждый)
- `MainScene.createCollisionObjects()` → `SpawnSystem.spawnBushMatrix()` (2×2 ячейки каждый)
- `MainScene.spawnInitialObjects()` → `SpawnSystem.spawnInitialEnemies()` + `SpawnSystem.spawnItems()`

**Периодический спавн:**
- `MainScene.setupPeriodicSpawns()` создает таймеры:
  - Враги: каждые `enemySpawn.periodicSpawnDelay` мс → `SpawnSystem.spawnEnemy()` (использует матрицу)
  - Предметы: каждые `itemSpawn.keys.spawnDelay` мс → `SpawnSystem.spawnPeriodicItems()` (использует матрицу)

**Параметры из конфига:**
- `LevelManager.getItemSpawnConfig()` → `itemSpawn.hearts.initial/max/spawnDelay`
- `LevelManager.getItemSpawnConfig()` → `itemSpawn.keys.initial/max/spawnDelay`
- `LevelManager.getEnemySpawnConfig()` → `enemySpawn.initialCount/maxEnemies/periodicSpawnDelay`

### Управление уроном

**Урон от порталов:**
- `AbstractPortal.damage` - устанавливается из `PortalConfig.damage` или по умолчанию `1`
- Используется в `MainScene.handlePortalEntry()` → `healthSystem.takeDamage(portal.getDamage())`

**Урон от врагов:**
- `AbstractEnemy.damage` - по умолчанию `1`, можно установить в `EnemyConfig`
- Используется в `CollisionSystem` при коллизии игрок-враг

**Урон от неправильных ответов на ключах:**
- `ParsedQuestion.damage` - по умолчанию `1` для мини-вопросов
- Используется в `MainScene.handleKeyQuizWrong(damage)`
- **Важно:** Звук `damage` не воспроизводится при неправильном ответе на ключе (только при столкновении с врагами на карте)

### Управление depth (Z-order)

**Послойный порядок снизу вверх:**

1. **Фоновая карта:** `-200` (расширенный фон), `-100` (основная карта)
2. **Трава:** `1` (фоновые спрайты)
3. **Тексты глобальных вопросов у оракула и ответов у порталов:** `2`
4. **Кусты:** `3` (объекты коллизии)
5. **Порталы:** `4` (спрайты порталов)
6. **Сердечки и ключи:** `5` (предметы на карте)
7. **Персонаж:** `6` (игрок)
8. **Враги:** `7` (все типы врагов)
9. **Оракул:** `8` (самый верхний слой игровых объектов)

**Дополнительные элементы:**
- **Сердечки над головой игрока:** `6` (на уровне персонажа)
- **Модальные окна:** `1000-1002`, `2000-2002` (поверх всего)
- **HUD элементы:** `1000` (ключи, подсказки)

---

## Структура MainScene.ts

**Файл:** `src/game/scenes/MainScene.ts`
**Размер:** 2665 строк (было ~4599, сокращение на 42%)
**Назначение:** Основная игровая сцена, управляющая всем игровым процессом
**Рефакторинг:** Завершен 2026-01-19 (10 шагов модуляризации)

### Модульная структура
MainScene использует 7 модулей в `src/game/scenes/`:

#### `animation/` (~500 строк вынесено)
- AnimationSyncManager, KeyAnimationSync, PortalAnimationSync, OracleAnimationSync, PlayerAnimationSync, EnemyAnimationSync
- **index.ts** - экспорты всех модулей анимации

#### `collision/` (~400 строк вынесено)
- EnemyCollisionHandler, ItemCollisionHandler, OracleCollisionHandler, PortalCollisionHandler
- **index.ts** - экспорты всех обработчиков коллизий

#### `enemy/` (~300 строк вынесено)
- EnemyManager, EnemySpawner, EnemyCloneFactory
- **index.ts** - экспорты всех модулей врагов

#### `quiz/` (~350 строк вынесено)
- GlobalQuestionManager, KeyQuizHandler, PortalQuizHandler
- **index.ts** - экспорты всех модулей квизов

#### `ui/` (~400 строк вынесено)
- HUDManager, CameraManager, EffectsManager
- **index.ts** - экспорты всех UI менеджеров

#### `gameflow/` (~780 строк вынесено)
- EventBusManager, GameOverHandler, LevelTransitionHandler
- **index.ts** - экспорты всех обработчиков игрового потока

#### `world/` (~350 строк вынесено)
- WorldFactory, EntityFactory, CollisionObjectFactory
- **index.ts** - экспорты всех фабрик мира

**Система index.ts файлов:**
Каждый модуль включает `index.ts` для централизованного экспорта, обеспечивая чистые импорты:
```typescript
// Вместо:
import { EnemyManager } from './enemy/EnemyManager';
import { EnemySpawner } from './enemy/EnemySpawner';

// Используется:
import { EnemyManager, EnemySpawner } from './enemy';
```

**Подробнее:** `documentation/Plans/Refactoring/Refactoring_MASTER_PLAN.md`

### Секции файла (по порядку)

#### 1. Импорты и свойства (строки 1-211)
- Импорты всех зависимостей (Phaser, системы, сущности, UI)
- Свойства класса (player, enemies, portals, системы, менеджеры)

#### 2. Жизненный цикл сцены

**`create()` (строки 212-679)**
- Инициализация всех систем игры
- Настройка EventBus
- Создание игрового мира (Tiled или Random)
- Создание коллизий, игрока, порталов, оракула
- Настройка камеры, HUD, отладочного UI

**`update(time, delta)` (строки 2032-2762)**
- Обновление игрока и врагов
- Синхронизация анимаций physics спрайтов
- Проверка коллизий
- Обновление HUD и DebugOverlay

#### 3. Создание игрового мира

**`createGameWorld()` (строки 745-791)**
- Выбор режима: Tiled Map или Random
- Вызов соответствующего метода создания

**`createGameWorldTiled()` (строки 851-906)**
- Загрузка Tiled Map (`level1.json`)
- Создание коллизий по tile layers
- Создание спрайтов из object layers

**`createGameWorldRandom()` (строки 907-963)**
- Создание случайной карты
- Генерация препятствий

**`createBackgroundSprites()` (строки 1213-1240)**
- Создание фоновых спрайтов для Random режима

**`createExtendedBackground()` (строки 1390-1457)**
- Расширенный фон за пределами камеры

#### 4. Коллизии

**`createCollisionObjects()` (строки 1241-1277)**
- Создание статических коллизий из Tiled или Random

**`setupCollisions()` (вызывается в create)**
- Настройка коллизий между всеми объектами

**`handlePlayerEnemyCollision()` (строки 2764-2839)**
- Обработка столкновения игрока с врагом

**`handlePlayerHeartCollision()` (строки 2847-3010)**
- Обработка подбора сердечка

**`handlePlayerOracleCollision()` (строки 3109-3191)**
- Обработка столкновения с оракулом

**`handlePortalSolidCollision()` (строки 3192-3339)**
- Твердая коллизия с порталами

**`handlePortalOverlapByMask()` (строки 866-906)**
- Перекрытие с tile маской портала

#### 5. Сущности

**`createPlayer()` (строки 1278-1304)**
- Создание игрока на стартовой позиции

**`createOracle()` (строки 964-999)**
- Создание оракула с системой состояний

**`createPortals()` (строки 1000-1152)**
- Создание порталов A, B, C из конфигурации
- Настройка коллизий и оверлапов

**`createPortalsFallback()` (строки 1153-1212)**
- Fallback создание при ошибке

**`updateEnemyInstances()` (строки 1642-1725)**
- Обновление всех врагов
- Обработка клонирования

**`createEnemyClone()` (строки 1726-1861)**
- Создание клона врага

#### 6. Порталы и викторины

**`handlePortalEntry()` (строки 3340-3375)**
- Обработка входа в портал
- Проверка mustExit

**`handlePortalEnterConfirmed()` (строки 792-831)**
- Подтверждение входа в портал

**`handlePortalEnterCancelled()` (строки 832-850)**
- Отмена входа в портал

**`handleQuizCompleted()` (строки 3376-3961)**
- Обработка завершения викторины портала

**`showFallbackGlobalQuestion()` (строки 3962-4028)**
- Показ запасного глобального вопроса

#### 7. Ключи и викторины

**`handleKeyQuizCorrect()` (строки 3010-3048)**
- Правильный ответ на ключе

**`handleKeyQuizWrong()` (строки 3049-3093)**
- Неправильный ответ на ключе

**`handleKeyQuizClose()` (строки 3094-3108)**
- Закрытие модалки ключа

#### 8. Камера

**`setupCameraBounds()` (строки 1305-1342)**
- Настройка границ камеры

**`setupCameraFollow()` (строки 1343-1389)**
- Настройка следования камеры за игроком

**`handleResize()` (строки 1458-1861)**
- Обработка изменения размера окна

#### 9. HUD и UI

**`createHUD()` (строки 4121-4164)**
- Создание Heads-Up Display

**`updateHUD()` (строки 4184-4222)**
- Обновление HUD каждый кадр

**`getZoomCompensatedHUDPosition()` (строки 4165-4183)**
- Расчёт позиции с компенсацией зума

#### 10. Game Over

**`handleGameOver()` (строки 4223-4515)**
- Обработка окончания игры (win/lose)

**`handleGameWin()` (строки 4516-4525)**
- Обработка победы

**`handleFullGameRestart()` (строки 4526+)**
- Полный перезапуск игры

#### 11. EventBus и обработчики событий

**`setupEventBus()` (строки 1905-2031)**
- Подписка на все события EventBus

**`setupEventListeners()` (строки 680-695)**
- DOM события (orientationchange, resize)

**Обработчики событий:**
- `onPortalEnterCancelledHandler` (строка 1860)
- `onPortalEnterConfirmedHandler` (строка 1865)
- `onKeyQuizCompletedHandler` (строка 1870)
- `onRestartGameHandler` (строка 1881)
- `onNextLevelHandler` (строка 1887)
- `onQuizHandler` (строка 1892)
- `onViewportHandler` (строка 1896)

#### 12. Утилиты

**`showFloatingText()` (строки 3776-3809)**
- Показ всплывающего текста

**`setupOracleClickHandler()` (строки 4029-4057)**
- Обработка кликов по оракулу

**`setupPortalClickHandlers()` (строки 4058-4120)**
- Обработка кликов по порталам

### Поиск функционала в MainScene

| Что нужно найти | Метод/секция | Строки |
|-----------------|--------------|--------|
| Инициализация | `create()` | 212-679 |
| Игровой цикл | `update()` | 2032-2762 |
| Создание игрока | `createPlayer()` | 1278-1304 |
| Создание порталов | `createPortals()` | 1000-1152 |
| Коллизия с врагом | `handlePlayerEnemyCollision()` | 2764-2839 |
| Коллизия с сердечком | `handlePlayerHeartCollision()` | 2847-3010 |
| Коллизия с оракулом | `handlePlayerOracleCollision()` | 3109-3191 |
| Вход в портал | `handlePortalEntry()` | 3340-3375 |
| Викторина портала | `handleQuizCompleted()` | 3376-3961 |
| Правильный ответ ключа | `handleKeyQuizCorrect()` | 3010-3048 |
| Неправильный ответ ключа | `handleKeyQuizWrong()` | 3049-3093 |
| Game Over | `handleGameOver()` | 4223-4515 |
| Создание HUD | `createHUD()` | 4121-4164 |
| Настройка камеры | `setupCameraBounds()`, `setupCameraFollow()` | 1305-1389 |
| EventBus | `setupEventBus()` | 1905-2031 |
| Tiled Map режим | `createGameWorldTiled()` | 851-906 |
| Random режим | `createGameWorldRandom()` | 907-963 |

---

## Структура документации

Вся документация проекта организована в три основные папки:

### `documentation/main/` - Основная документация

Содержит актуальную документацию проекта:

- **`ProjectMap.md`** - Карта проекта (главный документ)
  - Полное описание структуры проекта
  - Детальное описание всех компонентов
  - Конфигурационные файлы и типы
  - Потоки данных и ключевые методы

- **`ARCHITECTURE.md`** - Подробное описание архитектуры
  - Обзор архитектуры с принципами
  - Слои архитектуры (диаграмма)
  - Core модули, Entities, Systems, Scenes, UI
  - Интеграция React-Phaser через EventBus
  - Паттерны проектирования
  - Потоки данных

- **`BUILD.md`** - Инструкции по сборке и запуску
  - Требования и установка зависимостей
  - Режим разработки и сборка проекта
  - Переменные окружения
  - Типичные проблемы и решения

- **`DEVELOPMENT.md`** - Руководство по разработке
  - Настройка окружения разработки
  - Структура проекта
  - Взаимодействие React-Phaser
  - Работа с EventBus
  - Добавление новых компонентов
  - Отладка и лучшие практики

- **`DocumentationMaintenance.md`** - Поддержание документации
  - Регулярные ревизии
  - Обновление документации
  - Синхронизация между папками
  - Удаление устаревшей информации
  - Чек-листы и метрики качества

- **`TESTING.md`** - Руководство по тестированию
  - Виды тестов (unit, integration, e2e)
  - Инструменты и технологии (Jest, Testing Library, Cypress)
  - Структура тестов
  - Инструкции по запуску
  - Написание тестов
  - Покрытие кода
  - Отладка тестов
  - CI/CD интеграция
  - Рекомендации по поддержанию

- **`GameDescription.md`** - Идеологическое описание игры
  - Основная концепция игры
  - Игровые механики
  - Описание геймплея

- **`PrototypeAnalysis.md`** - Анализ прототипа проекта
  - Архитектура прототипа
  - Ключевые компоненты
  - Готовность к миграции

- **`ModularArchitecturePlan.md`** - План модульной архитектуры
  - Структура папок
  - Ключевые компоненты для создания
  - Порядок реализации
  - Статус выполнения

- **`ModularArchitectureProgress.md`** - Прогресс реализации модульной архитектуры
  - Выполненные этапы
  - Статистика реализации
  - Следующие шаги

- **`LOGGING_SYSTEM.md`** - Система логирования
  - 345+ рефакторенных `console.log` → `logger.log()`
  - 50+ категорий для тонкой настройки
  - Управление через `.env` флаги
  - Logger.ts, debugConfig.ts, BrowserLogger.ts

### `documentation/export_llm/` - Документация для LLM

Содержит упрощенную документацию, специально подготовленную для работы с Large Language Models:

- **`PROJECT_MAP.md`** - Упрощенная карта проекта
  - Быстрый поиск компонентов
  - Ключевые конфигурации
  - Важные детали реализации

- **`ARCHITECTURE.md`** - Упрощенная структурированная версия архитектуры
  - Core модули (таблица)
  - Entities, Systems, Scenes, UI (структурированные списки)
  - Интеграция React-Phaser (EventBus события)
  - Конфигурация (JSON примеры)
  - Быстрые ссылки

- **`QUICK_START.md`** - Быстрый старт для LLM
  - Команды установки и запуска
  - Структура проекта (кратко)
  - Ключевые файлы
  - Основные команды (таблица)

- **`TESTING_SUMMARY.md`** - Краткая информация о тестировании
  - Виды тестов (таблица)
  - Структура тестов
  - Команды запуска
  - Инструменты
  - Покрытие кода

- **`README.md`** - Описание папки
  - Назначение документации для LLM
  - Принципы форматирования
  - Правила использования
  - Синхронизация с основной документацией

- **`LOGGING_SYSTEM.md`** - Краткое описание системы логирования
  - Быстрый reference для использования logger
  - Таблица категорий и .env флагов
  - Пресеты для быстрой настройки

### `documentation/temp_docs/` - Временные файлы-отчеты

Содержит временные документы, отчеты о выполненных работах и промежуточные документы:

- **`DocumentationPlan.md`** - План документации проекта
  - Структура документации
  - Разделы документации
  - Шаблоны документов
  - Рекомендации по поддержанию

- **`DocumentationCreationPlan.md`** - План создания документов
  - Приоритеты документов (1, 2, 3)
  - Статус создания каждого документа
  - Статистика прогресса
  - Следующие шаги

- **`DocumentationCreationReport.md`** - Отчет о создании документации
  - Описание всех созданных файлов
  - Статистика создания
  - Особенности реализации
  - Результаты и рекомендации

- **`DocumentationReorganizationReport.md`** - Отчет о реорганизации документации
  - Выполненные изменения
  - Статистика изменений
  - Результаты и рекомендации

- **`DocumentationStructureReport.md`** - Отчет о созданной структуре
  - Выполненные задачи
  - Статистика
  - Особенности реализации

### `documentation/Plans/` - Планы работы и отчёты агентов

Содержит долговременные планы работы агентов и отчёты о выполнении шагов по этим планам:

**Назначение:**
- Планы работы над сложными задачами (рефакторинг, новые фичи, архитектурные изменения)
- Отчёты о прогрессе выполнения планов
- Позволяют новому агенту продолжить работу после перезапуска/новой сессии

**Стандарты именования:**
- **План:** `YYYY-MM-DD_feature-or-task-name_PLAN.md`
- **Отчёт:** `YYYY-MM-DD_feature-or-task-name_PROGRESS.md`

**Структура файла плана:**
- Заголовок с кратким описанием задачи
- Контекст: какие файлы/модули участвуют
- Нумерованный список шагов (каждый шаг максимально атомарный)
- Зависимости и примечания (опционально)

**Структура файла отчёта:**
- Ссылка на соответствующий файл плана
- Текущий статус (краткое резюме)
- Для каждого шага из плана:
  - Статус (TODO / IN PROGRESS / DONE / SKIPPED)
  - Краткое описание изменений
  - Список затронутых файлов
- Примечания и проблемы (опционально)

**Правила работы агента:**
- Перед началом сложной задачи агент создаёт или обновляет файл плана и согласует его с пользователем
- При выполнении работы агент обновляет файл отчёта после каждого логически завершённого шага
- При продолжении работы после перезапуска агент читает соответствующие файлы плана и прогресса и кратко резюмирует текущее состояние

**Важно:**
- Папка `documentation/Plans/` всегда доступна для чтения и записи
- Завершённые планы и отчёты не удаляются - они служат историей работы
- Подробные правила работы с планами и отчётами описаны в `.cursor/rules/02-docs-rules.mdc`

### `documentation/templates/` - Шаблоны документов

Содержит шаблоны для создания документов проекта:

- **`TEMPLATE_MAIN.md`** - Шаблон для основной документации
  - Полная структура с оглавлением
  - Примеры кода и конфигураций
  - Связанные документы
  - История изменений

- **`TEMPLATE_EXPORT_LLM.md`** - Шаблон для LLM документации
  - Упрощенная структура
  - Структурированные данные
  - Быстрые ссылки

- **`TEMPLATE_TEMP_DOCS.md`** - Шаблон для временных документов
  - Структура отчета
  - Статистика
  - Результаты и следующие шаги

- **`README.md`** - Описание папки
  - Назначение шаблонов
  - Правила использования

### `documentation/temp_docs/` - Временные файлы-отчеты

Содержит временные документы, отчеты о выполненных работах и промежуточные документы:

- **`DocumentationPlan.md`** - План документации проекта
  - Структура документации
  - Разделы документации
  - Шаблоны документов
  - Рекомендации по поддержанию

- **`EnemyBehaviorModels.md`** - Модели поведения врагов
  - Таблица моделей поведения по типам врагов
  - Детальное описание поведения каждого типа

- **`LevelConfigStructure.md`** - Структура конфигурации уровня
  - Описание блоков настроек level1.config.json

- **`SoundVolumeCheck.md`** - Проверка соответствия звуков и громкостей

- **`StateMachinesReview.md`** - Ревью машин состояний в проекте

- **`StateMachinesOverview.md`** - Обзор всех машин состояний

- **`FutureCompatibilityRecommendations.md`** - Рекомендации по будущей совместимости

- **`TEST_LOGS_README.md`** - Описание логов тестов

- **`README.md`** - Описание папки
  - Назначение временных документов
  - Типы документов
  - Правила работы с папкой
  - Рекомендации

**Примечание:** Также содержит логи тестов (*.log), которые перезаписываются при каждом запуске тестов.

### Правила работы с документацией

1. **Основная документация** (`main/`) - для постоянного хранения актуальной информации
2. **Документация для LLM** (`export_llm/`) - для быстрого поиска компонентов и работы с AI
3. **Шаблоны документов** (`templates/`) - постоянные инструменты для создания документов
4. **Временные документы** (`temp_docs/`) - для отчетов и промежуточных документов, которые могут быть удалены после интеграции информации

---

## Структура тестов

Проект использует комплексную систему тестирования с тремя уровнями:

### `src/tests/unit/` - Unit тесты

Тестирование отдельных модулей в изоляции:

- **`core/`** - Core системы
  - `GameState.test.ts` - управление состоянием игры
  - `AssetLoader.test.ts` - загрузка ресурсов (изображения, аудио, JSON)
  - `LevelManager.test.ts` - управление уровнями и конфигами

- **`systems/`** - Игровые системы
  - `HealthSystem.test.ts` - система здоровья (урон, лечение, отображение)
  - `ScoreSystem.test.ts` - система очков (ключи, порталы, форматирование)
  - `QuizManager.test.ts` - управление викторинами (загрузка, парсинг, случайный выбор)
  - `SpawnSystem.test.ts` - система спавна (враги, предметы, безопасные позиции)
  - `SpawnMatrix.test.ts` - матричная система безопасного спавна (37 тестов: конвертация координат, занятость ячеек, поиск позиций, логика перезаписи)
  - `CollisionSystem.test.ts` - система коллизий (настройка, обработчики)
  - `AudioManager.test.ts` - управление звуком (загрузка, воспроизведение, громкость)

- **`entities/`** - Игровые сущности
  - `Player.test.ts` - игрок
  - `AbstractEnemy.test.ts` - базовый класс врагов
  - `EnemyRandomWalker.test.ts` - случайное блуждание
  - `EnemyChaser.test.ts` - преследование
  - `EnemyFlam.test.ts` - универсальный враг с настраиваемым поведением
  - `AbstractPortal.test.ts` - базовый класс порталов
  - `StandardPortal.test.ts` - стандартный портал

- **`react/`** - React компоненты
  - `UIOverlay.test.tsx` - HUD компонент
  - `QuizModal.test.tsx` - модальное окно викторины
  - `GameOverModal.test.tsx` - экран Game Over

### `src/tests/integration/` - Integration тесты

Тестирование взаимодействия компонентов:

- **`phaser-react/`** - Взаимодействие Phaser-React
  - `EventBus.test.ts` - коммуникация через EventBus
  - Тесты интеграции систем

### `src/tests/e2e/` - E2E тесты

Тестирование полных пользовательских сценариев:

- **`cypress/`** - Cypress тесты
  - `e2e/gameplay.cy.ts` - основные сценарии геймплея
  - `support/` - поддержка и кастомные команды

### Инструменты тестирования

- **Jest** - unit и integration тесты
- **Testing Library** - тестирование React компонентов
- **Cypress** - E2E тесты в браузере

### Команды запуска

- `npm test` - все тесты
- `npm run test:unit` - unit тесты
- `npm run test:integration` - integration тесты
- `npm run test:e2e` - E2E тесты
- `npm run test:coverage` - с покрытием кода

**Подробная документация:** `documentation/main/TESTING.md`

---

## Правила для LLM

**Важно:** При работе с проектом всегда ориентируйтесь по этой карте (`documentation/main/ProjectMap.md`) для понимания структуры и назначения файлов.

**Структура документации:**
- Основная документация: `documentation/main/`
- Документация для LLM: `documentation/export_llm/`
- Временные документы: `documentation/temp_docs/`

**Быстрый поиск:**
- Нужен класс/система? → Смотрите раздел [Быстрый поиск](#быстрый-поиск)
- Нужен конфиг? → Смотрите раздел [Конфигурационные файлы](#конфигурационные-файлы)
- Нужен метод? → Смотрите раздел [Детальное описание компонентов](#детальное-описание-компонентов)
- Нужен тип? → Смотрите раздел [Типы и интерфейсы](#типы-и-интерфейсы)

---

**Последнее обновление:** 2025-01-27 (v3.14)

**Изменения в v3.7:**
- ✅ Унифицирована логика расчета размеров шрифтов для PortalModal и GameOverModal
- ✅ Все элементы (включая кнопки) используют единый `baseFontSize` в обоих модальных окнах
- ✅ Все элементы включаются в расчет `unifiedFontSize` (минимум из всех)
- ✅ Применяется единый множитель `1.5` ко всем элементам после расчета `unifiedFontSize`
- ✅ Использование `calculateOptimalBaseFontSize()` для поиска оптимального базового размера
- ✅ Гарантирована идентичность размеров шрифтов в кнопках порталов и окнах конца игры
- ✅ Обновлена документация FONT_SIZING_SYSTEM.md с описанием новой унифицированной логики

**Изменения в v3.14 (2025-01-27):**
- ✅ Обновлена документация: исправлены несоответствия с текущей реализацией
- ✅ Заменено упоминание `EnemyImmortal` на `EnemyFlam` (актуальный класс врага)
- ✅ Заменено упоминание `RuneQuestionModal` на `KeyQuestionModal` (актуальное название)
- ✅ Удалены устаревшие логи тестов из корня проекта и temp_docs
- ✅ Обновлены даты в документации

**Изменения в v3.4:**
- ✅ Обновлены depth значения: портал `5` (было `8`), игрок `150` (было `10`), сердечки над головой `160`
- ✅ Реорганизован Z-порядок: фон(-200/-100) → трава(1) → тексты(2) → кусты(3) → порталы(4) → предметы(5) → персонаж(6) → враги(7) → оракул(8)
- ✅ Обновлены текстуры: портал использует `Portal_64x48.png`, сердечки `Heart5x5.png` с масштабом 4x
- ✅ Добавлен звук `PORTAL_ACTIVATED_COMPLETE` для завершения активации портала
- ✅ Обновлены ограничения на звук `damage` - только при столкновении с врагами на карте
- ✅ Обновлен размер `PortalModal` - совпадает с `KeyQuestionModal` (95% x 71.25%)
- ✅ Добавлена задержка 1 секунда перед показом `GameOverModal`
- ✅ Добавлено описание `src/vite-env.d.ts` для типов Vite environment variables

**Изменения в v3.3:**
- ✅ Добавлен AnimationManager — универсальная система загрузки спрайтшитов и создания анимаций
- ✅ Добавлен SpriteAnimationHandler — обработчик анимаций для physics спрайтов
- ✅ Обновлен AudioManager — все звуки интегрированы, константы громкости в gameConstants.ts
- ✅ Обновлен LoadingScene — использует универсальную систему, удален хардкод
- ✅ Обновлен SpawnSystem — использует key_sheet для ключей вместо Dice 20.png
- ✅ Добавлена информация о spritesheetConfigs.ts — конфигурация всех спрайтшитов
- ✅ Добавлена информация о ручной синхронизации кадров для physics спрайтов
- ✅ Обновлены типы — добавлен animationTypes.ts

**Изменения в v3.2:**
- ✅ Добавлена информация о новых документах в структуру документации
- ✅ Обновлен раздел "Структура документации" с описанием всех новых документов
- ✅ Добавлены описания: ARCHITECTURE.md, BUILD.md, DEVELOPMENT.md, DocumentationMaintenance.md
- ✅ Добавлены описания документов для LLM: ARCHITECTURE.md, QUICK_START.md
- ✅ Добавлена папка `templates/` для шаблонов документов
- ✅ Обновлены описания документов в temp_docs

**Изменения в v3.1:**
- ✅ Добавлен раздел "Структура документации" с описанием всех папок документации
- ✅ Обновлена структура файлов с актуальной организацией документации
- ✅ Добавлена информация о папке `temp_docs/` для временных документов

**Изменения в v3.0:**
- ✅ Полная переработка карты проекта
- ✅ Добавлен раздел "Быстрый поиск" для оперативного нахождения компонентов
- ✅ Детальное описание всех классов, методов и свойств
- ✅ Полное описание конфигурационных файлов и их структуры
- ✅ Описание всех типов и интерфейсов
- ✅ Добавлена информация об управлении уроном и depth
- ✅ Добавлена информация об управлении спавном объектов
