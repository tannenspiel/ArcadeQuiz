# Debugging Guide - Руководство по отладке

**Версия:** 2.0
**Дата создания:** 2026-01-28
**Назначение:** Комплексное руководство по инструментам отладки и диагностике
**Затронутые файлы:**
- `src/game/ui/DebugOverlay.ts`
- `src/config/debugConfig.ts`
- `.env`

---

## Часть 1: DebugOverlay

**Файл:** `src/game/ui/DebugOverlay.ts`

Мощный визуальный инструмент отладки для мониторинга состояния игры и визуализации матрицы спавна.

### Обзор

`DebugOverlay` — отдельный класс для управления отладочным UI. Вынесен из `MainScene` для улучшения модульности.

**Преимущества:**
- Модульность — изолирован от основной логики
- Переиспользование — можно использовать в других сценах
- Тестируемость — легче тестировать отдельно
- Читаемость — `MainScene` стал проще

### Архитектура

```typescript
export class DebugOverlay {
    private debugText!: Phaser.GameObjects.Text;
    private debugUpdateCounter: number = 0;
    private spawnMatrixGrid!: Phaser.GameObjects.Graphics;

    constructor(
        private scene: Phaser.Scene,
        private deps: DebugOverlayDependencies
    ) {}
}
```

### Интерфейс зависимостей

```typescript
interface DebugOverlayDependencies {
    getPlayer: () => Player | null;
    getGameState: () => GameState;
    getSpawnSystem: () => SpawnSystem | null;
    getEnemyInstances: () => AbstractEnemy[];
    getMaxKeys: () => number;
    getHeartsGroup: () => Phaser.Physics.Arcade.Group | null;
    getKeysGroup: () => Phaser.Physics.Arcade.Group | null;
    getScore: () => number;
    getMaxPossibleScore: () => number;
    getTotalMaxPossibleScore: () => number;
}
```

**Dependency Injection** позволяет:
- Легко тестировать с моками
- Избежать жёсткой связанности с `MainScene`
- Переиспользовать в других сценах

---

## Часть 2: Конфигурация отладки

### Три независимых флага

Отладочный UI управляется через три независимые переменные окружения:

| Флаг | Назначение | `.env` переменная |
|------|------------|-------------------|
| `DEBUG_OVERLAY_ENABLED` | Текстовый оверлей (FPS, позиция, счёт, враги) | `VITE_DEBUG_OVERLAY_ENABLED` |
| `DEBUG_VISUAL_GRID_ENABLED` | Визуальная сетка на карте (ячейки, занятость) | `VITE_DEBUG_VISUAL_GRID_ENABLED` |
| `DEBUG_SPAWN_GRID_ENABLED` | Детальные логи сетки в консоли | `VITE_DEBUG_SPAWN_GRID_ENABLED` |

### DEBUG_OVERLAY_ENABLED — Текстовый оверлей

**Что включает:**
- FPS (кадры в секунду)
- Позиция игрока (X, Y)
- Позиция камеры (scrollX, scrollY)
- Счёт и максимальный возможный счёт (уровень/общий)
- Ключи (собрано/максимум, на карте)
- Сердечки на карте
- Статистика врагов по типам

**Включение:**
```env
VITE_DEBUG_OVERLAY_ENABLED=true
```

**Пример вывода:**
```
FPS: 60.0
Player: 1024x512
Camera: 512x256
Score: 150
Max Possible (Level): 500
Max Possible (Total): 2000
Keys Collected: 2/3
Keys on Map: 1
Hearts on Map: 3
Enemies: 5
  Beast: 2 (HP: 4)
  Dragon: 2 (HP: 2)
  Flam: 1 (HP: 4)
```

### DEBUG_VISUAL_GRID_ENABLED — Визуальная сетка

**Что включает:**
- Визуализация ячеек матрицы спавна (64×64 пикселей)
- Цветовая индикация занятости
- Линии сетки и границы карты

**Включение:**
```env
VITE_DEBUG_VISUAL_GRID_ENABLED=true
```

**Цветовая схема:**

| Тип занятости | Цвет | Alpha | Описание |
|---------------|------|-------|----------|
| `free` | — | — | Свободная ячейка (не отображается) |
| `permanent` | Красный (`0xff0000`) | 0.5 | Порталы, оракул, кусты |
| `item` | Желтый (`0xffff00`) | 0.3 | Ключи, сердечки |
| `enemy` | Синий (`0x0000ff`) | 0.3 | Враги |

### DEBUG_SPAWN_GRID_ENABLED — Логи сетки

**Что включает:**
- Детальные логи в консоли (координаты permanent cells)
- Информация о каждом обновлении сетки

**Включение:**
```env
VITE_DEBUG_SPAWN_GRID_ENABLED=true
```

**Осторожно:** Много спама при каждом обновлении сетки!

---

## Часть 3: Использование в MainScene

### Инициализация

```typescript
// В MainScene.create() после настройки камеры
if (DEBUG_OVERLAY_ENABLED) {
    this.debugOverlay = new DebugOverlay(this, {
        getPlayer: () => this.player,
        getGameState: () => this.gameState,
        getSpawnSystem: () => this.spawnSystem,
        getEnemyInstances: () => this.enemyInstances,
        getMaxKeys: () => this.MAX_KEYS,
        getHeartsGroup: () => this.hearts,
        getKeysGroup: () => this.keys,
        getScore: () => this.scoreSystem.getScore(),
        getMaxPossibleScore: () => this.scoreSystem.getMaxPossibleScore(),
        getTotalMaxPossibleScore: () => this.scoreSystem.getTotalMaxPossibleScore()
    });
    this.debugOverlay.create();
    this.debugOverlay.createSpawnMatrixGrid();
}
```

### Обновление

```typescript
// В MainScene.update()
if (DEBUG_OVERLAY_ENABLED && this.debugOverlay) {
    this.debugOverlay.update();
}
```

### Уничтожение

```typescript
// В MainScene.shutdown()
if (this.debugOverlay) {
    try {
        this.debugOverlay.destroy();
    } catch (e) {
        console.warn('⚠️ Error destroying debugOverlay:', e);
    }
}
```

---

## Часть 4: Другие инструменты отладки

### Tiled Map Debugging

**1. Визуализация коллизий:**

В `MainScene` включите отрисовку физических тел:
```typescript
physics: {
    default: 'arcade',
    arcade: {
        debug: true // Включить для просмотра границ тел
    }
}
```

**2. Overlap Mask (GID 12):**

Используйте `DebugOverlay` для проверки "постоянных" объектов. Зоны входа в порталы должны быть помечены как занятые (красные в матрице).

### VS Code Debugging

Для отладки кода (брейкпоинты):

1. Установите расширение "Debugger for Chrome/Firefox"
2. Запустите конфигурацию "Debug Client" (если настроена в `.vscode/launch.json`)

---

## Часть 5: Логирование

### Префиксы логов

В проекте используются `console.log` с префиксами:

| Префикс | Назначение | Пример |
|---------|------------|--------|
| `Setup:` | Инициализация систем | `Setup: LoadingScene create()` |
| `Spawn:` | События спавна | `Spawn: Enemy spawned at 256x128` |
| `Tiled:` | Загрузка карты | `Tiled: Map loaded successfully` |
| `Debug:` | Отладочная информация | `Debug: Spawn matrix updated` |

---

## Часть 6: Типичные проблемы и решения

### Проблемы с коллизиями в Tiled Map

**Симптомы:** Игрок проходит сквозь стены или не может войти в портал.

**Диагностика:**
1. Включите `physics.arcade.debug: true`
2. Проверьте, что тайлы стен находятся в слое "Collision Mask"
3. Для порталов: убедитесь, что физическое тело "сенсора" достаточно расширено

**Решение:**
```typescript
// Проверьте COLLISION_CONFIG.TILED_SENSOR_EXPANSION в gameConstants.ts
```

### Проблемы спавна

**Симптомы:** Враги или предметы не появляются.

**Диагностика:**
1. Включите `VITE_DEBUG_OVERLAY_ENABLED=true`
2. Посмотрите на счётчики "Failed Spawns" в оверлее
3. Красные зоны в матрице показывают занятые места

**Решение:**
- Проверьте матрицу спавна на свободные ячейки
- Увеличьте размер карты или уменьшите количество объектов

### Модальное окно не центрируется

**Симптомы:** Модальное окно смещено от центра.

**Диагностика:**
1. Проверьте использование Grid Snapping
2. Убедитесь, что размеры кратны 8, координаты — 4

**Решение:**
```typescript
const modalWidth = snapToGridDouble(desiredWidth);
const modalHeight = snapToGridDouble(desiredHeight);
```

### Текст размыт

**Симптомы:** Текст в модальных окнах или UI размыт.

**Диагностика:**
1. Проверьте компенсацию зума
2. Проверьте `setResolution()`

**Решение:**
```typescript
text.setScale(1 / camera.zoom);
text.setResolution(2); // Или выше
```

---

## Часть 7: Производительность

### Мониторинг FPS

Используйте `DEBUG_OVERLAY_ENABLED` для мониторинга FPS в реальном времени.

**Норма:** 60 FPS на десктопе, 30+ FPS на мобильных.

**Если FPS ниже:**
1. Проверьте количество врагов
2. Отключите визуальную сетку (`DEBUG_VISUAL_GRID_ENABLED=false`)
3. Проверьте утечки памяти (неуничтоженные tweens, sprites)

### Оптимизация

1. **Останавливайте tweens** при уничтожении объектов
2. **Уничтожайте Graphics** при закрытии модалок
3. **Используйте object pooling** для часто создаваемых элементов
4. **Лимитируйте логи** в продакшене

---

## Часть 8: Полная конфигурация .env

### Рекомендуемая конфигурация для разработки

```env
# 👁️ Визуальная отладка (Debug Overlay):
VITE_DEBUG_OVERLAY_ENABLED=true           # Текстовый оверлей
# VITE_DEBUG_VISUAL_GRID_ENABLED=true     # Визуальная сетка (по необходимости)
# VITE_DEBUG_SPAWN_GRID_ENABLED=true      # Логи сетки (только при отладке спавна)

# 🔍 Детальные логи (осторожно - много спама):
# VITE_ARCADE_LOG_SPAWN=true              # Логи спавна врагов
# VITE_ARCADE_LOG_PLAYER=true             # Логи игрока
# VITE_ARCADE_LOG_ENEMY_AI=true           # Логи AI врагов
```

### Продакшн-конфигурация

```env
# Все отладочные флаги выключены
VITE_DEBUG_OVERLAY_ENABLED=false
VITE_DEBUG_VISUAL_GRID_ENABLED=false
VITE_DEBUG_SPAWN_GRID_ENABLED=false
VITE_ARCADE_LOG_SPAWN=false
VITE_ARCADE_LOG_PLAYER=false
VITE_ARCADE_LOG_ENEMY_AI=false
```

---

## Связанные документы

- **SPAWN_MATRIX_SYSTEM.md** — Система матрицы спавна
- **UI_GUIDE.md** — Руководство по UI компонентам
- **MODAL_GUIDE.md** — Руководство по модальным окнам

---

## История изменений

| Дата | Версия | Изменение |
|------|--------|-----------|
| 2026-01-28 | 2.0 | Слияние DEBUGGING.md и DEBUG_OVERLAY.md |
| 2026-01-20 | 1.1 | Разделены флаги управления отладочным UI |
| 2025-12-08 | 1.0 | Создание базовой документации |
