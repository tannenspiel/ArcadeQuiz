# Архитектура проекта ArcadeQuiz

**Версия:** 1.9  
**Дата создания:** 2025-01-26  
**Дата последнего обновления:** 2025-01-27

**Изменения в v1.8:**
- ✅ Добавлена поддержка Tiled Map для создания уровней (A/B тестирование)
- ✅ Добавлена ссылка на документацию по Tiled Map реализации (TILED_MAP_IMPLEMENTATION.md)
- ✅ Обновлена информация о MainScene с описанием двух режимов создания мира

**Изменения в v1.7:**
- ✅ Добавлена ссылка на документацию по матричной системе позиционирования (SPAWN_MATRIX_SYSTEM.md)
- ✅ Обновлена информация о SpawnSystem с описанием матричной системы  
**Статус:** Актуален

---

## Краткое описание

Подробное описание архитектуры проекта ArcadeQuiz, включая модульную структуру, взаимодействие компонентов, паттерны проектирования и принципы организации кода.

---

## Содержание

1. [Обзор архитектуры](#обзор-архитектуры)
2. [Слои архитектуры](#слои-архитектуры)
3. [Система масштабирования](#система-масштабирования)
4. [Core модули](#core-модули)
5. [Entities (Сущности)](#entities-сущности)
6. [Systems (Системы)](#systems-системы)
7. [Scenes (Сцены)](#scenes-сцены)
8. [UI компоненты](#ui-компоненты)
9. [Интеграция React-Phaser](#интеграция-react-phaser)
10. [Паттерны проектирования](#паттерны-проектирования)
11. [Потоки данных](#потоки-данных)
12. [Tiled Map реализация](#tiled-map-реализация)

---

## Обзор архитектуры

ArcadeQuiz построен на модульной архитектуре с четким разделением ответственности. Проект использует комбинацию Phaser 3 для игровой логики и React для UI, связанных через систему событий EventBus.

### Основные принципы:

1. **Разделение ответственности** — каждый модуль отвечает за свою область
2. **Модульность** — компоненты изолированы и переиспользуемы
3. **Мульти-тема** — поддержка нескольких игр через `CURRENT_THEME`
4. **Расширяемость** — легко добавлять новые типы врагов/порталов
5. **Типизация** — полная типизация TypeScript

---

## Слои архитектуры

```
┌─────────────────────────────────────┐
│         React UI Layer              │
│  (UIOverlay, Modals, Components)    │
└──────────────┬──────────────────────┘
               │ EventBus
┌──────────────▼──────────────────────┐
│      Phaser Game Layer               │
│  ┌──────────────────────────────┐   │
│  │      Scenes                  │   │
│  │  (BaseScene, MainScene)      │   │
│  └──────────┬───────────────────┘   │
│             │                        │
│  ┌──────────▼───────────────────┐   │
│  │      Systems                 │   │
│  │  (Health, Score, Quiz, etc)  │   │
│  └──────────┬───────────────────┘   │
│             │                        │
│  ┌──────────▼───────────────────┐   │
│  │      Entities                 │   │
│  │  (Player, Enemies, Portals)   │   │
│  └──────────┬───────────────────┘   │
│             │                        │
│  ┌──────────▼───────────────────┐   │
│  │      Core                    │   │
│  │  (AssetLoader, GameState)    │   │
│  └──────────────────────────────┘   │
└──────────────────────────────────────┘
```

---

## Система масштабирования

### Обзор

Проект использует **двухуровневую систему масштабирования** для адаптации под различные устройства и разрешения экранов.

### Уровни масштабирования

1. **Виртуальное разрешение экрана (720×1280)**
   - Фиксированное разрешение для UI элементов
   - Масштабируется на реальный экран через `Phaser.Scale.FIT`
   - Автоматически создает letterboxing при несоответствии соотношения сторон

2. **Базовое масштабирование игрового мира (BASE_SCALE = 4.0)**
   - Карта: 512×512 базовых → 2048×2048 виртуальных пикселей
   - Акторы: масштабируются через `BASE_SCALE × ACTOR_SIZES[тип]`

### Координатные системы

- **Виртуальные координаты UI:** 0-720 по X, 0-1280 по Y (для позиционирования UI)
- **Виртуальные координаты игрового мира:** 0-2048 по X, 0-2048 по Y (для игровых объектов)

### Ключевые файлы

- `src/react/PhaserGame.tsx` - инициализация масштабирования виртуального экрана
- `src/constants/gameConstants.ts` - константы масштабирования (GAME_WIDTH, GAME_HEIGHT, BASE_SCALE, MAP_WIDTH, MAP_HEIGHT)
- `src/game/scenes/BaseScene.ts` - базовые настройки камеры и физики
- `src/game/scenes/MainScene.ts` - создание карты и настройка камеры

### Камера

- **Размер камеры:** 720×1280 (виртуальное разрешение экрана)
- **Границы камеры:** 0-2048 (виртуальные координаты игрового мира)
- **Следование:** за игроком с плавностью (lerp factor 0.15)
- **Zoom:** 1.0 (не используется для масштабирования)

### Подробная документация

Полное описание системы масштабирования, включая поведение камеры, карты и letterboxing, доступно в:
- `documentation/export_llm/SCALING_SYSTEM.md` - детальное описание для LLM анализа

---

## Core модули

### AssetLoader

**Путь:** `src/game/core/AssetLoader.ts`

**Назначение:** Централизованная загрузка ресурсов по текущей теме игры.

**Ключевые методы:**
- `loadImage(key, path)` — загрузка изображений
- `loadAudio(key, path)` — загрузка аудио
- `loadJSON(key, path)` — загрузка JSON файлов
- `loadLevelQuestions(levelNumber)` — загрузка вопросов уровня

**Особенности:**
- Использует `CURRENT_THEME` из `gameConfig.ts`
- Кеширует загруженные ресурсы
- Поддерживает fallback для отсутствующих ресурсов

**Пример использования:**
```typescript
const assetLoader = new AssetLoader(scene);
await assetLoader.loadImage('heart', '/images/Heart5x5.png');
const heartTexture = scene.textures.get('heart');
```

### LevelManager

**Путь:** `src/game/core/LevelManager.ts`

**Назначение:** Управление уровнями игры, загрузка конфигураций и управление сложностью.

**Ключевые методы:**
- `loadLevelConfig(levelNumber)` — загрузка конфига уровня
- `getCurrentLevelConfig()` — получение текущего конфига
- `getEnemySpawnConfig()` — конфиг спавна врагов
- `getEnemySpeedConfig()` — конфиг скорости врагов
- `getItemSpawnConfig()` — конфиг спавна предметов
- `nextLevel()` — переход на следующий уровень
- `getDifficultyMultiplier()` — множитель сложности

**Особенности:**
- Загружает конфиги из JSON файлов
- Поддерживает fallback на дефолтные значения
- Управляет прогрессией сложности

**Пример конфигурации:**
```json
{
  "levelNumber": 1,
  "enemySpawn": {
    "initialCount": 8,
    "periodicSpawnDelay": 6000,
    "maxEnemies": 15,
    "initialCount": {
      "randomWalker": 4,
      "chaser": 2,
      "flam": 2
    }
  }
}
```

### GameState

**Путь:** `src/game/core/GameState.ts`

**Назначение:** Глобальное состояние игры, доступное из любой части приложения.

**Ключевые методы:**
- `getHealth()`, `setHealth(value)` — управление здоровьем
- `getKeys()`, `addKey()`, `removeKey()` — управление ключами
- `getScore()`, `setScore(value)` — управление очками
- `getLevel()`, `setLevel(value)` — управление уровнем
- `isAltarActivated()`, `setAltarActivated(value)` — состояние алтаря
- `reset()` — сброс состояния

**Особенности:**
- Singleton паттерн
- Централизованное управление состоянием
- Синхронизация между Phaser и React

---

## Entities (Сущности)

### Player

**Путь:** `src/game/entities/Player.ts`

**Назначение:** Класс игрока, управление движением и взаимодействием.

**Ключевые методы:**
- `getPosition()` — получение позиции
- `getX()`, `getY()` — получение координат
- `stop()` — остановка движения и анимации
- `getSprite()` — получение спрайта

**Управление:**
- Клавиатура: WASD / стрелки
- Drag and Move для мобильных устройств

**Параметры:**
- Скорость: `PLAYER_SPEED = 250` (из `gameConstants.ts`)
- Размер: `ACTOR_SIZES.PLAYER = 4.0`
- **Depth:** `6` (выше порталов, предметов и кустов, но ниже врагов и оракула)

### AbstractEnemy

**Путь:** `src/game/entities/enemies/AbstractEnemy.ts`

**Назначение:** Базовый класс для всех врагов.

**Абстрактные методы:**
- `updateAI(player)` — логика AI (переопределяется в подклассах)

**Общие методы:**
- `update(player)` — обновление врага
  - ✅ **Защита от клонирования во время паузы:** проверка `scene.scene.isPaused()` в начале метода
  - ✅ Если сцена на паузе (модальное окно открыто), враг не обновляется и не клонируется
  - ✅ Проверки паузы также в `handlePlayerDetection()`, `startSpawningAnimation()`, `createClones()` и в callback `delayedCall`
- `onPlayerCollision(player)` — обработка коллизии с игроком
- `destroy()` — уничтожение врага
- `isActive()` — проверка активности

**Свойства поведения:**
- `chaseRadius` — радиус преследования (если > 0, враг преследует игрока)
- `chaseSpeed` — скорость преследования (если > 0, используется вместо baseSpeed при преследовании)
- `cloneDetectionRadius` — радиус детекции для клонирования
- `cloneCount` — количество клонов при детекции

**Наследники:**
- `EnemyRandomWalker` — случайное блуждание
- `EnemyChaser` — преследование игрока (chaseRadius > 0, chaseSpeed > baseSpeed)
- `EnemyFlam` — клонирующий враг (cloneDetectionRadius > 0, cloneCount > 0)

### AbstractPortal

**Путь:** `src/game/entities/portals/AbstractPortal.ts`

**Назначение:** Базовый класс для порталов.

**Абстрактные методы:**
- `onKeyDeposit()` — обработка депозита ключа
- `onEnter()` — обработка входа (возвращает `isCorrect`)

**Общие методы:**
- `depositKey()` — депозит ключа (требуется 3 для открытия)
- `activate()` — активация портала (воспроизводит два звука: начало и завершение)
- `isOpen()` — проверка открыт ли портал

**Особенности:**
- **Размер спрайта:** 64×48 пикселей (до масштабирования)
- **Текстуры и анимации:** Все состояния порталов используют спрайтшиты с анимациями:
  - Базовое состояние: `Portal_X.Base_24F_1536x48.png` (X = A, B, C) - 24 кадра, 8 fps, циклично (3 секунды на цикл)
  - Анимация активации: `Portal_X.Activation_24F_1536x48.png` (X = A, B, C) - 24 кадра, 8 fps, циклично во время активации (3 секунды на цикл)
  - Активированное состояние: `Portal_X.Activated_24F_1536x48.png` (X = A, B, C) - 24 кадра, 8 fps, циклично (3 секунды на цикл)
- Все анимации порталов используют единую скорость 8 fps
- Для physics спрайтов требуется ручная синхронизация кадров в `MainScene.update()`
- **Depth:** `4` (выше кустов и текстов, но ниже предметов, персонажа, врагов и оракула)

**Наследники:**
- `StandardPortal` — стандартный портал

**Состояния (PortalState):**
- `BASE` — базовое состояние (закрыт)
- `INTERACTION` — состояние взаимодействия (проигрывается анимация приема ключа, блокирует ввод 2-го ключа)
- `ACTIVATING` — состояние активации (проигрывается анимация открытия)
- `ACTIVATED` — активирован (открыт, циклическая анимация)

**Логика переходов:**
- `BASE` → При 1-м/2-м ключе → `INTERACTION` (анимация приема) → `BASE`
- `BASE` → При 3-м ключе → `ACTIVATING` (анимация открытия) → `ACTIVATED`
- `ACTIVATED` → Финальное состояние

**Особенности реализации:**
- Метод `depositKey()` возвращает `boolean` (защита от двойного списания)
- Блокировка ввода во время `INTERACTION` и `ACTIVATED`
- Ручная синхронизация анимаций в `MainScene` (fix freezing)

### Oracle

**Путь:** `src/game/entities/Oracle.ts`

**Назначение:** Класс оракула с системой состояний и анимаций.

**Ключевые методы:**
- `depositKey()` — добавить ключ в оракул (переходит в состояние ACTIVATING)
- `getState()` — получить текущее состояние (OracleState)
- `getStoredKeys()` — получить количество хранимых ключей
- `isActivated()` — проверить, активирован ли оракул
- `getSprite()` — получить спрайт
- `reset()` — сбросить состояние оракула
- `destroy()` — уничтожить оракул

**Состояния:**
- `BASE` — базовое состояние (статическая текстура `Oracle.Base_32x64.png`)
- `ACTIVATING` — состояние активации (анимация `oracle_activation`, проигрывается один раз при каждом ключе)
- `ACTIVATED` — активированное состояние (анимация `oracle_activated`, проигрывается циклично)

**Особенности:**
- Использует систему состояний с автоматическими переходами
- Для physics спрайтов требуется ручная синхронизация кадров в `MainScene.update()`
- При доставке каждого ключа проигрывается анимация активации (16 кадров, 2 секунды)
- После получения всех 3 ключей переходит в активированное состояние с циклической анимацией (16 кадров, 2 секунды на цикл)
- **Depth:** `8` (самый верхний слой игровых объектов, выше всех остальных)
- **Размер:** `ACTOR_SIZES.ORACLE = 1.0`

**Текстуры и анимации:**
- Базовое состояние: `Oracle.Base_32x64.png` (статическая текстура, 32×64 пикселей)
- Анимация активации: `Oracle.Activation_16F_512x64.png` (16 кадров, 32×64 каждый, 8 FPS, repeat: 0)
- Активированное состояние: `Oracle.Activated_16F_512x64.png` (16 кадров, 32×64 каждый, 8 FPS, repeat: -1)
- Все анимации используют единую скорость 8 fps

**Логика переходов:**
1. БАЗОВОЕ → при доставке ключа → АКТИВАЦИЯ → после завершения анимации → БАЗОВОЕ (если не все ключи) или АКТИВИРОВАННОЕ (если все 3 ключа)
2. АКТИВИРОВАННОЕ состояние проигрывается циклично и не прерывается

---

## Systems (Системы)

### HealthSystem

**Путь:** `src/game/systems/HealthSystem.ts`

**Назначение:** Управление здоровьем игрока.

**Ключевые методы:**
- `takeDamage(amount)` — нанести урон, возвращает `isAlive`
- `heal(amount)` — восстановить здоровье
- `updatePlayerHealthPosition()` — обновить позицию здоровья над игроком
- `getHealth()` — получить текущее здоровье
- `getMaxHealth()` — получить максимальное здоровье

**Особенности:**
- Использует PNG текстуру `Heart5x5.png` с масштабом 4x для отображения сердечек над головой игрока
- Контейнер со здоровьем имеет **depth `6`** (на уровне персонажа)
- Fallback текстура если PNG не загружен (программно сгенерированная)

### ScoreSystem

**Путь:** `src/game/systems/ScoreSystem.ts`

**Назначение:** Подсчет очков игрока.

**Ключевые методы:**
- `addKeyScore()` — добавить очки за ключ (1 очко)
- `addPortalScore()` — добавить очки за правильный портал (10 очков)
- `getScore()` — получить текущие очки
- `reset()` — сбросить очки

**Система подсчета:**
- Ключ = 1 очко
- Правильный портал = 10 очков

### QuizManager

**Путь:** `src/game/systems/QuizManager.ts`

**Назначение:** Загрузка и управление вопросами из JSON файлов.

**Ключевые методы:**
- `loadLevelQuestions(levelNumber)` — загрузить вопросы уровня
- `getRandomMiniQuiz()` — получить случайный мини-квиз для ключа
- `getGlobalQuestion()` — получить глобальный вопрос для порталов
- `parseQuestion(questionData)` — парсить вопрос (текст или с изображением)
- `getWinMessages()` — получить победные сообщения

**Источник данных:**
- `assets/{CURRENT_THEME}/questions/level1.json`

### SpawnSystem

**Путь:** `src/game/systems/SpawnSystem.ts`

**Назначение:** Система спавна объектов (враги, ключи, сердечки).

**📚 Документация:** [SPAWN_MATRIX_SYSTEM.md](SPAWN_MATRIX_SYSTEM.md) - Подробное описание матричной системы позиционирования

**Ключевые методы:**
- `spawnInitialEnemies()` — начальный спавн врагов
- `spawnEnemy()` — периодический спавн врага
- `spawnItems()` — начальный спавн предметов
- `spawnPeriodicItems()` — периодический спавн предметов
- `getSafePosition(radius)` — получить безопасную позицию для спавна

**✅ Матричная система:**
- Использует `SpawnMatrix` для отслеживания занятости ячеек карты
- Карта 2048×2048 разделена на ячейки 64×64 (итого 32×32 ячейки)
- Типы занятости: `'free'`, `'permanent'`, `'item'`, `'enemy'`
- Гарантирует отсутствие пересечений объектов при спавне
- Враги могут перезаписывать предметы, но не постоянные объекты

**Особенности:**
- Использует `LevelManager` для параметров спавна
- Безопасный спавн (избегает занятых зон)
- Проверка расстояния от игрока
- **Динамические отступы от краев карты:** учитывают размер объекта (radius)
  - Минимальный отступ: 64 виртуальных пикселя
  - Для больших объектов (порталы): отступ = max(radius, 64)
  - Гарантирует, что объекты полностью помещаются внутри карты (0-2048)

### CollisionSystem

**Путь:** `src/game/systems/CollisionSystem.ts`

**Назначение:** Централизованная обработка коллизий.

**Ключевые методы:**
- `setupCollisions()` — настройка всех коллизий
- Обработчики для всех типов объектов

**Типы коллизий:**
- Игрок-враг
- Игрок-предмет (ключ, сердечко)
- Игрок-портал
- Игрок-алтарь
435: - **Поддержка режимов:**
436:   - Стандартный: Arcade physics Bodies
437:   - Tiled Map: Воксельная коллизия + Sensor Bodies для интеракции

### AudioManager

**Путь:** `src/game/systems/AudioManager.ts`

**Назначение:** Управление звуками и музыкой.

**Ключевые методы:**
- `loadAllSounds()` — загрузка всех аудио файлов из конфигурации
- `playBackgroundMusic()` — воспроизведение фоновой музыки
- `playWinMusic()`, `playGameOverMusic()` — музыка победы/проигрыша
- `playButtonClick()`, `playQuestionSuccess()`, `playQuestionFailure()` — звуки UI
- `playPickupLife()`, `playPickupKey()`, `playApplyKey()` — звуки игровых событий
- `playDamage()`, `playDamageKey()` — звуки урона
- `playOracleActivated()`, `playPortalActivated()`, `playPortalActivatedComplete()` — звуки активации
- `setMasterVolume(volume)`, `setMusicVolume(volume)`, `setSoundVolume(volume)` — управление громкостью
- `updateAllVolumes()` — обновление громкости всех звуков
- `pauseAll()`, `resumeAll()`, `stopMusic()` — управление воспроизведением

**Особенности:**
- Все звуки определены в `SOUND_KEYS` и `SOUND_VOLUMES` в `gameConstants.ts`
- Трехуровневая система громкости: master, music, sound
- Индивидуальные базовые громкости для каждого звука
- Кеширование загруженных звуков для предотвращения повторной загрузки
- Порталы имеют два звука активации: `PORTAL_ACTIVATED` (начало) и `PORTAL_ACTIVATED_COMPLETE` (когда появляется надпись ответа)
- Звук `damage` воспроизводится только при столкновении с врагами на карте, не используется в модальных окнах

### AnimationManager

**Путь:** `src/game/systems/AnimationManager.ts`

**Назначение:** Универсальная система загрузки спрайтшитов и создания анимаций.

**Ключевые методы:**
- `loadSpritesheet(config)` — загрузка спрайтшита
- `createAnimations(spritesheetKey, animations)` — создание анимаций из спрайтшита
- `generateHorizontalFrames(start, end)` — генерация кадров для горизонтальной полосы
- `generateVerticalFrames(start, end, cols)` — генерация кадров для вертикальной полосы
- `generateGridDirectionFrames(direction, frameCount, cols)` — генерация кадров для сетки с направлениями
- `generateGridFrames(startRow, endRow, startCol, endCol, cols)` — генерация кадров для произвольной сетки

**Особенности:**
- Все спрайтшиты конфигурируются в `src/config/spritesheetConfigs.ts`
- Поддержка разных форматов: HORIZONTAL, VERTICAL, GRID, CUSTOM
- Автоматическая валидация кадров
- Предотвращение повторной загрузки и создания анимаций

**Конфигурация:**
- Все спрайтшиты определены в `SPRITESHEET_CONFIGS` (см. `src/config/spritesheetConfigs.ts`)
- Поддерживаемые форматы: горизонтальные полосы (ключи, BoyJump), сетки 4x4 (игрок, враги)

### SpriteAnimationHandler

**Путь:** `src/game/systems/SpriteAnimationHandler.ts`

**Назначение:** Универсальный обработчик анимаций для физических спрайтов.

**Ключевые методы:**
- `playDirectionAnimation(velocityX, velocityY)` — воспроизведение анимации по направлению движения
- `syncFrame()` — ручная синхронизация кадров (для physics спрайтов)
- `stop()` — остановка анимации
- `isPlaying()` — проверка воспроизведения
- `getCurrentAnimation()` — получение текущей анимации

**Особенности:**
- Используется для игрока и врагов (physics спрайты)
- Ручная синхронизация кадров через таймер (Phaser не обновляет кадры автоматически для physics спрайтов)
- Поддержка "мертвой зоны" для предотвращения мерцания анимаций
- Автоматическое определение направления по скорости

**Использование:**
- `Player` использует `SpriteAnimationHandler` для анимаций ходьбы
- `AbstractEnemy` использует `SpriteAnimationHandler` для анимаций движения врагов
- Для обычных спрайтов (не physics) используется стандартная система Phaser

---

## Scenes (Сцены)

### BaseScene

**Путь:** `src/game/scenes/BaseScene.ts`

**Назначение:** Базовый класс для всех игровых сцен.

**Ключевые методы:**
- `initBaseSystems()` — инициализация AssetLoader, LevelManager, GameState
- `setupPhysics()` — настройка физики Arcade
- `setupCamera()` — настройка камеры
- `getCameraWorldSize()` — получение размера камеры в координатах мира

**Свойства:**
- `assetLoader: AssetLoader`
- `levelManager: LevelManager`
- `gameState: GameState`

### LoadingScene

**Путь:** `src/game/scenes/LoadingScene.ts`

**Назначение:** Сцена загрузки ресурсов.

**Ключевые методы:**
- `preload()` — загрузка всех текстур и спрайтшитов через `AnimationManager`
- `startLoading()` — инициализация загрузки через универсальную систему

**Особенности:**
- Использует `AnimationManager` для загрузки всех спрайтшитов из `SPRITESHEET_CONFIGS`
- Все спрайтшиты загружаются автоматически из конфигурации
- Анимации создаются в `MainScene` после загрузки (не в `LoadingScene`)
- Нет хардкода — все через конфигурацию

### MainScene

**Путь:** `src/game/scenes/MainScene.ts`

**Назначение:** Основная игровая сцена.

**Использует все модули:**
- `Player` класс (с `SpriteAnimationHandler`)
- `AbstractEnemy` и наследники (с `SpriteAnimationHandler`)
- `AbstractPortal` и наследники
- `HealthSystem` для здоровья
- `ScoreSystem` для очков
- `QuizManager` для вопросов
- `AudioManager` для всех звуков
- `AnimationManager` для создания анимаций
- `KeyQuestionModal`, `PortalModal`, `GameOverModal` — модальные окна
- `LevelManager` для параметров уровня
- `SpawnSystem` для спавна
- `CollisionSystem` для коллизий

**Особенности анимаций:**
- Создает все анимации через `AnimationManager` в `initializeSystems()`
- Ручная синхронизация кадров для ключей и BoyJump в `update()` (physics спрайты)
- Игрок и враги используют `SpriteAnimationHandler` для автоматической синхронизации

**Режимы создания игрового мира:**
- **Random Spawn (старая реализация):** `createGameWorldRandom()` — случайное размещение порталов и оракула
- **Tiled Map (новая реализация):** `createGameWorldTiled()` — размещение объектов на основе Tiled Map JSON
- Переключение через флаг `useTiledMap` в конфигурации уровня
- Подробная документация: [TILED_MAP_IMPLEMENTATION.md](TILED_MAP_IMPLEMENTATION.md)

---

## UI компоненты

### Button

**Путь:** `src/game/ui/Button.ts`

**Назначение:** Интерактивная кнопка с состояниями.

**Состояния:** `NORMAL`, `HOVER`, `ACTIVE`, `DISABLED`, `BLINKING`, `WRONG`

**Особенности:**
- Защита от двойных кликов
- Адаптивный размер шрифта
- Поддержка wordWrap

### QuestionBubble

**Путь:** `src/game/ui/QuestionBubble.ts`

**Назначение:** Баббл сообщения для отображения вопросов Оракула и ответов порталов в игровом мире.

**Типы бабблов:**
- `'oracle'` — для Оракула (BubbleMsg.Transparent152x56.png, 152×56px, рабочая область 134×36px)
- `'portal'` — для порталов (BubbleMsg.Transparent136x48.png, 136×48px, рабочая область 118×28px)

**Основные компоненты:**
- `bubbleSprite` — спрайт фона баббла (Depth: 10)
- `questionText` — текст вопроса/ответа (Depth: 10, стиль: жирный, черный, без обводки)
- `questionImage` — опциональное изображение вопроса (Depth: 10, правая часть, 1/4 ширины)
- `hintText` — текст подсказки для пользователя (Depth: 4, стиль: белый с черной обводкой)

**Ключевые особенности:**
- **Глобальный размер шрифта:** рассчитывается один раз на основе самого длинного текста из всех вопросов уровня, используется для всех бабблов (оптимизация производительности)
- **Автоматический расчет размера:** использует `calculateOptimalBaseFontSize()` для адаптации под длину текста
- **Поддержка вопросов с изображениями:** при наличии изображения текст занимает 3/4 ширины (выравнивание по правому краю), изображение — 1/4 ширины
- **Анимации:**
  - Fade in/out для показа/скрытия (300ms, Power2 easing)
  - Ping-pong анимация для подсказок (75% → 25% → 75%, 1500ms цикл, бесконечное повторение)
- **Управление видимостью:** через тап/клик по родительскому объекту (Oracle/Portal)
- **Позиционирование:** нижняя граница баббла совпадает с верхней границей родительского объекта

**Интеграция:**
- **Oracle:** баббл создается в `createQuestionBubble()`, управляется через `setQuestion()`, `toggleQuestionBubble()`, `updateBubblePosition()`
- **Portal:** баббл создается в `createLabels()`, показывается при активации (`setActivatedState()`), скрывается при деактивации (`setBaseState()`)

**Z-иерархия:**
- Бабблы имеют максимальный приоритет отображения (Depth: 10), выше всех игровых элементов
- Подсказки (Depth: 4) выше кустов и травы, но ниже предметов, персонажа и врагов

**Константы:**
- Размеры: `BUBBLE_SIZES` в `gameConstants.ts`
- Стили текста: `BUBBLE_QUESTION_*`, `BUBBLE_HINT_*` в `textStyles.ts`
- Смещения подсказок: `HINT_OFFSETS` в `gameConstants.ts`

**Документация:** Подробное описание в [BUBBLE_SYSTEM.md](BUBBLE_SYSTEM.md)

### KeyQuestionModal

**Путь:** `src/game/ui/KeyQuestionModal.ts`

**Назначение:** Модальное окно для вопросов при поднятии ключей.

**Логика:**
- Правильный ответ: мигание → второе нажатие → закрытие + ключ
- Неправильный ответ: минус жизнь → кнопка неактивна → wrongFeedback
- Крестик → закрытие без ключа

### PortalModal

**Путь:** `src/game/ui/PortalModal.ts`

**Назначение:** Модальное окно для подтверждения входа в портал.

**Конфигурация:**
- Размер: рассчитывается через `calculateModalSize()` на основе реального размера canvas
- Depth: 1000-1002
- Layout: 5 равных блоков (вопрос, картинка, ответ, кнопка Войти, кнопка Отмена)

**Расчет размеров шрифтов:**
- Использует единый `baseFontSize` для всех элементов (вопрос, ответ, кнопки)
- `baseFontSize` рассчитывается через `calculateOptimalBaseFontSize()` на основе самого длинного текста
- Все элементы включаются в расчет `unifiedFontSize` (минимум из всех)
- Применяется единый множитель `1.5` ко всем элементам
- Гарантирует идентичность размеров шрифтов с кнопками в окнах конца игры

**Особенности:**
- Звук `damage` не воспроизводится при входе в неправильный портал (урон наносится, но звук только при столкновении с врагами)
- Отложенное создание UI: `scene.time.delayedCall(1, ...)` для корректного расчета размеров canvas
- Защита от случайных нажатий: кнопки активны через 150мс после создания

### GameOverModal

**Путь:** `src/game/ui/GameOverModal.ts`

**Назначение:** Модальное окно окончания игры.

**Конфигурация:**
- Размер: рассчитывается через `calculateModalSize()` на основе реального размера canvas
- Depth: 2000-2002
- Layout: 5 равных блоков (заголовок, счетчик, персонаж, кнопка Рестарт, кнопка "Следующий уровень")
- Типы: `WIN_GAME`, `WIN_LEVEL`, `LOSE`

**Расчет размеров шрифтов:**
- Использует единый `baseFontSize` для всех элементов (заголовок, счетчик, кнопки)
- `baseFontSize` рассчитывается через `calculateOptimalBaseFontSize()` на основе самого длинного текста
- Все элементы включаются в расчет `unifiedFontSize` (минимум из всех)
- Применяется единый множитель `1.5` ко всем элементам
- Гарантирует идентичность размеров шрифтов с кнопками в окнах порталов

**Особенности:**
- Использует анимацию `boy_jump_lose` или `boy_jump_win` из конфигурации
- Анимация создается через `AnimationManager` (не хардкод)
- Ручная синхронизация кадров в `MainScene.update()` (physics спрайт)
- Появляется с задержкой 1 секунда после события окончания игры, чтобы успели проиграться звуки события
- Отложенное создание UI: `scene.time.delayedCall(1, ...)` для корректного расчета размеров canvas
- Пересоздание UI при изменении ориентации экрана (не закрывается, так как игра завершена)

---

## Интеграция React-Phaser

### EventBus

**Путь:** `src/game/EventBus.ts`

**Назначение:** Система событий для коммуникации между React и Phaser.

**События Phaser → React:**
- `update-hud` — обновление HUD (health, keys, altarActive)
- `show-quiz` — показ модального окна викторины
- `game-over` — окончание игры (win/lose)

**События React → Phaser:**
- `quiz-completed` — результат викторины (correct, context)
- `restart-game` — перезапуск игры

**Пример использования:**
```typescript
// Phaser → React
EventBus.emit('update-hud', { health: 3, keys: 2, altarActive: false });

// React → Phaser
EventBus.emit('quiz-completed', { correct: true, context: 'key' });
```

### PhaserGame компонент

**Путь:** `src/react/PhaserGame.tsx`

**Назначение:** React обертка для Phaser.Game.

**Особенности:**
- Использует `forwardRef` для доступа к Phaser.Game
- Инициализирует Phaser при монтировании
- Очищает ресурсы при размонтировании

---

## Паттерны проектирования

### Singleton

- `GameState` — глобальное состояние игры
- `EventBus` — система событий

### Factory

- `SpawnSystem` — создание врагов и предметов
- `AssetLoader` — создание ресурсов

### Strategy

- `AbstractEnemy` и наследники — разные стратегии AI
- `AbstractPortal` и наследники — разные типы порталов

### Observer

- `EventBus` — подписка на события

---

## Потоки данных

### Инициализация игры

```
index.html
  └─> main.tsx
       └─> App.tsx
            ├─> PhaserGame.tsx (создает Phaser.Game)
            │    └─> LoadingScene (загрузка ресурсов)
            │         └─> MainScene (основная сцена)
            └─> UIOverlay (HUD)
```

### Игровой цикл

```
MainScene.update()
  ├─> Player.update() (движение + SpriteAnimationHandler.syncFrame())
  ├─> HealthSystem.updatePlayerHealthPosition()
  ├─> Enemy.update() (AI врагов + SpriteAnimationHandler.syncFrame())
  ├─> Camera follow (автоматическое)
  ├─> CollisionSystem (проверка коллизий)
  ├─> Key animation sync (ручная синхронизация кадров для ключей)
  ├─> BoyJump animation sync (ручная синхронизация кадров для GameOverModal)
  └─> Key rings rendering
```

### Взаимодействие с порталами

```
handlePortalOverlapEntry(portal)
  └─> showPortalModal(portal)
       └─> PortalModal
            ├─> onEnter → handlePortalEntry()
            │              ├─> isCorrect → WIN
            │              └─> !isCorrect → LOSE
            └─> onCancel → resumeGame()
```

---

## Конфигурация TypeScript

### vite-env.d.ts

**Путь:** `src/vite-env.d.ts`

**Назначение:** TypeScript типы для Vite environment variables.

**Содержит:**
- Интерфейс `ImportMetaEnv` с типами для переменных окружения:
  - `VITE_CURRENT_THEME?: string`
  - `VITE_ENABLE_FEEDBACKS?: string`
  - `VITE_ENABLE_WRONG_FEEDBACKS?: string`
- Расширение интерфейса `ImportMeta` для поддержки `import.meta.env`

**Использование:**
- Используется в `gameConfig.ts` для типизации `import.meta.env`
- Предотвращает ошибки TypeScript при работе с переменными окружения Vite

---

## Tiled Map реализация

### Обзор

Проект поддерживает два режима создания уровней:
1. **Random Spawn** (старая реализация) — случайное размещение объектов
2. **Tiled Map** (новая реализация) — размещение объектов на основе Tiled Map JSON

### A/B тестирование

Переключение между режимами происходит через флаг `useTiledMap` в конфигурации уровня:

```json
{
  "levelConfig": {
    "useTiledMap": true,
    "tiledMapKey": "level1_json"
  }
}
```

### Структура Tiled Map

Tiled Map JSON файл содержит:
- **Tile Layer "Collision Mask"** — зоны коллизий (сетка 64×64 тайлов)
- **Object Layer "InteractiveObjects"** — объекты Oracle и Portal с координатами

### Интеграция

- Коллизии из Tiled Map интегрируются в SpawnMatrix как `'permanent'` ячейки
- Порталы из Tiled Map используют статичные текстуры (32×48 пикселей)
- Три слоя фона: Base BG, Struct BG, Overlay BG
- Координаты объектов конвертируются из Tiled Map в мировые координаты

### Подробная документация

Полное описание реализации: [TILED_MAP_IMPLEMENTATION.md](TILED_MAP_IMPLEMENTATION.md)

---

## Связанные документы

- [ProjectMap.md](ProjectMap.md) — карта проекта с детальным описанием всех компонентов
- [ModularArchitecturePlan.md](ModularArchitecturePlan.md) — план модульной архитектуры
- [ModularArchitectureProgress.md](ModularArchitectureProgress.md) — прогресс реализации
- [TILED_MAP_IMPLEMENTATION.md](TILED_MAP_IMPLEMENTATION.md) — реализация уровней с Tiled Map
- [SPAWN_MATRIX_SYSTEM.md](SPAWN_MATRIX_SYSTEM.md) — матричная система позиционирования

---

## История изменений

| Дата | Версия | Изменения | Автор |
|------|--------|-----------|-------|
| 2025-01-26 | 1.0 | Создание документа | AI Assistant |
| 2025-01-27 | 1.1 | Добавлен AnimationManager и SpriteAnimationHandler | AI Assistant |
| 2025-01-27 | 1.1 | Обновлен AudioManager (все звуки, константы громкости) | AI Assistant |
| 2025-01-27 | 1.1 | Обновлен LoadingScene (универсальная система, без хардкода) | AI Assistant |
| 2025-01-27 | 1.1 | Добавлена информация о ручной синхронизации кадров | AI Assistant |
| 2025-01-27 | 1.2 | Обновлены depth значения: портал `5`, игрок `150`, сердечки `160` | AI Assistant |
| 2025-01-27 | 1.3 | Реорганизован Z-порядок: фон(-200/-100) → трава(1) → тексты(2) → кусты(3) → порталы(4) → предметы(5) → персонаж(6) → враги(7) → оракул(8) | AI Assistant |
| 2025-01-27 | 1.2 | Обновлены текстуры: портал `Portal_64x48.png`, сердечки `Heart5x5.png` | AI Assistant |
| 2025-01-27 | 1.2 | Добавлен звук `PORTAL_ACTIVATED_COMPLETE` для завершения активации портала | AI Assistant |
| 2025-01-27 | 1.2 | Обновлены ограничения на звук `damage` - только при столкновении с врагами | AI Assistant |
| 2025-01-27 | 1.2 | Обновлен размер `PortalModal` - совпадает с `KeyQuestionModal` | AI Assistant |
| 2025-01-27 | 1.2 | Добавлена задержка 1 секунда перед показом `GameOverModal` | AI Assistant |
| 2025-01-27 | 1.2 | Добавлено описание `vite-env.d.ts` для типов Vite environment variables | AI Assistant |
| 2025-01-27 | 1.3 | Унифицирована логика расчета размеров шрифтов для PortalModal и GameOverModal | AI Assistant |
| 2025-01-27 | 1.3 | Все элементы (включая кнопки) используют единый `baseFontSize` в обоих модальных окнах | AI Assistant |
| 2025-01-27 | 1.3 | Применяется единый множитель `1.5` ко всем элементам после расчета `unifiedFontSize` | AI Assistant |
| 2025-01-27 | 1.3 | Гарантирована идентичность размеров шрифтов в кнопках порталов и окнах конца игры | AI Assistant |
| 2025-12-08 | 1.7 | Добавлена ссылка на документацию по матричной системе позиционирования | AI Assistant |
| 2025-01-XX | 1.8 | Добавлена поддержка Tiled Map для создания уровней (A/B тестирование), добавлен раздел Tiled Map реализация | AI Assistant |

---

**Последнее обновление:** 2025-01-XX


