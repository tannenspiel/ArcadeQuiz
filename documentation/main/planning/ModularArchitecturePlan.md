# План реорганизации проекта для модульности

**Дата:** 2025-01-XX  
**Версия:** 1.0

---

## Обзор изменений

Проект реорганизуется в модульную архитектуру с четким разделением ответственности, поддержкой множественных тем и расширяемостью.

---

## 1. Структура папок (новая)

```
src/
├── config/
│   ├── gameConfig.ts              ✅ СОЗДАН - CURRENT_THEME, A/B тестирование
│   └── levelConfigs/              ✅ СОЗДАН - JSON конфиги уровней
│       ├── level1.json            ✅ СОЗДАН
│       ├── level2.json
│       └── ...
│
├── assets/
│   ├── Game_01/                   ✅ СУЩЕСТВУЕТ
│   │   ├── images/
│   │   │   └── QuizGame_Heart15x15.png
│   │   ├── audio/
│   │   └── questions/            ✅ СОЗДАН
│   │       └── level1.json        ✅ СОЗДАН
│   ├── Game_02/                   ✅ СУЩЕСТВУЕТ
│   └── ...
│
├── game/
│   ├── core/                      ✅ СОЗДАН
│   │   ├── AssetLoader.ts         ✅ Загрузка ресурсов по CURRENT_THEME
│   │   ├── LevelManager.ts       ✅ Управление уровнями и сложностью
│   │   ├── GameState.ts           ✅ Глобальное состояние игры
│   │   └── SceneManager.ts        ⏳ Управление сценами (опционально)
│   │
│   ├── entities/                  ✅ СОЗДАН
│   │   ├── Player.ts              ✅ Класс игрока
│   │   ├── enemies/
│   │   │   ├── AbstractEnemy.ts  ✅ Базовый класс врагов
│   │   │   ├── EnemyRandomWalker.ts ✅ Случайное блуждание
│   │   │   ├── EnemyChaser.ts    ✅ Преследование игрока
│   │   │   └── EnemyFlam.ts  ✅ Универсальный враг с настраиваемым поведением (клонирование, преследование)
│   │   ├── Rune.ts                ⏳ Класс ключа (опционально, можно оставить как спрайт, устаревшее название)
│   │   ├── Altar.ts               ⏳ Класс алтаря (опционально)
│   │   └── portals/
│   │       ├── AbstractPortal.ts  ✅ Базовый класс порталов
│   │       ├── StandardPortal.ts ✅ Стандартный портал
│   │       ├── TrapPortal.ts     ⏳ Ловушка (опционально)
│   │       └── BonusPortal.ts    ⏳ Бонусный портал (опционально)
│   │
│   ├── systems/                   ✅ СОЗДАН
│   │   ├── SpawnSystem.ts         ✅ Система спавна (использует LevelManager)
│   │   ├── CollisionSystem.ts     ✅ Система коллизий
│   │   ├── QuizManager.ts         ✅ Загрузка и управление вопросами из JSON
│   │   ├── HealthSystem.ts        ✅ Управление жизнями (PNG текстура)
│   │   ├── ScoreSystem.ts         ✅ Подсчет очков (ключ=1, портал=10)
│   │   └── AudioManager.ts        ✅ Управление звуками и музыкой
│   │
│   ├── ui/                        ✅ ЧАСТИЧНО СОЗДАН
│   │   ├── Button.ts              ✅ СОЗДАН - Класс кнопки с состояниями
│   │   ├── KeyQuestionModal.ts   ✅ СОЗДАН - Модальное окно для ключей
│   │   ├── PortalModal.ts          ✅ СОЗДАН - Модальное окно для порталов
│   │   ├── GameOverModal.ts       ✅ СОЗДАН - Модальное окно Game Over
│   │   ├── ButtonManager.ts       ⏳ Управление всеми кнопками (опционально)
│   │   └── Renderer.ts            ⏳ Централизованный рендеринг UI (опционально)
│   │
│   ├── scenes/
│   │   ├── BaseScene.ts           ✅ Базовый класс сцены
│   │   ├── LoadingScene.ts        ✅ Загрузка ресурсов
│   │   └── MainScene.ts           ✅ РЕФАКТОРИНГ ЗАВЕРШЕН - использует новые модули
│   │
│   └── EventBus.ts                ✅ СУЩЕСТВУЕТ
│
├── types/
│   ├── gameTypes.ts               ✅ СУЩЕСТВУЕТ
│   ├── questionTypes.ts           ✅ СОЗДАН - Типы для JSON вопросов
│   ├── levelTypes.ts              ✅ СОЗДАН - Типы для конфигов уровней
│   ├── enemyTypes.ts              ✅ СОЗДАН - Типы для врагов
│   ├── portalTypes.ts             ✅ СОЗДАН - Типы для порталов
│   └── scoreTypes.ts              ✅ СОЗДАН - Типы для системы очков
│
└── constants/
    └── gameConstants.ts           ✅ СУЩЕСТВУЕТ (частично мигрировать в config)
```

---

## 2. Ключевые компоненты для создания

### 2.1. Core (Ядро)

#### `AssetLoader.ts`
- Загружает ресурсы по `CURRENT_THEME`
- Путь: `src/assets/${CURRENT_THEME}/...`
- Кеширует загруженные ресурсы
- Методы: `loadImage()`, `loadAudio()`, `loadJSON()`, `getAsset()`

#### `LevelManager.ts`
- Загружает конфиги уровней из JSON
- Управляет сложностью:
  - Количество врагов
  - Скорость врагов
  - Соотношение типов врагов (random/chaser)
  - Какие типы доступны на каждом уровне
- Поддерживает неограниченное количество уровней
- Методы: `getLevelConfig()`, `getEnemySpawnConfig()`, `getDifficultyMultiplier()`

#### `GameState.ts`
- Глобальное состояние игры
- Текущий уровень, здоровье, ключи, очки
- Методы: `getState()`, `setState()`, `reset()`

#### `SceneManager.ts`
- Управление переходами между сценами
- Методы: `switchScene()`, `getCurrentScene()`

---

### 2.2. Entities (Сущности)

#### `Player.ts`
- Класс игрока
- Управление движением, здоровьем, ключами
- Методы: `move()`, `takeDamage()`, `addKey()`, `update()`

#### `AbstractEnemy.ts`
- Базовый класс для всех врагов
- Общий функционал (позиция, здоровье, коллизии)
- Абстрактные методы: `updateAI()`, `onCollision()`

#### `EnemyRandomWalker.ts` extends `AbstractEnemy`
- Реализация случайного блуждания
- Не преследует игрока
- Движется случайными направлениями

#### `EnemyChaser.ts` extends `AbstractEnemy`
- Реализация преследования
- Агрессивный радиус
- Движется к игроку при приближении

#### `AbstractPortal.ts`
- Базовый класс для порталов
- Общий функционал (активация, ключи, состояние)
- Абстрактные методы: `onActivate()`, `onEnter()`

#### `StandardPortal.ts` extends `AbstractPortal`
- Стандартная реализация портала
- Текущая логика из MainScene

---

### 2.3. Systems (Системы)

#### `SpawnSystem.ts`
- Использует `LevelManager` для параметров спавна
- Управляет спавном врагов, ключей, сердец
- Методы: `spawnEnemy()`, `spawnKeyMatrix()`, `spawnHeartMatrix()`

#### `CollisionSystem.ts`
- Централизованная обработка коллизий
- Методы: `registerCollision()`, `checkCollisions()`

#### `QuizManager.ts`
- Загружает JSON вопросы из `assets/${CURRENT_THEME}/questions/`
- Парсит структуру (обычные и с изображениями)
- Методы: `loadLevelQuestions()`, `getRandomMiniQuiz()`, `getGlobalQuestion()`

#### `HealthSystem.ts`
- Управление жизнями
- Использует PNG текстуру `QuizGame_Heart15x15.png`
- Отображение над головой игрока и в UI
- Методы: `setHealth()`, `takeDamage()`, `addHealth()`, `render()`

#### `ScoreSystem.ts`
- Подсчет очков:
  - Ключ = 1 очко
  - Правильный портал = 10 очков
- Методы: `addScore()`, `getScore()`, `reset()`

#### `AudioManager.ts`
- Управление звуками и музыкой
- Загрузка из `assets/${CURRENT_THEME}/audio/`
- Методы: `playSound()`, `playMusic()`, `setVolume()`, `pause()`, `resume()`

---

### 2.4. UI (Интерфейс)

#### `Button.ts` ✅ СОЗДАН
- Класс кнопки с состояниями
- Состояния: NORMAL, HOVER, ACTIVE, DISABLED, BLINKING, WRONG
- Методы: `setState()`, `setText()`, `setFeedback()`

#### `KeyQuestionModal.ts` ✅ СОЗДАН
- Модальное окно для мини-вопросов
- Логика: правильный ответ → мигание → второе нажатие → закрытие
- Неправильный ответ → минус жизнь → кнопка неактивна → wrongFeedback
- Крестик → закрытие без ключа

#### `ButtonManager.ts`
- Управление всеми кнопками в игре
- Методы: `createButton()`, `updateButtons()`, `destroyAll()`

#### `Renderer.ts`
- Централизованный рендеринг UI элементов
- Методы: `renderHUD()`, `renderHealth()`, `renderScore()`

---

### 2.5. Scenes (Сцены)

#### `BaseScene.ts`
- Базовый класс для всех сцен
- Общий функционал (камера, физика, EventBus)
- Методы: `init()`, `create()`, `update()`

#### `LoadingScene.ts`
- Загрузка ресурсов через `AssetLoader`
- Прогресс-бар загрузки
- Переход в `MainScene` после загрузки

#### `MainScene.ts` ⏳ РЕФАКТОРИНГ
- Использовать новые модули:
  - `Player` вместо прямого управления
  - `AbstractEnemy` и наследники
  - `AbstractPortal` и наследники
  - `HealthSystem` для здоровья
  - `ScoreSystem` для очков
  - `QuizManager` для вопросов
  - `KeyQuestionModal` вместо React модала
  - `LevelManager` для параметров уровня

---

## 3. Конфигурационные файлы

### 3.1. `config/gameConfig.ts` ✅ СОЗДАН
```typescript
export const CURRENT_THEME = 'Game_01';
export const ASSETS_BASE_PATH = `/src/assets/${CURRENT_THEME}`;
export const AB_TESTING = {
  ENABLE_FEEDBACKS: true,
  ENABLE_WRONG_FEEDBACKS: true
};
```

### 3.2. `config/levelConfigs/level1.json` ✅ СОЗДАН
```json
{
  "levelNumber": 1,
  "enemySpawn": {
    "initialCount": 5,
    "periodicSpawnDelay": 6000,
    "maxEnemies": 15,
    "initialCount": {
      "randomWalker": 4,
      "chaser": 2
    },
    "enabledTypes": ["randomWalker", "chaser"]
  },
  "enemySpeed": {
    "randomWalker": 80,
    "chaser": 45
  },
  "itemSpawn": {
    "hearts": { "initial": 5, "max": 10, "spawnDelay": 24000 },
    "keys": { "initial": 8, "max": 12, "spawnDelay": 24000 }
  }
}
```

### 3.3. JSON вопросы ✅ СОЗДАН
- `assets/Game_01/questions/level1.json` ✅
- Структура: `miniQuizzes`, `globalQuestion`, `globalQuestionWithImage`

---

## 4. Типы TypeScript

### 4.1. `types/questionTypes.ts` ✅ СОЗДАН
- `QuestionType`, `QuestionData`, `ParsedQuestion`, `LevelQuestionsData`

### 4.2. `types/levelTypes.ts` ✅ СОЗДАН
```typescript
export interface LevelConfig {
  levelNumber: number;
  enemySpawn: EnemySpawnConfig;
  enemySpeed: EnemySpeedConfig;
  itemSpawn: ItemSpawnConfig;
}
```

### 4.3. `types/enemyTypes.ts` ✅ СОЗДАН
```typescript
export enum EnemyType {
  RANDOM_WALKER = 'randomWalker',
  CHASER = 'chaser'
}
```

### 4.4. `types/portalTypes.ts` ✅ СОЗДАН
```typescript
export enum PortalType {
  STANDARD = 'standard',
  TRAP = 'trap',
  BONUS = 'bonus'
}
```

---

## 5. Порядок реализации

### Этап 1: Базовая инфраструктура ✅ ЗАВЕРШЕН
1. ✅ Создать `config/gameConfig.ts`
2. ✅ Создать `types/questionTypes.ts`
3. ✅ Создать пример JSON вопросов
4. ✅ Создать `Button.ts`
5. ✅ Создать `KeyQuestionModal.ts`
6. ✅ Создать `types/levelTypes.ts`
7. ✅ Создать `types/enemyTypes.ts`
8. ✅ Создать `types/portalTypes.ts`
9. ✅ Создать `types/scoreTypes.ts`

### Этап 2: Core системы ✅ ЗАВЕРШЕН
10. ✅ Создать `AssetLoader.ts`
11. ✅ Создать `LevelManager.ts`
12. ✅ Создать `GameState.ts`
13. ⏳ Создать `SceneManager.ts` (опционально)

### Этап 3: Entities ✅ ЗАВЕРШЕН
14. ✅ Создать `Player.ts`
15. ✅ Создать `AbstractEnemy.ts`
16. ✅ Создать `EnemyRandomWalker.ts`
17. ✅ Создать `EnemyChaser.ts`
18. ✅ Создать `EnemyFlam.ts`
19. ✅ Создать `AbstractPortal.ts`
20. ✅ Создать `StandardPortal.ts`
21. ⏳ Создать `Rune.ts` (опционально)
22. ⏳ Создать `Altar.ts` (опционально)

### Этап 4: Systems ✅ ЗАВЕРШЕН
23. ✅ Создать `QuizManager.ts`
24. ✅ Создать `HealthSystem.ts` (с PNG текстурой)
25. ✅ Создать `ScoreSystem.ts`
26. ✅ Создать `AudioManager.ts`
27. ✅ Создать `SpawnSystem.ts`
28. ✅ Создать `CollisionSystem.ts`

### Этап 5: UI ✅ ЧАСТИЧНО ЗАВЕРШЕН
29. ✅ Создать `Button.ts`
30. ✅ Создать `KeyQuestionModal.ts`
31. ✅ Создать `PortalModal.ts`
32. ✅ Создать `GameOverModal.ts`
33. ⏳ Создать `ButtonManager.ts` (опционально)
34. ⏳ Создать `Renderer.ts` (опционально)

### Этап 6: Scenes ✅ ЗАВЕРШЕН
35. ✅ Создать `BaseScene.ts`
36. ✅ Создать `LoadingScene.ts`
37. ✅ Рефакторинг `MainScene.ts` - ЗАВЕРШЕН

### Этап 7: Интеграция ✅ ЗАВЕРШЕН
38. ✅ Интегрировать все модули в `MainScene.ts`
39. ✅ Обновить `App.tsx` для использования новой архитектуры
40. ✅ Тестирование всех систем

---

## 6. Миграция существующего кода

### Из `MainScene.ts` вынести:
- Логику игрока → `Player.ts`
- Логику врагов → `AbstractEnemy` и наследники
- Логику порталов → `AbstractPortal` и наследники
- Логику здоровья → `HealthSystem.ts`
- Логику вопросов → `QuizManager.ts`
- Логику спавна → `SpawnSystem.ts`
- Логику коллизий → `CollisionSystem.ts`

### Из `gameConstants.ts`:
- Константы уровней → `LevelManager.ts`
- Константы вопросов → JSON файлы
- Оставить только базовые константы (MAP_WIDTH, SPEED и т.д.)

---

## 7. Переменные окружения

Создать `.env` файл:
```env
VITE_CURRENT_THEME=Game_01
VITE_ENABLE_FEEDBACKS=true
VITE_ENABLE_WRONG_FEEDBACKS=true
```

---

## 8. Преимущества новой архитектуры

1. **Модульность** - каждый компонент отвечает за свою область
2. **Расширяемость** - легко добавлять новые типы врагов/порталов
3. **Мульти-тема** - поддержка N игр через CURRENT_THEME
4. **Тестируемость** - изолированные модули легче тестировать
5. **Поддержка** - четкая структура упрощает поддержку
6. **Переиспользование** - компоненты можно использовать в разных сценах

---

## 9. Статус выполнения

✅ - Выполнено  
⏳ - Требуется создание  
🔄 - Требуется рефакторинг

### Текущий прогресс:

#### ✅ Этап 1: Базовая инфраструктура - ЗАВЕРШЕН
- ✅ `config/gameConfig.ts` - CURRENT_THEME, A/B тестирование
- ✅ `types/questionTypes.ts` - Типы для JSON вопросов
- ✅ `types/levelTypes.ts` - Типы для конфигов уровней
- ✅ `types/enemyTypes.ts` - Типы для врагов
- ✅ `types/portalTypes.ts` - Типы для порталов
- ✅ `types/scoreTypes.ts` - Типы для системы очков
- ✅ `config/levelConfigs/level1.json` - Конфиг уровня
- ✅ `assets/Game_01/questions/level1.json` - Пример JSON вопросов

#### ✅ Этап 2: Core системы - ЗАВЕРШЕН
- ✅ `AssetLoader.ts` - Загрузка ресурсов по CURRENT_THEME
- ✅ `LevelManager.ts` - Управление уровнями и сложностью
- ✅ `GameState.ts` - Глобальное состояние игры
- ⏳ `SceneManager.ts` - Управление сценами (опционально)

#### ✅ Этап 3: Entities - ЗАВЕРШЕН
- ✅ `Player.ts` - Класс игрока
- ✅ `AbstractEnemy.ts` - Базовый класс врагов
- ✅ `EnemyRandomWalker.ts` - Случайное блуждание
- ✅ `EnemyChaser.ts` - Преследование игрока
- ✅ `AbstractPortal.ts` - Базовый класс порталов
- ✅ `StandardPortal.ts` - Стандартный портал

#### ✅ Этап 4: Systems - ЗАВЕРШЕН
- ✅ `QuizManager.ts` - Загрузка JSON вопросов
- ✅ `HealthSystem.ts` - Управление жизнями (PNG текстура)
- ✅ `ScoreSystem.ts` - Подсчет очков (ключ=1, портал=10)
- ✅ `AudioManager.ts` - Управление звуками и музыкой
- ✅ `SpawnSystem.ts` - Система спавна
- ✅ `CollisionSystem.ts` - Система коллизий

#### ✅ Этап 5: UI - ЧАСТИЧНО ЗАВЕРШЕН
- ✅ `Button.ts` - Класс кнопки с состояниями
- ✅ `KeyQuestionModal.ts` - Модальное окно для ключей (устаревшее название, используется KeyQuestionModal)
- ⏳ `ButtonManager.ts` - Управление всеми кнопками
- ⏳ `Renderer.ts` - Централизованный рендеринг

#### ✅ Этап 6: Scenes - ЗАВЕРШЕН
- ✅ `BaseScene.ts` - Базовый класс сцены
- ✅ `LoadingScene.ts` - Загрузка ресурсов
- ✅ `MainScene.ts` - РЕФАКТОРИНГ ЗАВЕРШЕН - использует новые модули

---

**Статус:** Модульная архитектура полностью реализована! Все основные компоненты созданы и интегрированы. Проект готов к использованию.

