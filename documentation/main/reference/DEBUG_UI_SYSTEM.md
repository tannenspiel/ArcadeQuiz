# Система отладочного UI и HUD

**Версия:** 4.1
**Дата создания:** 2025-12-03
**Последнее обновление:** 2026-01-20

**⚠️ ВАЖНО:** Этот документ содержит справочную информацию о системе отладочного UI и HUD.
**📚 Актуальная документация по DebugOverlay:** [DEBUG_OVERLAY.md](DEBUG_OVERLAY.md) - Документация нового класса DebugOverlay
**📚 Подробный анализ:** [DEBUG_UI_DETAILED_ANALYSIS.md](DEBUG_UI_DETAILED_ANALYSIS.md) - Полный анализ системы масштабирования, letterboxing и позиционирования

**Статус документа:** Справочный. Информация про Debug Text устарела (перенесена в DebugOverlay). Информация про HUD (Keys HUD, Hint Text) и технические детали остаётся актуальной.

---

## Оглавление

1. [Обзор системы](#обзор-системы)
2. [Управление через переменную окружения](#управление-через-переменную-окружения)
3. [Архитектура элементов](#архитектура-элементов)
4. [Создание элементов](#создание-элементов)
5. [Позиционирование](#позиционирование)
6. [Обновление элементов](#обновление-элементов)
7. [Жизненный цикл](#жизненный-цикл)
8. [Константы и стили](#константы-и-стили)
9. [CSS стили для canvas](#css-стили-для-canvas)
10. [Координатные системы](#координатные-системы)
11. [Проблемы и решения](#проблемы-и-решения)

---

## Обзор системы

**⚠️ УСТАРЕЛО:** Отладочный UI теперь реализован в классе `DebugOverlay`. См. [DEBUG_OVERLAY.md](DEBUG_OVERLAY.md) для актуальной информации.

### Компоненты системы

Система отладочного UI и HUD состоит из трех основных элементов:

1. **Debug Text** (`debugText`) — отладочная информация в левом верхнем углу
   - Позиция: `(10, 10)` в виртуальных координатах камеры (фиксированное позиционирование)
   - Обновляется каждые 30 кадров (0.5 секунды при 60 FPS)
   - Показывает: позицию игрока, позицию камеры, количество ключей, статистику врагов
   - **Текущая реализация:** Упрощенное фиксированное позиционирование для диагностики

2. **Keys HUD** (`keysHUDText`) — счетчик ключей в правом верхнем углу
   - Позиция: `(cameraWidth - 20, 20)` — фиксированное позиционирование от правого края камеры
   - Обновляется при изменении количества ключей
   - Показывает: `Keys: X/3`

3. **Hint Text** (`hintText`) — подсказка внизу по центру
   - Позиция: `(cameraWidth / 2, cameraHeight - 60)` — фиксированное позиционирование от центра и низа камеры
   - Скрывается при активации оракула
   - Показывает: `Collect 3 Keys and find the Oracle!`

### Ключевые особенности

- **Все элементы используют `setScrollFactor(0)`** — позиционируются в координатах камеры, не двигаются с игровым миром
- **Создаются сразу после настройки камеры** — без задержек, так как камера уже готова
- **Используют виртуальные координаты камеры** — `cam.width` и `cam.height`
- **Высокий depth** — отображаются поверх всех игровых элементов
- **Управление через переменную окружения** — включение/выключение через `VITE_DEBUG_UI_ENABLED` (по умолчанию отключен)
- **✅ УПРОЩЕННОЕ ПОЗИЦИОНИРОВАНИЕ** — фиксированные координаты относительно камеры (временное решение для диагностики)

---

## Управление через переменную окружения

### Конфигурация

Отладочный UI управляется через переменную окружения `VITE_DEBUG_UI_ENABLED` в файле `src/config/gameConfig.ts`:

```typescript
// Настройки отладочного UI
export const DEBUG_UI_ENABLED = import.meta.env.VITE_DEBUG_UI_ENABLED === 'true';
```

**По умолчанию:** `false` (отладочный UI отключен)

### Использование

**Для включения отладочного UI:**

Создайте файл `.env` в корне проекта (или добавьте в существующий):

```env
VITE_DEBUG_UI_ENABLED=true
```

**Для отключения (по умолчанию):**

```env
VITE_DEBUG_UI_ENABLED=false
```

Или просто не указывайте эту переменную — по умолчанию UI будет отключен.

**Важно:** После изменения `.env` перезапустите dev-сервер (`npm run dev`), чтобы изменения вступили в силу.

### Применение в коде

Все вызовы методов создания и обновления отладочного UI обернуты в условие:

```typescript
// В MainScene.create()
if (DEBUG_UI_ENABLED) {
    this.createDebugUI();
}

if (DEBUG_UI_ENABLED) {
    this.createHUD();
}

// В MainScene.update()
if (DEBUG_UI_ENABLED) {
    this.updateDebugUI();
}

// При обновлении HUD
if (DEBUG_UI_ENABLED) {
    this.updateHUD();
}
```

**Файл конфигурации:** `src/config/gameConfig.ts` (строка **25**)

**Импорт в MainScene:** `src/game/scenes/MainScene.ts` (строка **14**)

---

## Архитектура элементов

**⚠️ УСТАРЕЛО:** Отладочный UI теперь реализован в классе `DebugOverlay` (`src/game/ui/DebugOverlay.ts`). См. [DEBUG_OVERLAY.md](DEBUG_OVERLAY.md) для актуальной информации.

### Новая архитектура: DebugOverlay

**Файл:** `src/game/ui/DebugOverlay.ts`

Отладочный UI вынесен в отдельный класс для улучшения модульности:

```typescript
export class DebugOverlay {
    private debugText!: Phaser.GameObjects.Text;
    private debugUpdateCounter: number = 0;
    private spawnMatrixGrid!: Phaser.GameObjects.Graphics;
    
    constructor(
        private scene: Phaser.Scene,
        private deps: DebugOverlayDependencies
    ) {}
}
```

**Использование в MainScene:**
- Создание: строки **226-237**
- Обновление: строки **560-561**, **1583-1584**
- Уничтожение: строки **3933-3938**

### Старая архитектура (устарело)

**Файл:** `src/game/scenes/MainScene.ts` (старая реализация)

**⚠️ Эта информация устарела. Реализация была вынесена в DebugOverlay.**

#### Объявление свойств класса (устарело)

```typescript
export default class MainScene extends BaseScene {
    // ... другие свойства ...
    
    // ✅ Отладочный UI (УСТАРЕЛО - теперь в DebugOverlay)
    private debugText!: Phaser.GameObjects.Text;
    
    // ✅ HUD элементы
    private keysHUDText!: Phaser.GameObjects.Text;
    private hintText!: Phaser.GameObjects.Text;
    
    // ✅ Счетчик обновлений для debug UI (обновляется каждые 30 кадров) (УСТАРЕЛО - теперь в DebugOverlay)
    private debugUpdateCounter: number = 0;
    
    // ... остальные свойства ...
}
```

---

## Создание элементов

### Порядок создания

Элементы создаются в методе `create()` класса `MainScene`:

```typescript
// В MainScene.create() (строка ~209)
this.setupCameraFollow(); // ✅ Настройка камеры ПЕРЕД созданием UI

// Настройка EventBus
this.setupEventBus();

// ✅ Создание UI сразу после настройки камеры (только если включен)
// Камера уже настроена в setupCameraFollow(), поэтому UI можно создавать сразу
// setScrollFactor(0) работает в координатах камеры, которые уже готовы
if (DEBUG_UI_ENABLED) {
    this.createDebugUI();  // Строка 221
}

if (DEBUG_UI_ENABLED) {
    this.createHUD();      // Строка 224
}
```

**Важно:** Элементы создаются **сразу после `setupCameraFollow()`**, без задержек. Это отличается от модальных окон, которые используют `delayedCall(1, ...)`.

**Почему без задержки:**
- Камера уже настроена в `setupCameraFollow()` → имеет корректные `width`, `height`, `scrollX/Y`
- `setScrollFactor(0)` не зависит от `getBoundingClientRect()` → работает сразу
- Это стандартный подход для UI с `setScrollFactor(0)` в Phaser

---

### Создание Debug Text

**⚠️ УСТАРЕЛО:** Метод `createDebugUI()` больше не находится в MainScene. Реализация перенесена в `DebugOverlay.createDebugUI()`.

**Актуальная реализация:** См. [DEBUG_OVERLAY.md](DEBUG_OVERLAY.md)

**Старая реализация (устарело):**

**Метод:** `createDebugUI()` (строки **1129-1152** в старой версии MainScene)

**Полный код (текущая упрощенная версия):**

```typescript
/**
 * Создание отладочного UI
 * ✅ УПРОЩЕНО: Фиксированное позиционирование относительно камеры (10, 10)
 * Временное решение для диагностики проблем видимости
 */
private createDebugUI(): void {
    // Удаляем старый текст, если он был (для надежности при ресайзе)
    if (this.debugText && this.debugText.active) {
        this.debugText.destroy();
    }
    
    // ✅ Временное решение для поиска текста - жесткое фиксированное позиционирование
    this.debugText = this.add.text(10, 10, 'DEBUG UI FOUND', {
        fontSize: '32px',
        color: '#00ff00', // Ярко-зеленый для максимальной заметности
        backgroundColor: '#000000' // Черный фон
    })
     .setScrollFactor(0) // Приклеить к камере
     .setOrigin(0, 0)    // Якорь в левый верхний угол
     .setDepth(999999);  // Поверх всего
    
    console.log('✅ Debug UI created at fixed position (10, 10) with setScrollFactor(0)');
    console.log('✅ Debug UI - Element visible:', this.debugText.visible, 'active:', this.debugText.active, 'depth:', this.debugText.depth);
}
```

**Ключевые моменты:**

1. **Позиция:** `(10, 10)` — **абсолютно жестко заданные координаты** в виртуальных пикселях камеры, без использования `cameraWidth` или `cameraHeight`
2. **Origin:** `(0, 0)` — левый верхний угол элемента совпадает с позицией
3. **Depth:** `999999` — максимальный depth, отображается поверх всех элементов
4. **ScrollFactor:** `0` — элемент не двигается с камерой, остается в фиксированной позиции относительно виртуального экрана
5. **Стили:** Временные жестко заданные стили для диагностики (32px, зеленый цвет, черный фон)

**Примечание:** Это **максимально упрощенная версия** с абсолютно жестко заданными координатами `(10, 10)` для диагностики проблем видимости. После решения проблемы стили будут заменены на константы из `src/constants/textStyles.ts`.

---

### Создание HUD (Keys и Hint)

**Метод:** `createHUD()` (строки **4000-4050**)

**Полный код (текущая упрощенная версия):**

```typescript
/**
 * Создание HUD в Phaser (Keys и подсказки)
 * ✅ УПРОЩЕНО: Фиксированное позиционирование относительно камеры
 * Временное решение для диагностики проблем видимости
 */
private createHUD(): void {
    const cameraWidth = this.cameras.main.width;
    const cameraHeight = this.cameras.main.height;
    
    // Удаляем старые элементы, если есть
    if (this.keysHUDText && this.keysHUDText.active) {
        this.keysHUDText.destroy();
    }
    if (this.hintText && this.hintText.active) {
        this.hintText.destroy();
    }
    
    // --- KEYS HUD (Правый верхний угол) ---
    // ✅ УПРОЩЕНО: Фиксированное позиционирование от правого края камеры
    this.keysHUDText = this.add.text(
        cameraWidth - 20, // Правый край с отступом 20px
        20,              // Верхний край с отступом 20px
        `Keys: ${this.gameState.getKeys()}/${this.MAX_KEYS}`,
        {
            fontSize: '24px',
            fontFamily: DEFAULT_FONT_FAMILY,
            fontStyle: 'bold',
            color: '#00ff00',
            stroke: '#000000',
            strokeThickness: 4,
            backgroundColor: '#000000',
            padding: { x: 10, y: 5 }
        }
    ).setOrigin(1, 0) // Якорь справа сверху
     .setDepth(1000)
     .setScrollFactor(0)
     .setVisible(true);
    
    // --- HINT TEXT (Внизу по центру) ---
    // ✅ УПРОЩЕНО: Фиксированное позиционирование от центра и низа камеры
    this.hintText = this.add.text(
        cameraWidth / 2,  // Центр по X
        cameraHeight - 60, // Низ с отступом 60px
        'Collect 3 Keys and find the Oracle!',
        {
            fontSize: '20px',
            fontFamily: DEFAULT_FONT_FAMILY,
            fontStyle: 'bold',
            color: '#ffff00',
            stroke: '#000000',
            strokeThickness: 4,
            backgroundColor: 'rgba(0,0,0,0.5)',
            padding: { x: 15, y: 8 }
        }
    ).setOrigin(0.5, 0.5) // Центр
     .setDepth(1000)
     .setScrollFactor(0)
     .setVisible(true);
    
     // Скрываем подсказку, если оракул уже активирован (при ресайзе восстанавливаем состояние)
     if (this.isOracleActivated) {
         this.hintText.setVisible(false);
     }
     
     console.log('✅ HUD created with fixed positioning');
     console.log(`✅ Keys HUD: (${cameraWidth - 20}, 20), Hint Text: (${cameraWidth / 2}, ${cameraHeight - 60})`);
}
```

**Ключевые моменты:**

1. **Keys HUD:**
   - Позиция: `(cameraWidth - 20, 20)` — **относительное позиционирование** от правого края камеры (использует `cameraWidth`)
   - Origin: `(1, 0)` — правый верхний угол элемента совпадает с позицией
   - Расчет позиции: `cameraWidth - 20` (без сложных вычислений масштаба, но все еще использует размер камеры)

2. **Hint Text:**
   - Позиция: `(cameraWidth / 2, cameraHeight - 60)` — **относительное позиционирование** от центра и низа камеры (использует `cameraWidth` и `cameraHeight`)
   - Origin: `(0.5, 0.5)` — центр элемента совпадает с позицией
   - Расчет позиции: `cameraWidth / 2` по X, `cameraHeight - 60` по Y (без сложных вычислений масштаба, но все еще использует размеры камеры)

3. **Упрощение:**
   - Убраны все вычисления масштаба (`scaleX`, `scaleY`)
   - Убраны вызовы `getBoundingClientRect()`
   - Используются только виртуальные координаты камеры (`cameraWidth`, `cameraHeight`)

**Важно:** В отличие от Debug Text, который использует **абсолютно жестко заданные координаты** `(10, 10)`, Keys HUD и Hint Text используют **относительное позиционирование** на основе размеров камеры. Это все еще упрощенная версия, но не максимально упрощенная.

---

## Позиционирование

### Координатная система

Все элементы UI используют **виртуальные координаты камеры**:

- **X:** от `0` до `cameraWidth` (адаптивная ширина, например 2560 на ultrawide)
- **Y:** от `0` до `cameraHeight` (фиксированная высота 1280)

**Важно:** `cameraWidth` и `cameraHeight` — это размеры виртуального экрана, а не реального canvas.

### Позиции элементов (упрощенная версия)

| Элемент | X | Y | Origin | Тип позиционирования | Обоснование |
|---------|---|---|--------|---------------------|-------------|
| **Debug Text** | `10` | `10` | `(0, 0)` | **Абсолютно жестко заданное** | Левый верхний угол, абсолютные координаты без использования размеров камеры |
| **Keys HUD** | `cameraWidth - 20` | `20` | `(1, 0)` | **Относительное** | Правый верхний угол, использует `cameraWidth` для расчета позиции |
| **Hint Text** | `cameraWidth / 2` | `cameraHeight - 60` | `(0.5, 0.5)` | **Относительное** | Центр по X, внизу, использует `cameraWidth` и `cameraHeight` для расчета позиции |

### Уровни упрощения

**1. Максимально упрощенная версия (Debug Text):**
- **Абсолютно жестко заданные координаты** `(10, 10)`
- Не использует `cameraWidth` или `cameraHeight`
- Простейший вариант для диагностики проблем видимости

**2. Частично упрощенная версия (Keys HUD, Hint Text):**
- **Относительное позиционирование** на основе размеров камеры
- Использует `cameraWidth` и `cameraHeight`, но без сложных вычислений масштаба
- Убраны вызовы `getBoundingClientRect()` и вычисления `scaleX`/`scaleY`

### Почему упрощено?

**Проблема:** Сложные вычисления масштаба (`scaleX`, `scaleY`) и использование `getBoundingClientRect()` приводили к тому, что элементы не были видны на экране.

**Решение:** 
- **Debug Text:** Использовать абсолютно жестко заданные координаты `(10, 10)` для максимального упрощения
- **Keys HUD и Hint Text:** Использовать простые относительные координаты на основе размеров камеры, без сложных вычислений масштаба

**Результат:** Элементы всегда видны в виртуальных координатах камеры, независимо от letterboxing или масштабирования.

---

## Обновление элементов

### Debug Text

**Метод:** `updateDebugUI()` (строки **1338-1407**)

**Вызов:** В методе `update()` каждые 30 кадров (0.5 секунды при 60 FPS)

**Полный код:**

```typescript
private updateDebugUI(): void {
    // ✅ ВАЖНО: Проверяем все необходимые объекты перед использованием
    if (!this.debugText || !this.player || !this.gameState) return;
    
    // ✅ Проверяем, что debugText не уничтожен И активен
    if (!this.debugText.active) {
        console.warn('⚠️ Debug Text is not active or destroyed.');
        return;
    }

    this.debugUpdateCounter++;
    // ✅ Обновляем каждые 30 кадров (0.5 сек при 60 FPS)
    if (this.debugUpdateCounter % 30 !== 0) {
        // ✅ Обновляем сетку матрицы чаще (каждые 10 кадров) для более плавной визуализации
        if (this.debugUpdateCounter % 10 === 0) {
            this.updateSpawnMatrixGrid();
        }
        return;
    }
    
    // ✅ Обновляем сетку матрицы при обновлении debug UI
    this.updateSpawnMatrixGrid();

    const playerPos = this.player.getPosition();
    const cam = this.cameras.main;
    
    // ✅ Упрощенная информация без лишних вычислений
    // ✅ Проверяем enemyInstances перед фильтрацией
    const activeEnemies = this.enemyInstances && Array.isArray(this.enemyInstances)
        ? this.enemyInstances.filter(e => e && e.getSprite && e.getSprite().active && e.isActive())
        : [];
    
    // ✅ Подсчитываем врагов по типам и их жизни
    const enemyStats = {
        randomWalker: { count: 0, totalHealth: 0 },
        chaser: { count: 0, totalHealth: 0 },
        flam: { count: 0, totalHealth: 0 }
    };
    
    activeEnemies.forEach(enemy => {
        const type = enemy.getType();
        const health = enemy.getHealth();
        
        if (type === EnemyType.RANDOM_WALKER) {
            enemyStats.randomWalker.count++;
            enemyStats.randomWalker.totalHealth += health;
        } else if (type === EnemyType.CHASER) {
            enemyStats.chaser.count++;
            enemyStats.chaser.totalHealth += health;
        } else if (type === EnemyType.FLAM) {
            enemyStats.flam.count++;
            enemyStats.flam.totalHealth += health;
        }
    });
    
    const debugInfo = [
        `Player: ${playerPos.x|0}x${playerPos.y|0}`,
        `Camera: ${cam.scrollX|0}x${cam.scrollY|0}`,
        `Keys: ${this.gameState.getKeys()}/${this.MAX_KEYS}`,
        `Enemies: ${activeEnemies.length}`,
        `  Beast: ${enemyStats.randomWalker.count} (HP: ${enemyStats.randomWalker.totalHealth})`,
        `  Dragon: ${enemyStats.chaser.count} (HP: ${enemyStats.chaser.totalHealth})`,
        `  Flam: ${enemyStats.flam.count} (HP: ${enemyStats.flam.totalHealth})`
    ].join('\n');
    
    // ✅ Проверяем, что setText доступен
    if (this.debugText.setText) {
        this.debugText.setText(debugInfo);
    }
}
```

**Отображаемая информация:**

1. **Player:** Позиция игрока в виртуальных координатах игрового мира (0-2048)
2. **Camera:** Позиция камеры (scrollX, scrollY) в виртуальных координатах игрового мира
3. **Keys:** Количество собранных ключей / максимальное количество (3)
4. **Enemies:** Общее количество активных врагов
5. **Статистика по типам врагов:**
   - **Beast** (RandomWalker): количество и суммарное здоровье
   - **Dragon** (Chaser): количество и суммарное здоровье
   - **Flam** (Flam): количество и суммарное здоровье

**Частота обновления:** Каждые 30 кадров (0.5 секунды при 60 FPS) для оптимизации производительности.

---

### HUD (Keys и Hint)

**Метод:** `updateHUD()` (строки **4119-4128**)

**Вызов:** При изменении количества ключей или активации оракула

**Полный код:**

```typescript
private updateHUD(): void {
    if (this.keysHUDText) {
        this.keysHUDText.setText(`Keys: ${this.gameState.getKeys()}/${this.MAX_KEYS}`);
    }

    if (this.hintText) {
        // Скрываем подсказку, если оракул активирован
        this.hintText.setVisible(!this.isOracleActivated);
    }
}
```

**Когда вызывается:**

1. **При создании HUD:** В конце `createHUD()` (строка **4050**)
2. **При сборе ключа:** В методах обработки сбора ключей
3. **При активации оракула:** В методе активации оракула

**Обновляемые данные:**

- **Keys HUD:** Текст обновляется при изменении количества ключей
- **Hint Text:** Видимость меняется при активации оракула (скрывается)

---

## Жизненный цикл

### Создание

1. **В `MainScene.create()`:**
   ```typescript
   this.setupCameraFollow(); // Настройка камеры
   this.createDebugUI();      // Создание debug text
   this.createHUD();          // Создание HUD
   ```

2. **Порядок важен:** Камера должна быть настроена перед созданием UI элементов.

### Обновление

1. **Debug Text:** Обновляется в `update()` каждые 30 кадров (только если `DEBUG_UI_ENABLED === true`)
2. **Keys HUD:** Обновляется при изменении количества ключей через `updateHUD()` (только если `DEBUG_UI_ENABLED === true`)
3. **Hint Text:** Видимость обновляется при активации оракула через `updateHUD()` (только если `DEBUG_UI_ENABLED === true`)

**Важно:** Все вызовы `updateDebugUI()` и `updateHUD()` обернуты в условие `if (DEBUG_UI_ENABLED)`, поэтому при отключенном UI обновления не выполняются.

### Уничтожение

**Метод:** `shutdown()` или `destroy()` (строки **3810-3817**)

```typescript
// ✅ Очищаем debugText (он будет пересоздан в create())
if (this.debugText && this.debugText.destroy) {
    try {
        this.debugText.destroy();
    } catch (e) {
        console.warn('⚠️ Error destroying debugText:', e);
    }
    this.debugText = null as any; // ✅ Сбрасываем ссылку
}
```

**Важно:** HUD элементы (`keysHUDText`, `hintText`) уничтожаются автоматически при уничтожении сцены Phaser.

---

## Константы и стили

### Константы из `src/constants/textStyles.ts`

**Debug Text (будущие константы, после возврата к нормальной версии):**

```typescript
export const DEBUG_FONT_FAMILY = 'monospace'; // Строка 14
export const DEBUG_TEXT_FONT_SIZE = 14; // Строка 86
export const DEBUG_TEXT_FONT_STYLE = FONT_STYLE.NORMAL; // Строка 118
export const DEBUG_TEXT_COLOR = TEXT_COLORS.DEBUG_GREEN; // Строка 167
export const DEBUG_TEXT_BACKGROUND_COLOR = TEXT_COLORS.BLACK; // Строка 168
```

**DEFAULT_FONT_FAMILY:**

```typescript
export const DEFAULT_FONT_FAMILY = 'Nunito'; // Используется для HUD элементов (строка 13)
```

**Примечание:** В текущей упрощенной версии `createDebugUI()` используются жестко заданные стили (`32px`, `#00ff00`, `#000000`) вместо констант. После решения проблем видимости стили будут заменены на константы.

### Стили элементов

| Элемент | Font Size | Font Family | Color | Background | Stroke |
|---------|-----------|-------------|-------|------------|--------|
| **Debug Text** (текущая версия) | `32px` (жестко задано) | Не задано | `#00ff00` (жестко задано) | `#000000` (жестко задано) | Нет |
| **Debug Text** (будущая версия) | `14px` | `monospace` | `DEBUG_GREEN` | `BLACK` | Нет |
| **Keys HUD** | `24px` | `Nunito` | `#00ff00` (зеленый) | `#000000` (черный) | `#000000`, толщина 4 |
| **Hint Text** | `20px` | `Nunito` | `#ffff00` (желтый) | `rgba(0,0,0,0.5)` (полупрозрачный черный) | `#000000`, толщина 4 |

---

## CSS стили для canvas

### Файл: `index.html`

**Расположение:** Строки **13-51**

**Полный код CSS:**

```html
<style>
    html, body {
        height: 100%;
        width: 100%;
        margin: 0;
        padding: 0;
        position: fixed;
        overflow: hidden;
        top: 0;
        left: 0;
        background: #000;
        touch-action: none;
    }
    #root {
        width: 100%;
        height: 100%;
        position: fixed;
        top: 0;
        left: 0;
        overflow: hidden;
    }
    #game-container { 
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        overflow: hidden;
        /* ✅ Убрали display: flex - Phaser сам центрирует canvas через autoCenter: CENTER_BOTH */
        /* Flexbox может конфликтовать с Phaser центрированием при повороте экрана */
    }
    
    /* Для мобильных устройств используем visualViewport */
    @supports (height: 100dvh) {
        #game-container {
            height: 100dvh;
            width: 100dvw;
        }
    }
</style>
```

**Ключевые особенности:**

1. **`html, body`:** Фиксированная позиция, полная высота/ширина, скрыт overflow, черный фон
2. **`#root`:** Фиксированная позиция, полная высота/ширина, скрыт overflow
3. **`#game-container`:** Фиксированная позиция, полная высота/ширина (`100vw`/`100vh`), скрыт overflow, **явно убран `display: flex`** для избежания конфликтов с Phaser центрированием
4. **Мобильная поддержка:** Используется `100dvh`/`100dvw` для корректной работы на мобильных устройствах

**Важно:** CSS стили в `index.html` управляют контейнером для canvas, но не самим canvas элементом. Phaser создает canvas внутри `#game-container` и управляет его размерами и позиционированием через `Phaser.Scale.FIT`.

### Файл: `src/react/App.tsx`

**Расположение:** Строки **12-16**

**Полный код:**

```typescript
return (
  <div className="w-screen h-screen overflow-hidden bg-gray-900">
    {/* Phaser Game - заполняет всю область */}
    <div className="absolute inset-0">
      <PhaserGame ref={gameRef} />
    </div>
  </div>
);
```

**Проблема:** Используются Tailwind CSS классы (`w-screen`, `h-screen`, `overflow-hidden`, `bg-gray-900`, `absolute`, `inset-0`), но **Tailwind CSS не настроен и не импортирован в проекте**, поэтому эти классы **не работают**.

**Результат:** Стили из `App.tsx` не применяются, реальные стили для контейнера определяются только в `index.html`.

**Рекомендация:** Либо настроить Tailwind CSS, либо заменить классы на обычные CSS стили или удалить их, так как стили уже определены в `index.html`.

---

## Координатные системы

### Виртуальные координаты камеры

**Используются для:** Всех UI элементов с `setScrollFactor(0)`

**Диапазон:**
- **X:** `0` до `cameraWidth` (адаптивная ширина, например 2560 на ultrawide)
- **Y:** `0` до `cameraHeight` (фиксированная высота 1280)

**Получение размеров:**
```typescript
const cameraWidth = this.cameras.main.width;
const cameraHeight = this.cameras.main.height;
```

### Виртуальные координаты игрового мира

**Используются для:** Игровых объектов (игрок, враги, карта)

**Диапазон:**
- **X:** `0` до `2048` (MAP_WIDTH * BASE_SCALE)
- **Y:** `0` до `2048` (MAP_HEIGHT * BASE_SCALE)

**Получение позиции игрока:**
```typescript
const playerPos = this.player.getPosition(); // { x: 0-2048, y: 0-2048 }
```

### Реальные координаты canvas

**Используются для:** (В текущей упрощенной версии не используются)

**Получение:**
```typescript
const canvasRect = this.game.canvas?.getBoundingClientRect();
const canvasWidth = canvasRect?.width;  // Реальные CSS-пиксели
const canvasHeight = canvasRect?.height; // Реальные CSS-пиксели
```

**Примечание:** В упрощенной версии позиционирования эти координаты не используются, так как все элементы позиционируются напрямую в виртуальных координатах камеры.

---

## Проблемы и решения

### Проблема 1: Элементы не видны на экране

**Симптомы:** Debug UI и HUD создаются, но не отображаются на экране.

**Причина:** Сложные вычисления масштаба и использование `getBoundingClientRect()` приводили к неправильному позиционированию элементов.

**Решение:** Упростить позиционирование до фиксированных координат относительно камеры:

```typescript
// Debug Text
this.debugText = this.add.text(10, 10, 'DEBUG UI FOUND', { /* ... */ })
    .setScrollFactor(0)
    .setOrigin(0, 0)
    .setDepth(999999);

// Keys HUD
this.keysHUDText = this.add.text(cameraWidth - 20, 20, `Keys: ...`, { /* ... */ })
    .setOrigin(1, 0)
    .setScrollFactor(0);

// Hint Text
this.hintText = this.add.text(cameraWidth / 2, cameraHeight - 60, '...', { /* ... */ })
    .setOrigin(0.5, 0.5)
    .setScrollFactor(0);
```

**Статус:** ✅ Решено в упрощенной версии `createDebugUI()` (строки **1129-1150**) и `createHUD()` (строки **4000-4050**)

---

### Проблема 2: Элементы создаются до инициализации камеры

**Симптомы:** Элементы создаются в неправильных координатах или не создаются вообще.

**Причина:** Элементы создаются до того, как камера настроена в `setupCameraFollow()`.

**Решение:** Создавать элементы сразу после `setupCameraFollow()`, без задержек:

```typescript
this.setupCameraFollow(); // Настройка камеры
this.createDebugUI();      // Создание UI (камера уже готова)
this.createHUD();          // Создание HUD (камера уже готова)
```

**Статус:** ✅ Решено в `create()` (строки **209, 221, 224**)

---

### Проблема 3: Debug Text обновляется слишком часто

**Симптомы:** Снижение производительности из-за частых обновлений текста.

**Причина:** Обновление в каждом кадре `update()`.

**Решение:** Обновлять каждые 30 кадров (0.5 секунды при 60 FPS):

```typescript
this.debugUpdateCounter++;
if (this.debugUpdateCounter % 30 !== 0) return;
```

**Статус:** ✅ Решено в `updateDebugUI()` (строки **1348-1356**)

---

## Файлы системы

### Основные файлы

**Актуальная реализация:**
- **`src/game/ui/DebugOverlay.ts`** — класс для управления отладочным UI
  - `create()` — создание отладочного UI
  - `createSpawnMatrixGrid()` — создание сетки матрицы спавна
  - `update()` — обновление UI и сетки
  - `destroy()` — уничтожение элементов
  - См. [DEBUG_OVERLAY.md](DEBUG_OVERLAY.md) для полной документации

**Устаревшие файлы (информация для справки):**
- **`src/game/scenes/MainScene.ts`** — основной файл с методами создания и обновления UI (устарело)
  - `createDebugUI()` — строки **1129-1150** (устарело, теперь в DebugOverlay)
  - `updateDebugUI()` — строки **1338-1407** (устарело, теперь в DebugOverlay)
  - `createHUD()` — строки **4000-4050** (актуально, HUD остается в MainScene)
  - `updateHUD()` — строки **4119-4128** (актуально, HUD остается в MainScene)

### Константы

- **`src/constants/textStyles.ts`** — константы стилей для debug text
  - `DEBUG_FONT_FAMILY` — строка **14**
  - `DEBUG_TEXT_FONT_SIZE` — строка **86**
  - `DEBUG_TEXT_FONT_STYLE` — строка **118**
  - `DEBUG_TEXT_COLOR` — строка **167**
  - `DEBUG_TEXT_BACKGROUND_COLOR` — строка **168**
  - `DEFAULT_FONT_FAMILY` — строка **13**

### CSS стили

- **`index.html`** — CSS стили для контейнера canvas (строки **13-51**)
  - Стили для `html`, `body`, `#root`, `#game-container`
  - Мобильная поддержка через `@supports (height: 100dvh)`

- **`src/react/App.tsx`** — React компонент с Tailwind CSS классами (строки **12-16**)
  - **Проблема:** Tailwind CSS не настроен, классы не работают
  - Стили из этого файла не применяются

### Связанные файлы

- **`src/config/gameConfig.ts`** — конфигурация `DEBUG_UI_ENABLED` (строка **25**)
- **`src/constants/gameConstants.ts`** — константы игры (MAX_KEYS и др.)
- **`src/game/core/GameState.ts`** — состояние игры (количество ключей)

---

## Заключение

**⚠️ ВАЖНО:** Отладочный UI был вынесен в отдельный класс `DebugOverlay` для улучшения модульности. См. [DEBUG_OVERLAY.md](DEBUG_OVERLAY.md) для актуальной информации.

Система отладочного UI и HUD обеспечивает:

- ✅ **Модульность** — отладочный UI изолирован в отдельном классе `DebugOverlay`
- ✅ **Надежность** — элементы всегда видны и правильно позиционированы
- ✅ **Производительность** — оптимизированное обновление (каждые 30 кадров для debug)
- ✅ **Адаптивность** — корректная работа на всех экранах через виртуальные координаты камеры
- ✅ **Простота** — упрощенное позиционирование без сложных вычислений масштаба

Все элементы используют виртуальные координаты камеры с `setScrollFactor(0)`, что обеспечивает их фиксированное позиционирование относительно виртуального экрана, независимо от движения камеры по игровому миру.

**Текущее состояние:** Отладочный UI реализован в классе `DebugOverlay` (`src/game/ui/DebugOverlay.ts`). HUD элементы (Keys HUD, Hint Text) остаются в `MainScene`.
